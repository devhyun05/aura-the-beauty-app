# 아우라딘 검색/추천 엔진 세션 인사이트 정리

## 1. 전체 평가

현재 아우라딘 검색/추천 엔진은 단순 GPT 추천이 아니라, 검색엔진에 가까운 구조를 가지고 있다.

핵심 구조는 다음과 같다.

```text
Intent Parsing
→ Candidate Retrieval
→ Rule Score
→ Semantic Score
→ Question Engine
→ MMR
→ Recommendation Cards
→ Interaction Logging
```

이 구조는 매우 좋다. 특히 다음 세 가지가 잘 설계되어 있다.

1. **Rule Score와 Semantic Score를 분리한 점**
2. **MMR로 중복 추천을 줄이는 점**
3. **정보이득 기반 Question Engine으로 후보를 능동적으로 좁히는 점**

다만 문서에는 “무엇을 한다”는 설명은 잘 되어 있지만, **왜 그렇게 하는지**, **어떤 값이 어디서 오는지**, **빈 부분을 어떻게 구현해야 하는지**가 일부 부족하다.

---

## 2. Rule Score 관련 보완점

현재 Rule Score는 profile이 있으면 기본 74점에서 시작하고, 색상·finish·skinType·feature·tone 일치 여부에 따라 가산/감점하는 구조다. 색상 일치는 개당 +12점, finish/effects는 +4점, skinTypes는 +4점, features는 +3점, tones는 +7점, 색상 완전 일치 bonus는 +10점이다. 반대로 웜/쿨톤 충돌은 -8점, 색상 불일치는 -12점, 검색 결과 index penalty도 적용된다.

### 부족한 설명

문서에는 점수표는 있지만, **왜 색상은 12점이고 finish는 4점인지** 설명이 없다. 현재 값들은 학습된 값이 아니라 휴리스틱이다. 즉, 사람이 “립/치크/섀도우 추천에서는 색상이 중요하다”고 보고 경험적으로 정한 값이다.

### 전달할 인사이트

Rule Score는 “정답 확률”이 아니라 **사용자 조건과 상품 속성의 일치도를 나타내는 UX용 점수**다. 따라서 74점, 62점 하한, 99점 상한 같은 값은 절대적 의미보다 “사용자에게 너무 낮거나 너무 과장된 점수로 보이지 않게 하는 보정값”에 가깝다.

### 개선 제안

Rule Score 공식 옆에 다음 설명을 추가하는 것이 좋다.

```text
Rule Score는 검색 후보가 이미 어느 정도 관련성이 있다는 전제에서 시작하는 UX match score다.
74점은 기본 신뢰 점수이며, 이후 색상/톤/finish/피부타입/기능 일치 여부로 상승 또는 하락한다.
현재 가중치는 학습값이 아니라 MVP 휴리스틱이며, 추후 클릭/저장/구매/AR 적용 로그 기반으로 조정한다.
```

---

## 3. Product Specs 생성 과정이 가장 큰 빈틈

현재 Rule Score는 `specs.colors`, `specs.effects`, `specs.skinTypes`, `specs.tones`, `specs.features` 같은 구조화된 상품 속성이 있다고 가정한다. 실제 구현은 Naver item → `_map_naver_item` → `_extract_product_specs` → `_score_product_match` 흐름이다.

문서에는 상품 title/category/brand/maker/source payload에서 colors, effects, skinTypes, tones, features 등을 추출한다고 되어 있고, term list도 있다. 예를 들어 colors는 베이지·핑크·코랄·로즈·피치 등, finishes는 매트·세미매트·글로우·글로시 등, skin types는 모든피부용·건성·지성·복합성 등이다.

### 부족한 설명

상품이 정말 “건성용”인지, “글로우 finish”인지, “웜톤 적합”인지는 현재 규칙 기반 term matching에 크게 의존한다. 그런데 네이버 API는 화장품 전용 색상, 제형, 피부타입, 마감감, 리뷰 키워드, 컬러칩 같은 구조화 metadata를 직접 제공하지 않는다.

즉, 현재 구조는 **상품 spec이 이미 있다고 가정하지만, spec을 신뢰성 있게 만드는 Product Enrichment 파이프라인은 아직 약하다.**

### 전달할 인사이트

Rule Score의 품질은 Rule 공식보다 **상품 spec 추출 품질**에 더 크게 좌우된다.
좋은 Rule Score라도 상품 metadata가 부정확하면 추천 품질이 흔들린다.

### 개선 제안

검색 시점에 LLM을 매번 쓰기보다, 상품 수집/갱신 시점에 LLM을 한 번 사용해 Product Enrichment를 수행하는 것이 좋다.

```text
Naver API / 브랜드 공식몰 / catalog row
→ 상품명 + 브랜드 + 카테고리 + 상세설명
→ LLM 또는 rule enrichment
→ colorFamily, finish, texture, undertone, skinTypes, features, confidence 저장
→ 추천 시에는 저장된 catalog spec만 사용
```

이렇게 하면 검색 시점은 빠르고 결정론적으로 유지하면서, 상품 metadata 품질을 높일 수 있다.

---

## 4. 사용자 Profile → Target Terms 변환은 잘 설계됨

문서에서 좋은 부분은 사용자 보고서를 검색 가능한 target terms로 바꾸는 부분이다.

`_profile_text(profile, category)`는 추천 메이크업 카드, personalColor, skinType, toneSummary, recommendedMood, report summary, makeupGuideline 등을 합쳐 검색 가능한 텍스트를 만든다. 이후 `_target_terms(profile, category)`가 colors, features, finishes, skinTypes, tones를 뽑는다.

예를 들어 skinType이 건성이면 `촉촉`, `글로우`, `윤광`, `건성`을 추가하고, 지성/복합성이면 `세미매트`, `매트`, `보송`을 추가한다.

### 좋은 점

사용자가 직접 “촉촉한 제품 원해요”라고 말하지 않아도, 보고서의 피부타입을 바탕으로 검색 조건을 자동 확장한다. 이건 단순 검색이 아니라 추천 시스템다운 설계다.

### 개선 제안

이 규칙은 문서에서 더 강조해야 한다.

```text
사용자 profile은 단순 설명문이 아니라 검색 조건 생성의 원천이다.
예: 건성 → 촉촉/글로우/윤광 target 추가
예: 봄웜/가을웜 → 웜톤 target 추가
예: 여름쿨/겨울쿨 → 쿨톤 target 추가
```

---

## 5. Semantic Score의 핵심은 “무엇을 임베딩하느냐”

현재 semantic scoring은 Bedrock Titan Embedding을 사용한다. profile embedding text와 product embedding text를 각각 만들고, 코사인 유사도를 0~100 semantic rate로 변환한다. 이후 Rule Score 65%, Semantic Score 35%로 결합한다.

### 중요한 인사이트

Embedding의 핵심은 “문장을 벡터로 바꾼다”가 아니라, **어떤 텍스트를 벡터화하느냐**다.

현재 문서에는 profile embedding text에 추천 카테고리, 핵심 색상 조건, 분석 보고서 text, 추천 타깃 특징이 들어가고, product embedding text에는 상품 카테고리, 핵심 색상, 브랜드, 상품명/호수, 태그, productInfo, 추천 이유가 들어간다.

### 개선 제안

FaceProfile이나 분석 보고서 raw text를 그대로 넣기보다, LLM이 검색 최적화용 Search Profile Text로 변환한 뒤 임베딩하는 것을 고려할 수 있다.

예:

```text
원본 profile:
봄웜 라이트, 대비 낮음, 자연스러운 메이크업 선호

Search Profile Text:
이 사용자는 봄웜 라이트에 가까우며, 코랄·피치 계열의 맑고 투명한 발색이 잘 어울린다.
강한 대비보다 자연스럽고 촉촉한 데일리 메이크업이 적합하다.
```

이 문장을 임베딩하면 semantic score가 더 안정적일 수 있다.

---

## 6. `_color_match_adjustment`는 문서 보완 필요

현재 문서에는 Rule Score 안에 이미 `COLOR_MATCH_BONUS = 10`이 들어간다. 그런데 semantic 결합 이후 `_color_match_adjustment`를 다시 더한다고 되어 있다.

### 문제

문서에는 `_color_match_adjustment`를 호출한다고만 되어 있고, 내부 공식이 없다. 따라서 색상이 Rule Score 안에서 한 번, 최종 adjustment에서 한 번 더 반영되는 구조로 보이지만, 왜 두 번 보는지 설명이 부족하다.

### 개선 제안

둘 중 하나로 정리하는 것이 좋다.

#### 선택지 A: 색상 보정 이유를 명시

```text
Rule Score의 color bonus는 명시적 term match를 반영한다.
_color_match_adjustment는 semantic rerank 이후에도 립/치크/섀도우처럼 색상이 핵심인 카테고리에서 색상 불일치가 최종 순위를 뒤집지 않도록 하는 안전 보정이다.
```

#### 선택지 B: 더 일반화

`_color_match_adjustment`를 `_final_adjustment` 또는 `_category_adjustment`로 일반화한다.

```text
lip → color 중요
base → skin tone / finish 중요
shadow → finish / shimmer 중요
channel → 구매 가능성 중요
```

이렇게 하면 색상뿐 아니라 재고, 브랜드 제외, 선호 쇼핑몰, 신상품, 구매 가능성까지 마지막 보정 단계에서 다룰 수 있다.

---

## 7. Question Engine은 문서에서 가장 강한 기술적 챌린지

현재 Question Engine은 “LLM이 감으로 질문하는 구조”가 아니라, 후보군을 보고 가장 가치 있는 질문을 서버가 결정적으로 고르는 구조다. 문서에서도 `askNextQuestion`은 LLM 감으로 고르지 않고, 후보군을 보고 가장 가치 있는 질문을 고른다고 명시되어 있다.

핵심 질문은 다음이다.

```text
지금 이 후보군에서 어떤 속성을 물어보면 후보가 가장 잘 갈리는가?
```

문서상 gain 공식은 다음과 같다.

```text
gain =
entropy
× coverage
× confidence
× uxPriority
× actionability
```

또는 실행 스펙에서는 다음과 같이 표현된다.

```text
gain(a) = H(C, a) * coverage(a) * uxPriority(a) * confidence(a)
```

### 핵심 개념 정리

#### Entropy

속성값 분포가 얼마나 균등하게 갈리는지를 본다.

좋은 예:

```text
finish:
glossy 10개
matte 10개
```

나쁜 예:

```text
finish:
glossy 19개
matte 1개
```

중요한 점은 “무조건 두 개로 나누는 것”이 좋은 게 아니라, **선택지가 몇 개든 균등하게 갈리는 것이 좋다**는 것이다.

```text
pink 7
coral 7
rose 6
```

처럼 3개로 균등하게 갈려도 좋은 질문이다.

#### Coverage

Coverage는 해당 속성을 알고 있는 후보 비율이다.

```text
coverage = 속성값이 있는 후보 수 / 전체 후보 수
```

예:

```text
후보 20개 중 finish를 아는 상품 18개
coverage = 18 / 20 = 0.9
```

문서에도 `known = [v for v in values if v]`, `coverage = len(known) / len(items)` 식으로 계산한다고 되어 있다.

#### Confidence

Coverage는 “값이 있느냐”이고, confidence는 “그 값을 믿을 수 있느냐”다.

예:

```text
상품명에 "매트"가 명시됨 → confidence 높음
LLM이 "아마 매트일 듯" 추론 → confidence 낮음
```

문서에는 confidence가 공식에 들어가지만, confidence를 어떻게 산출하는지는 아직 명확하지 않다.

### 개선 제안

Confidence 산출 규칙을 문서에 추가해야 한다.

예:

| 출처                     | confidence 예시 |
| ---------------------- | ------------: |
| 사람이 검수한 catalog        |          0.98 |
| 브랜드 공식 DB              |          0.95 |
| 상품명 명시 term match      |          0.85 |
| 상세설명 rule extraction   |          0.80 |
| LLM enrichment + 근거 있음 |     0.65~0.80 |
| fallback 추정            |     0.40~0.55 |

---

## 8. “균등하게 나누는 질문”이 좋은 이유도 설명 필요

문서에는 entropy 기반 선택이 있다고 되어 있지만, 왜 균등한 분포가 좋은지 설명이 부족하다.

### 핵심 설명

후보 100개가 있을 때 다음 두 질문을 비교한다.

#### 질문 A

```text
50개 / 50개
```

사용자가 어느 쪽을 골라도 평균 50개가 남는다.

#### 질문 B

```text
1개 / 99개
```

운 좋게 1개 쪽을 고르면 바로 끝나지만, 99% 확률로 99개가 남는다. 평균적으로는 거의 줄어들지 않는다.

즉 Question Engine은 한 명에게 운 좋게 맞는 질문이 아니라, **평균적으로 후보를 가장 많이 줄이는 질문**을 고른다. 이것이 information gain의 핵심이다.

---

## 9. 질문을 언제 멈출지도 중요한 설계

문서에는 질문 종료 조건이 잘 들어가 있다.

예를 들어 질문 수가 3개 이상이면 중단, 후보가 3개 이하이면 중단, top1과 top2 점수 차이가 충분히 크면 중단, 더 물을 수 있는 고신뢰 속성이 없으면 중단, 사용자가 noop 성격의 답변을 선택했고 다음 질문의 정보이득이 낮으면 중단한다.

또 다른 문서에는 조건별 임계값도 있다. 질문 count가 3 이상이면 중단, broad prompt 첫 질문은 물음, 후보가 8개 초과이면 gain threshold 기준으로 질문, 직전 답이 “상관없어요”면 threshold를 0.1에서 0.18로 올려 질문을 덜 하도록 한다.

### 좋은 점

이 구조는 단순히 질문을 많이 하는 챗봇이 아니라, **질문의 비용**을 고려한다.

### 개선 제안

문서에 다음 원칙을 명시하면 좋다.

```text
Question Engine은 질문을 많이 하는 엔진이 아니라, 질문할 가치가 있을 때만 묻는 엔진이다.
후보가 충분히 좁혀졌거나, top 후보가 명확하거나, 사용자가 세부 취향 선택을 원하지 않는 신호를 보이면 질문을 중단한다.
```

---

## 10. 옵션 생성과 “N개 남아요” 퍼널은 강점

문서에는 `build_question`이 후보 분포에서 가장 많은 값 상위 3개를 선택지로 만들고, 각 선택지를 고르면 후보가 몇 개 남는지 `expectedCandidateCount`를 계산한다고 되어 있다. 선택지가 2개 미만이면 질문을 포기하고, 마지막에 “상관 없어요” noop 옵션도 추가한다.

### 좋은 점

사용자는 단순히 질문에 답하는 것이 아니라, “이 답을 고르면 후보가 얼마나 줄어드는지”를 볼 수 있다. 이건 검색 UX에서 매우 좋은 장치다.

### 개선 제안

앱 UI에서 다음처럼 보여주면 좋다.

```text
촉촉한 광택감 — 후보 12개 남아요
보송한 마무리 — 후보 8개 남아요
상관 없어요 — 현재 조건 유지
```

이렇게 하면 사용자가 검색 과정에 참여하고 있다는 느낌을 받는다.

---

## 11. LLM 역할은 더 명확히 계약화 필요

현재 문서상 Claude/LLM은 후보 생성, 질문 여부 결정, filterDelta 결정에 개입하면 안 된다. Claude는 intent 보조, 질문 카피 rewrite, 결과 카피 생성 정도만 한다. 실패해도 서버 기본 루프는 계속 동작한다.

### 좋은 점

서버 결정론을 유지한다.
LLM이 실패해도 검색 루프가 망가지지 않는다.

### 부족한 점

`parse_intent_optional`이 구체적으로 어떤 JSON을 반환해야 하는지, Rule Parser와 충돌할 때 누가 이기는지, confidence는 어떻게 줄지 명확하지 않다.

### 개선 제안

LLM은 “빈칸 보조”로 제한하는 것이 좋다.

```text
1. Rule Parser가 lockedFilters를 먼저 만든다.
2. LLM은 optionalHints만 반환한다.
3. 서버가 enum/allowlist/confidence를 검증한다.
4. lockedFilters와 충돌하면 lockedFilters가 항상 우선한다.
5. LLM hint는 softFilter 또는 semantic hint로만 반영한다.
```

문서에 넣을 문장:

```text
LLM optional intent helper는 ProductSearchState를 직접 확정하지 않는다.
allowlisted optionalHints만 반환하며, 서버는 이를 검증해 softFilters로 병합한다.
prompt/rule에서 나온 lockedFilters와 충돌하면 항상 lockedFilters를 우선한다.
```

---

## 12. MMR 관련 핵심 설명 보강

MMR은 `λ × relevance - (1-λ) × similarity`로 “관련성 보너스 - 중복 패널티”를 계산하는 방식이다. 핵심은 다양성이 절대값이 아니라 **이미 선택된 결과와의 상대적 유사도**라는 점이다.

### 보완하면 좋은 설명

MMR은 조건을 무시하는 알고리즘이 아니다. 반드시 Hard Filter와 Rule/Semantic Score를 통과한 후보 안에서만 중복을 줄이는 후처리다.

```text
Hard Filter
→ Candidate Scoring
→ MMR
```

이 순서가 문서에 더 명확히 들어가야 한다.

### 중요한 안전 원칙

```text
사용자가 명시한 가격, 카테고리, 구매 채널 같은 lockedFilters는 MMR이 절대 깨면 안 된다.
MMR은 조건을 완화하는 장치가 아니라, 조건을 만족한 후보들 사이에서 중복 추천을 줄이는 장치다.
```

---

## 13. Jaccard Similarity와 유사도 계산 보완

MMR에서 다양성은 실제로 “이미 뽑힌 상품과 얼마나 비슷한가”를 similarity로 계산한다. 현재 설명상 속성 집합의 교집합/합집합 비율, 즉 Jaccard similarity를 사용한다.

```text
Jaccard similarity = 겹치는 속성 수 / 전체 속성 수
```

### 개선 제안

유사도 계산에 들어가는 attribute signature를 명확히 문서화하면 좋다.

예:

```text
brand
category
colorFamily
finish
texture
tone
priceTier
channel
```

또 같은 브랜드면 유사도에 추가 점수를 주는 방식은 좋지만, 왜 그런지 설명이 필요하다.

```text
같은 브랜드 + 같은 색상군 + 같은 finish는 사용자가 중복 추천처럼 느낄 가능성이 높으므로 similarity를 높게 본다.
```

---

## 14. Current MVP vs 최종 목표 구분 필요

문서 여러 곳에서 MVP, 현재 구현, 최종 목표가 섞여 보인다.

현재 구현은 Naver API 중심이고, 상품명/title에서 규칙 기반으로 속성을 추론한다. Naver API는 title, link, image, lprice, mallName, productId, brand, maker, category 정도만 제공하며, 화장품 전용 metadata는 제공하지 않는다.

최종 목표는 Curated Catalog를 source of truth로 두고, Naver API는 live offer check, 가격, 구매 링크 확인용으로 쓰는 방향이다.

### 개선 제안

문서에 다음 구분을 명확히 넣으면 좋다.

```text
현재 구현:
Naver API → title/category 기반 spec 추론 → rule/semantic score

MVP 목표:
작은 curated catalog + Naver live offer check

최종 목표:
enriched Product Catalog를 source of truth로 사용하고,
Naver/commerce API는 가격·재고·구매 링크 확인용으로 제한
```

---

## 15. Live Offer Check와 Catalog 역할 구분

RAG 쇼핑 플랜에는 Live offer check가 별도 단계로 있다. Naver Shopping API 또는 허가된 shopping API로 가격/링크를 확인하고, 이후 card build에서 추천 이유, tradeoff, refinement button을 포함한다.

### 전달할 인사이트

Catalog는 “이 상품이 어떤 상품인가”를 담당하고, Live Offer는 “지금 살 수 있는가”를 담당해야 한다.

```text
Catalog = 상품 지식의 원본
Live Offer = 가격/재고/링크 확인
```

이 둘이 섞이면 추천 품질과 법적/운영 안정성이 모두 흔들릴 수 있다.

---

## 16. Interaction Logging은 LTR의 기반

문서에는 ProductInteractionEvent가 정의되어 있다. view, open_detail, open_purchase_link, save, hide, compare, share 같은 행동을 기록하고, searchStateSnapshot과 scoreSnapshot도 남긴다.

SearchRefinementEvent도 정의되어 있다. cheaper, less_dark, more_glossy, more_matte, different_brand, exclude_brand, store_only, color_family_change 같은 refinement를 기록한다.

### 중요한 인사이트

이 로그 구조는 나중에 Learning to Rank를 붙이기 위한 기반이다.

현재는 사람이 정한 Rule Score를 쓰지만, 나중에는 다음 데이터를 feature/label로 쓸 수 있다.

```text
검색 query
profile
candidate features
rule score
semantic score
MMR role
view/click/save/hide/purchase
AR 적용 시간
refinement
```

### 개선 제안

LTR를 지금 구현할 필요는 없지만, 로그는 지금부터 LTR 가능하게 남기는 것이 좋다.

특히 AURA는 일반 쇼핑몰보다 강한 신호가 있다.

```text
추천 → AR 적용 → 오래 착용 → 저장 → 구매
```

이건 일반 이커머스에는 없는 매우 강한 preference signal이다.

---

## 17. RecommendationCard의 evidence 필드 활용 필요

RecommendationCard에는 evidence가 있다.

```ts
evidence: {
  catalogFields: string[];
  liveOfferFetchedAt?: string;
  knowledgeDocIds?: string[];
}
```

### 좋은 점

추천 근거를 구조적으로 남길 수 있다.

### 개선 제안

사용자에게는 숫자 점수를 그대로 보여주지 말고, evidence를 자연어로 번역해야 한다.

예:

```text
색감이 비슷해요
예산 안에 들어요
촉촉한 질감 조건과 맞아요
구매 링크가 확인됐어요
```

문서에서도 점수 숫자를 그대로 보여주지 않고 “색감이 비슷함”, “예산 안”, “촉촉한 질감”처럼 번역해 보여준다고 되어 있다.

---

## 18. 개인정보/이미지 입력 경계는 잘 설계됨

문서에는 사진을 MCP가 아니라 Media API 책임으로 두고, 추천 엔진에는 원본 사진 대신 mediaAnalysisSummary를 전달한다고 되어 있다. 또한 원본 사진, raw frame, ARFace mesh 전체, 세밀한 landmark 전체는 별도 동의와 보관 정책 전까지 추천 입력에서 제외한다고 되어 있다.

### 좋은 점

검색 추천 시스템이 얼굴 원본 데이터를 직접 다루지 않도록 경계가 잘 잡혀 있다.

### 개선 제안

이 원칙은 계속 유지해야 한다.

```text
추천 엔진에는 원본 얼굴 이미지가 아니라 summary만 들어간다.
외부 MCP에는 사용자 사진, 얼굴 원본, 민감한 피부 정보를 직접 전달하지 않는다.
```

---

## 19. API 설계는 좋지만 현재 구현과 차이 명시 필요

RAG 쇼핑 플랜에는 신규 MVP API가 잘 정의되어 있다.

```text
POST /api/recommendation-sessions
POST /api/recommendation-sessions/{sessionId}/messages
POST /api/recommendations/products
GET /api/recommendations/{recommendationId}
POST /api/recommendations/{recommendationId}/interactions
POST /api/recommendation-sessions/{sessionId}/refinements
```

반면 현재 구현 문서에는 `/api/products/recommendations`, `/api/products/liked`, like/unlike 등이 중심이다.

### 개선 제안

문서에 다음을 명확히 표시해야 한다.

```text
현재 구현 API:
GET /api/products/recommendations 중심

목표 API:
recommendation-sessions 기반 대화형 narrowing API

마이그레이션 방향:
기존 단발 추천 API를 유지하면서,
Auradin은 별도 session API로 확장
```

---

## 20. 담당자에게 전달할 최종 제안 요약

### 반드시 보완할 문서 항목

1. Rule Score 각 가중치의 의미와 휴리스틱 설명
2. 74점 base score와 62~99 clamp의 UX적 의미
3. Product Specs가 어디서 오는지
4. Product Enrichment 파이프라인
5. `_color_match_adjustment` 내부 공식 또는 제거/일반화
6. Confidence 산출 기준
7. LLM optional parser JSON 계약
8. lockedFilters vs softFilters 충돌 규칙
9. Question Engine의 entropy/coverage/confidence/actionability 설명
10. Current MVP와 최종 Catalog 구조의 차이
11. Interaction Logging을 LTR로 확장하는 로드맵

### 가장 큰 기술적 강점

1. Rule + Semantic + MMR 조합
2. 정보이득 기반 Question Engine
3. 후보를 줄인 뒤 비싼 rerank/MMR을 적용하는 검색엔진식 구조
4. 사용자 profile을 target terms로 변환하는 도메인 룰
5. AR 행동 로그까지 LTR feature로 쓸 수 있는 장기 확장성

### 가장 큰 리스크

1. Naver API가 화장품 전용 metadata를 주지 않음
2. 상품 spec 추출 신뢰도가 낮으면 Rule Score가 흔들림
3. LLM 보조 역할이 구체 계약 없이 들어가면 결정론이 깨짐
4. Color adjustment가 중복 보정처럼 보임
5. Rule Score 가중치가 아직 검증된 값이 아님
6. 현재 Auradin mock UI와 실제 backend/search service 사이의 연결이 아직 약함

### 가장 추천하는 개선 방향

```text
1. Rule Parser + lockedFilters는 서버 결정론으로 유지
2. LLM은 optionalHints와 Product Enrichment에만 사용
3. Product Catalog를 source of truth로 만든다
4. Naver API는 live offer check로 역할을 제한한다
5. Question Engine은 entropy + coverage + confidence + UX + actionability 기반으로 유지한다
6. 모든 추천 결과에 scoreSnapshot/evidence/interaction log를 남긴다
7. 나중에 Learning to Rank가 붙을 수 있게 로그 스키마를 지금부터 설계한다
```

## 최종 한 줄

아우라딘 엔진은 “GPT가 제품을 추천하는 앱”이 아니라, **사용자 의도와 얼굴 분석 결과를 구조화하고, 후보를 검색하고, 질문으로 좁히고, rule/semantic/MMR로 랭킹하는 검색 추천 시스템**이다. 현재 구조는 매우 좋지만, 상품 metadata 생성, confidence 정의, LLM 보조 계약, final adjustment, LTR 로그 설계가 문서에 더 명확히 들어가야 한다.
