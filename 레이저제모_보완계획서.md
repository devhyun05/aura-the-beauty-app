# 레이저제모 시뮬레이션 보완 계획 제안서

작성일: 2026-07-08 KST
상태: 수염 인식/마스크 품질 보완 제안서 v0.1
대상: 레이저 제모 전후 시뮬레이션 엔진, 로컬 실험 환경, RN 연동 전 검증 단계

---

## 0. 한 줄 결론

현재 방식은 버리지 않는다.
다만 지금의 단일 `beard mask` 방식은 하관 전체를 과하게 잡거나, 모공·조명 그림자·푸른 수염자국을 섞어 판단할 가능성이 크다.

따라서 다음 방향으로 보완한다.

```text
현재 C1 결정론 방식 유지
→ beard confidence를 단일 마스크가 아니라 4채널로 분리
→ hard hair / stubble dot / blue-gray shadow / protect
→ 각 채널마다 다른 보정 적용
→ 동시에 SAM2 / CLIPSeg / 수동 라벨 기반 학습형 beard segmenter 후보를 병렬 실험
```

MVP 목표는 “두꺼운 수염을 완전히 깨끗한 피부로 재생성”이 아니라, **짧은 수염·면도 자국·푸른 그림자·까슬한 stubble이 줄어든 느낌을 자연스럽게 보여주는 것**으로 고정한다.

---

## 1. 현재 상황 요약

현재 레이저 제모 시뮬레이션은 다음 흐름을 갖는다.

```text
사진 입력
→ 얼굴 랜드마크 검출
→ 하관 ROI 추출
→ 수염/그림자 후보 마스크 생성
→ protect 영역 검사
→ 수염/그림자 완화 보정
→ geometry guard
→ mild / medium / strong 3단계 결과 출력
```

현재 방법의 핵심은 두 가지다.

```text
1. 실제 털/stubble
   주변보다 어두운 작고 가는 점·선·덩어리를 영상처리로 탐지한다.

2. 면도 자국/푸른 그림자
   본인 피부 기준색과 비교해 하관의 어둡고 푸른 색 변화 영역을 탐지한다.
```

이 방향은 타당하지만, 지금 병목은 **수염 보정 알고리즘 자체보다 수염 마스크의 정밀도**다.

---

## 2. 핵심 문제 정의

### 2.1 단일 beard mask의 한계

현재처럼 “수염이다/아니다” 한 장의 마스크로 처리하면 다음 문제가 생긴다.

```text
- 약한 그림자까지 잡으려 하면 하관 전체가 켜진다.
- 오탐을 줄이면 약한 수염자국을 놓친다.
- 짧은 수염 점과 모공이 구분되지 않는다.
- 푸른 수염자국과 조명 그림자가 섞인다.
- 입술, 콧구멍, 턱선 보호가 늦게 적용되면 침범이 생긴다.
```

즉 현재 문제는 “수염을 인식할 수 있느냐”가 아니라, 다음 네 가지를 분리하지 못하는 데 있다.

```text
1. 진짜 털
2. 짧은 까슬한 수염 점
3. 피부 밑 수염뿌리/면도 자국의 푸른 그림자
4. 조명 그림자와 자연 피부 텍스처
```

---

## 3. 현재 방법을 유지하면서 수염 인식을 개선하는 방법

## 3.1 4채널 confidence map으로 분리

단일 마스크를 폐기하고 내부 표현을 다음 4개 채널로 나눈다.

```text
hair_hard
= 검고 선형/점형인 실제 털

stubble_dot
= 짧고 까슬한 점/작은 덩어리

shadow_bluegray
= 면도 자국/푸른 기운/저주파 색 변화

protect
= 입술, 콧구멍, 치아, 턱선 외곽, 점/상처/강한 주름
```

각 채널은 다른 방식으로 보정한다.

| 채널                | 탐지 방식                               | 보정 방식                          |
| ----------------- | ----------------------------------- | ------------------------------ |
| `hair_hard`       | black-hat, Gabor, dark line/blob    | Telea inpaint, 약한 texture 복원   |
| `stubble_dot`     | LoG/DoG blob, small black-hat       | 약한 inpaint 또는 local mean blend |
| `shadow_bluegray` | local Lab/LCh 색차, low-frequency map | 색 보정, blue-gray suppression    |
| `protect`         | face landmark/semantic guard        | 절대 수정 금지                       |

중요한 점은 `shadow_bluegray`에는 강한 inpaint를 쓰면 안 된다는 것이다.
그림자는 피부 정보가 남아 있으므로 **색/명도/채도 보정**으로 처리하고, 실제 털/점에만 제한적으로 inpaint를 사용한다.

---

## 3.2 이마 기준이 아니라 하관 주변 기준 피부색 사용

기존 방식은 이마/윗볼의 깨끗한 피부를 기준으로 하관을 비교하는 경향이 있다. 이 방식은 조명이 조금만 달라도 하관 전체가 수염 그림자로 켜질 수 있다.

개선 방향:

```text
나쁜 기준:
이마/윗볼 평균 피부색 vs 하관 전체

좋은 기준:
하관 안의 비수염 후보 피부 ring/patch vs 바로 옆 수염 후보 픽셀
```

구체 구현:

```text
1. 하관 ROI를 mustache / chin / jaw / clean-cheek-reference로 분리
2. 각 local tile마다 reference skin 후보를 추출
3. Lab 색공간에서 local skin GMM 생성
4. 각 픽셀이 주변 피부 기준으로 얼마나 벗어났는지 Mahalanobis distance 계산
5. 단순 어두움이 아니라 blue-gray 방향성을 함께 평가
```

shadow confidence는 다음 요소를 곱해서 만든다.

```text
shadow_conf =
  local_skin_distance_Lab
  × beard_prior_position
  × low_frequency_mask
  × blue_gray_direction_score
  × (1 - protect_mask)
```

---

## 3.3 멀티스케일 black-hat + 방향성 texture 탐지

수염은 한 가지 크기가 아니다.

```text
면도 직후 점수염: 1~3px 어두운 점
짧은 까슬수염: 3~7px 점/짧은 선
조금 긴 수염: 7~15px 방향성 결
dense beard: 넓은 덩어리 + texture
```

따라서 black-hat도 하나의 커널로 끝내면 안 된다.

추천 feature 조합:

```text
blackhat_small   : 1~3px stubble dot
blackhat_medium  : 3~7px short hair
Gabor 0/45/90/135: 방향성 있는 털 결
LoG/DoG blob     : 점 형태 수염
low_freq_shadow  : Gaussian blur 후 Lab 색차
```

최종 hair confidence는 단순 합이 아니라 다음 조건을 함께 본다.

```text
- 하관 beard prior 안에 있는가
- 작은 어두운 점이 군집을 이루는가
- 방향성 texture가 주변보다 강한가
- protect 영역과 겹치지 않는가
- 얼굴 전체 모공 분포와 다르게 하관에 밀집하는가
```

---

## 3.4 공간 정규화: connected component / CRF / graph-cut 계열

픽셀 단위 점수만으로는 마스크가 지저분하게 켜진다. 수염은 대체로 해부학적 위치와 연결성을 가지므로 후처리에서 공간 정규화가 필요하다.

MVP에서는 복잡한 graph cut 대신 다음 단계부터 적용한다.

```text
1. confidence map 생성
2. protect mask 제거
3. connected component 분석
4. beard prior 밖 작은 component 제거
5. 입술/콧구멍에 닿는 component 제거
6. dense region은 closing 적용
7. 경계는 feather 처리
8. uncertain 영역은 soft mask로 유지
```

이렇게 하면 약한 그림자를 놓치지 않으면서도 하관 전체를 칠하는 문제를 줄일 수 있다.

---

## 3.5 CLIPSeg는 정답 마스크가 아니라 coarse prior로만 사용

CLIPSeg는 텍스트 prompt 기반으로 대략적인 영역을 잡는 데 유용할 수 있다. 하지만 수염 경계의 픽셀 단위 정밀도까지 기대하면 안 된다.

권장 사용 방식:

```text
CLIPSeg("beard", "mustache", "facial hair")
→ dense beard 외곽 prior 생성
→ C1 color/texture confidence와 곱함
→ 경계는 local color/texture/protect guard로 다시 결정
```

즉 C2는 다음처럼 정의한다.

```text
C2 = C1 deterministic confidence × CLIPSeg coarse beard prior
```

CLIPSeg가 결정권을 갖는 것이 아니라, C1이 놓치는 dense beard 외곽을 보강하는 정도로 제한한다.

---

## 4. 현재 방법과 다른, 성공 가능성이 높은 방식

## 4.1 가장 추천: 약지도 학습형 beard/shadow segmenter

현재 방법과 다른 방식 중 가장 성공 가능성이 높은 것은 **작은 전용 segmentation model을 직접 만드는 것**이다.

이유:

```text
- 일반 face parsing 데이터셋은 skin, lip, hair, eyes 등은 있지만 beard/five-o'clock shadow 클래스가 없는 경우가 많다.
- beard/no-beard classifier는 있어도 실제 수염 영역 segmentation 모델은 범용 공개 모델에서 바로 얻기 어렵다.
- 우리 문제는 “수염이 있는가”가 아니라 “어디를 얼마나 줄일 것인가”다.
```

따라서 우리 목적에 맞는 클래스를 직접 정의한다.

```text
class 0: clean skin / untouched
class 1: hard hair / stubble
class 2: blue-gray beard shadow
class 3: protect / do-not-edit
```

초기 데이터 생성 방법:

```text
1. 현재 C1으로 자동 마스크 생성
2. CLIPSeg 또는 SAM2로 coarse prior 생성
3. 두 결과를 합쳐 pseudo-label 생성
4. CVAT / Label Studio / 간단한 내부 툴에서 사람이 수정
5. 30장: 스파이크
6. 100장 이상: 실사용 가능성 검증
7. lightweight SegFormer / BiSeNet / U-Net 계열 fine-tune
```

이 방식은 단기 구현 비용은 조금 있지만, 수염/그림자/모공/조명 분리를 가장 명확히 개선할 가능성이 높다.

---

## 4.2 SAM2 / CLIPSeg + 결정론 후처리 서버형 fallback

학습까지 바로 가지 못하면, 로컬 실험 서버에서 다음 방식도 병렬 테스트한다.

```text
lower-face ROI box
+ positive points: 인중/턱/턱선 beard prior
+ negative points: 입술/콧구멍/윗볼 clean skin
→ SAM2 mask 후보
→ C1 color/texture confidence와 교차
→ protect guard
→ final mask
```

장점:

```text
- dense beard 외곽을 더 잘 잡을 가능성이 있음
- 라벨링 보조 도구로 매우 유용
- 빠르게 실험 가능
```

단점:

```text
- 자동 prompt 안정성이 낮을 수 있음
- 입술/그림자/목 그림자를 같이 잡을 위험
- 모바일 실시간 기본 경로로 쓰기에는 무거울 수 있음
```

판정:

```text
제품 기본 경로: C1/C2 결정론 유지
실험/라벨링 보조: SAM2 사용
```

---

## 4.3 Image-Based Shaving식 clean-face prior

아예 다른 접근은 “수염을 찾는다”가 아니라 “수염 없는 얼굴을 추정한다”는 방식이다.

개념:

```text
bearded face
→ skin / beard / lighting layer 분리
→ beard layer 약화 또는 제거
→ clean-shaven appearance 합성
```

장점:

```text
- dense beard에는 더 강할 수 있음
- 단순 색 보정보다 강력한 결과 가능
```

단점:

```text
- clean-shaven paired data가 필요할 수 있음
- 얼굴 정체성 변경 위험이 큼
- 생성형 보정으로 보일 수 있음
- geometry / identity guard가 훨씬 중요해짐
```

판정:

```text
이번 MVP에서는 제외.
Dense beard를 제품 범위에 반드시 넣어야 할 때 2단계 연구로 재검토.
```

---

## 5. 문제별 해결책 매트릭스

| 현재 문제                  | 원인                      | 해결책                                                       |
| ---------------------- | ----------------------- | --------------------------------------------------------- |
| 하관 전체가 수염 그림자로 켜짐      | 이마/볼 기준 피부색과 하관 조명 차이   | 하관 local reference skin 사용, low-frequency illumination 보정 |
| 약한 수염을 잡으면 모공까지 잡힘     | 수염 점과 모공 모두 작고 어두운 점    | component density, beard prior, cluster/방향성 feature 추가    |
| 푸른 자국과 조명 그림자 구분 실패    | 둘 다 L*가 낮고 넓게 퍼짐        | Lab/LCh 색 방향, 좌우 대칭성, jaw/neck shadow guard 추가            |
| 입술/콧구멍 침범              | protect mask가 늦게 적용됨    | segmentation 전 protect pre-mask 적용, dilation margin 추가    |
| dense beard 제거가 부자연스러움 | 피부 정보가 실제로 가려짐          | dense beard는 quality gate 차단 또는 후속 생성형 연구로 분리             |
| strong 단계에서 피부가 뭉개짐    | hair/shadow를 동일 보정으로 처리 | hair는 inpaint, shadow는 color correction으로 분리              |
| 어두운 피부에서 오탐 증가         | 절대 밝기 기준이 피부색 편향을 가짐    | 본인 local skin GMM, 어두운 피부 전용 테스트 subset                   |

---

## 6. 보완된 파이프라인 제안

```text
Input photo
→ Quality gate
→ FaceLandmarker
→ Lower-face ROI
→ Protect mask 생성
   - lip
   - nostril
   - teeth / inner mouth
   - jaw boundary outside
   - mole / scar 후보
→ Local skin reference sampling
→ Multi-scale hair/stubble detection
→ Blue-gray shadow confidence
→ Optional CLIPSeg/SAM2 coarse prior
→ 4-channel confidence map 생성
→ Mask-protect intersection reject
→ Channel-specific correction
   - hard hair: limited inpaint
   - stubble dot: weak inpaint / local blend
   - shadow: Lab/LCh correction
→ Multi-band feather blend
→ Geometry / identity guard
→ mild / medium / strong outputs
→ HTML grid / RN result
```

---

## 7. 구현 단계

## Stage 0. 실험 데이터와 정책 정리

목표:

```text
- 실사진 10장 스파이크 세트 구성
- 이후 30장 튜닝 세트 구성
- 사진 로컬 전용 정책 문서화
- gitignore로 samples/outputs 제외
```

추천 데이터 구성:

```text
- stubble 2장
- 짧은 수염 2장
- dense beard 2장
- 어두운 피부 + 검은 수염 2장
- 푸른 그림자 위주 2장
- 이후 안경 / 조명 / 표정 변형 추가
```

완료 기준:

```text
- samples/에 실사진 준비
- outputs/에 산출물 저장 가능
- 실사진이 git에 올라가지 않음
```

---

## Stage 1. Segmentation 스파이크

비교 후보:

| 후보 | 구성                              | 목적                            |
| -- | ------------------------------- | ----------------------------- |
| C1 | 결정론 black-hat + Gabor + Lab GMM | 기본 경로                         |
| C2 | C1 + CLIPSeg beard prior        | dense beard 외곽 보강             |
| C3 | face parsing 확인                 | beard가 skin/hair로 어떻게 섞이는지 확인 |
| C4 | SAM2 prompt 기반 mask             | 라벨링 보조/서버형 fallback 가능성 확인    |

산출물:

```text
outputs/spike_segmentation_grid.html
outputs/spike_metrics.json
각 사진별 overlay: hair_hard / stubble_dot / shadow / protect
```

판정 기준:

```text
1. 입술·콧구멍 침범률
2. dense beard 외곽 커버리지
3. 어두운 피부 케이스 열화 여부
4. 장당 처리 시간
5. 사람이 봤을 때 수염 영역으로 납득 가능한가
```

게이트 결정:

```text
- C1 단독 채택
- C1 + C2 채택
- C1 + C4 라벨링 보조 채택
- dense beard 차단 결정
- 실패 시 후속 단계 중단
```

---

## Stage 2. 계약 고정

Feature-local 계약으로 둔다.

```text
apps/mobile/src/features/beard-simulation/contracts/types.ts
tools/beard-simulation-lab/engine/contracts.py
```

핵심 타입:

```ts
type BeardSimulationStage = 'mild' | 'medium' | 'strong';

type BeardMaskChannels = {
  hairHard: string;       // 8-bit confidence map path or base64
  stubbleDot: string;
  shadowBluegray: string;
  protect: string;
};

type BeardSimulationResult = {
  status: 'ready' | 'blocked' | 'failed';
  inputImageUri: string;
  stages: Array<{
    stage: BeardSimulationStage;
    outputImageUri: string;
    maskDebugUri?: string;
    preservationScore: number;
    warnings: string[];
  }>;
  analysis: {
    beardDensity: 'low' | 'medium' | 'dense' | 'unknown';
    shadowSeverity: 'low' | 'medium' | 'high' | 'unknown';
    confidence: number;
    blockedReason?: string;
  };
  guard: {
    protectIntersectionRate: number;
    landmarkDriftScore: number;
    identityDistance?: number;
    seamScore: number;
    passed: boolean;
  };
};
```

제외:

```text
- sideburn 전용 mask
- 피부 자극 설문 필드
- 회차 숫자 직접 노출 필드
- 의학적 효과 단정 필드
```

---

## Stage 3. CLI 파이프라인 골격

디렉터리:

```text
tools/beard-simulation-lab/
  engine/
    detect_face.py
    lower_face_roi.py
    beard_segmentation.py
    beard_shadow_corrector.py
    stubble_inpaint.py
    blend.py
    geometry_guard.py
    pipeline.py
    contracts.py
  configs/
    mild.yaml
    medium.yaml
    strong.yaml
  cli.py
```

CLI 사용 예:

```bash
python cli.py samples/test01.jpg --stage medium
```

산출물:

```text
outputs/runs/<run_id>/
  input.jpg
  lower_face_roi.png
  mask_hair_hard.png
  mask_stubble_dot.png
  mask_shadow_bluegray.png
  mask_protect.png
  output_mild.jpg
  output_medium.jpg
  output_strong.jpg
  overlay_grid.jpg
  metrics.json
```

---

## Stage 4. 결정론 보정 구현

### 4.1 Shadow branch

```text
- Lab/LCh 색 보정
- blue-gray suppression
- local skin mean matching
- frequency separation으로 고주파 피부결 보존
- 강한 blur 금지
```

### 4.2 Hair branch

```text
- black-hat/Gabor 기반 hard hair mask
- OpenCV Telea inpaint 제한 적용
- mask feather 후 주변 피부와 blend
- strong에서도 protect margin 유지
```

### 4.3 Stubble branch

```text
- 작은 점/짧은 선만 대상으로 함
- 모공 오탐 방지를 위해 cluster/density 조건 사용
- mild에서는 거의 색 보정 수준
- medium/strong에서만 약한 inpaint 사용
```

### 4.4 Recompose

```text
- lower-face crop 안에서만 보정
- full-face img2img 금지
- 원본 face geometry 유지
- multi-band feather blend
```

---

## Stage 5. Guard 구현

필수 guard:

```text
1. protect intersection guard
2. landmark drift guard
3. jawline IoU guard
4. identity embedding distance guard
5. seam score guard
```

실패 시 완화 순서:

```text
1. 강도 낮추기
2. mask erode
3. protect margin 확대
4. 해당 stage만 차단
5. 전체 blocked 처리
```

pytest 합성 케이스:

```text
- 입술 인위 이동 이미지 → reject
- protect 침범 마스크 주입 → reject
- 타인 얼굴 치환 → identity reject
- 턱선 밖 목 그림자 과검출 → reject 또는 erode
```

---

## Stage 6. Batch 하네스와 튜닝

Batch runner:

```bash
python batch.py samples/ --stages mild,medium,strong
```

산출물:

```text
outputs/batch_<date>/report.html
outputs/batch_<date>/metrics.json
outputs/batch_<date>/review_sheet.csv
```

튜닝 순서:

```text
1. shadow 3단계 먼저 수렴
2. hair/stubble 강도 얹기
3. protect margin 조정
4. dark skin subset 재검증
5. dense beard 차단 기준 확정
```

휴먼 리뷰 질문:

```text
1. 본인이라 믿기는가?
2. 제모된 느낌인가, 면도 직후 느낌인가?
3. 피부가 뭉개졌는가?
```

---

## Stage 7. 로컬 dev API

FastAPI 서버:

```text
tools/beard-simulation-lab/server.py
```

API:

```http
POST /simulate
GET /runs/{run_id}/{artifact}
```

`POST /simulate` 입력:

```text
- image file
- shavingState
- desiredStage optional
- consent flag
```

응답:

```json
{
  "status": "ready",
  "runId": "20260708_001",
  "stages": [
    { "stage": "mild", "outputImageUrl": "/runs/20260708_001/output_mild.jpg" },
    { "stage": "medium", "outputImageUrl": "/runs/20260708_001/output_medium.jpg" },
    { "stage": "strong", "outputImageUrl": "/runs/20260708_001/output_strong.jpg" }
  ],
  "guard": { "passed": true }
}
```

주의:

```text
- dev 전용
- 프로덕션 배포 아님
- 실사진은 로컬 저장소에만 존재
```

---

## Stage 8. RN feature 연결

디렉터리:

```text
apps/mobile/src/features/beard-simulation/
  screens/
  components/
  services/
  contracts/
  __tests__/
```

화면:

```text
1. 입력 화면
   - 촬영/업로드
   - 설문 6문항
   - 사진 처리 동의 고지

2. 로딩 화면
   - 분석 중 카드
   - 품질 검사 진행 표시

3. 결과 화면
   - Before/After slider
   - 단계 탭: 초기 / 눈에 띄는 / 많이 줄어든 변화 느낌
   - 보존 체크리스트
   - 면책 문구 상시 노출

4. 실패 화면
   - guard reject 이유
   - 재촬영 안내
   - dense beard 차단 안내
```

컴포넌트:

```text
BeforeAfterSlider
StageTabs
AnalysisCard
PreservationChecklist
DisclaimerBlock
```

서비스:

```text
beardSimulationService.ts
- local dev API client
- mock fallback
- result mapper
```

금지:

```text
- Unity AR makeup path 재사용 금지
- 의료 효과 단정 금지
- 레이저 회차 숫자 직접 노출 금지
- 기기명/시술명 추천 금지
```

---

## Stage 9. 통합 검증

E2E 시나리오:

```text
1. 시뮬레이터 또는 실기기에서 사진 선택
2. 설문 입력
3. local FastAPI /simulate 호출
4. 결과 3단계 표시
5. Before/After slider 동작
6. 실패 케이스에서 재촬영 안내 표시
```

명령:

```bash
npm run mobile:test:beard-simulation
npm run mobile:typecheck
pytest tools/beard-simulation-lab/tests
```

완료 기준:

```text
- RN → dev API → 결과 슬라이더까지 1회전 성공
- 30장 batch report 기준 통과
- guard 합성 케이스 전부 통과
- 사진 제외 지표만 reports/에 기록
```

---

## 8. 수용 기준

게이트 ② 기준:

```text
1. 실사진 30장 기준 사전 reject 제외 guard 통과율 ≥ 90%
2. identity 임베딩 거리 임계 이하 100%
3. 블라인드 휴먼 리뷰 “본인 맞음” ≥ 95%
4. “제모/감소된 느낌” 과반
5. 어두운 피부톤 subset에서 유의한 품질 저하 없음
6. 합성 케이스 pytest 전부 통과
7. 입술/콧구멍 protect 침범률 임계 이하
8. medium 단계에서 피부 뭉개짐이 명확하지 않음
```

---

## 9. 제품/UX 표현 원칙

### 9.1 단계 라벨

앱에는 회차 숫자를 직접 노출하지 않는다.

```text
초기 변화 느낌
눈에 띄는 변화 느낌
많이 줄어든 변화 느낌
```

회차/의학 정보는 상담용 참고 문구나 접힌 안내 영역에서만 보수적으로 다룬다.

### 9.2 면책 문구

```text
이 이미지는 레이저 제모 후 모습을 보장하는 결과가 아니라,
수염 색감과 그림자가 줄어든 경우의 시각적 참고 이미지입니다.
실제 결과는 피부 상태, 모질, 시술 장비, 시술 간격, 개인차에 따라 달라질 수 있습니다.
```

### 9.3 실패 UX

```text
이번 사진에서는 수염과 그림자, 피부 음영을 안정적으로 구분하기 어려워요.
정면에서 밝고 균일한 조명으로 다시 촬영해 주세요.
```

Dense beard 차단 예:

```text
수염이 너무 두껍게 얼굴 피부를 가리고 있어 자연스러운 제모 미리보기를 만들기 어렵습니다.
짧게 정리한 뒤 촬영하면 더 정확한 결과를 볼 수 있어요.
```

---

## 10. 주요 리스크와 대응

| 리스크             | 대응                                             |
| --------------- | ---------------------------------------------- |
| segmentation 실패 | Stage 1 게이트에서 중단, 뒤 단계 진입 금지                   |
| dense beard 미해결 | quality gate 차단, diffusion은 후순위 연구             |
| 어두운 피부 편향       | local skin GMM + dedicated dark-skin subset 검증 |
| 실사진 유출          | gitignore + local-only + reports에는 지표만 저장      |
| 피부 뭉개짐          | frequency separation + preserveDetail + guard  |
| identity 변경     | ArcFace류 embedding distance guard              |
| 의료 표현 과장        | 상담 질문 생성만 제공, 결과 보장 표현 금지                      |

---

## 11. 개발자/에이전트 지시 프롬프트

```text
너는 컴퓨터비전, 얼굴 이미지 처리, React Native, FastAPI 기반 실험 파이프라인에 익숙한 시니어 엔지니어다.

현재 목표는 레이저 제모 시뮬레이션의 수염 인식 품질을 보완하는 것이다.
기존 방향은 결정론 기반 수염/그림자 탐지와 제한적 보정이며, diffusion inpainting은 이번 범위에서 제외한다.

반드시 지킬 원칙:
1. 단일 beard mask 대신 hair_hard / stubble_dot / shadow_bluegray / protect 4채널 confidence map을 사용한다.
2. 실제 털과 푸른 그림자를 같은 보정으로 처리하지 않는다.
3. shadow는 색 보정 중심, hard hair/stubble만 제한적으로 inpaint한다.
4. 입술, 콧구멍, 치아, 턱선 외곽은 protect pre-mask로 먼저 제외한다.
5. 하관 전체가 켜지는 문제를 막기 위해 local skin reference를 사용한다.
6. 실사진은 절대 git에 커밋하지 않는다.
7. dense beard가 자연스럽게 처리되지 않으면 제품 범위에서 차단한다.
8. RN 연결은 로컬 FastAPI dev server로만 시작한다.
9. Unity AR makeup path는 재사용하지 않는다.
10. 결과는 의료 효과를 보장하지 않는 시각적 참고 이미지로만 표현한다.

우선 구현 순서:
Stage 0 데이터/정책 정리
Stage 1 C1/C2/C3/C4 segmentation spike
Stage 2 계약 고정
Stage 3 CLI pipeline
Stage 4 결정론 보정
Stage 5 geometry/identity guard
Stage 6 batch report 튜닝
Stage 7 local FastAPI
Stage 8 RN feature
Stage 9 E2E 검증

먼저 Stage 1의 segmentation spike를 구현하고, 10장 실사진에 대해 overlay grid와 metrics.json을 산출하라.
성공/실패를 숨기지 말고, dense beard가 안 되면 quality gate 차단 결정을 명확히 제안하라.
```

---

## 12. 최종 판단

가장 현실적인 방향은 다음이다.

```text
단기 MVP:
C1 결정론 개선판 + C2 coarse prior 옵션

성공 가능성 높은 병렬 연구:
SAM2/CLIPSeg로 pseudo-label 생성
→ 사람 수정
→ 작은 beard/shadow segmentation model fine-tune

제품 범위:
dense beard는 일단 guard로 차단
“짧게 면도한 뒤 남은 수염자국/까슬한 수염 감소 시뮬레이션”을 1차 목표로 고정
```

이 방향은 현재 실험 환경을 유지하면서도, 가장 큰 병목인 **수염 인식 정밀도**를 빠르게 끌어올릴 수 있다.

---

## 13. 참고 자료

* DullRazor: 피부 이미지에서 털을 검출하고 제거하는 고전적 morphological + inpainting 접근
* CLIPSeg: 텍스트/이미지 prompt 기반 coarse segmentation 후보
* SAM2: interactive prompt 기반 segmentation 및 pseudo-label 생성 보조
* Image-Based Shaving: bearded face를 clean-shaven appearance로 변환하는 연구 방향
* CelebAMask-HQ / LaPa / BiSeNet / SegFace: face parsing 참고 자료. 단, beard 전용 class 부재와 라이선스 확인 필요
