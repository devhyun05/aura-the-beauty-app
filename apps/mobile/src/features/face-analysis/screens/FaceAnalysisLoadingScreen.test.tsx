import React from 'react';

import {FaceAnalysisLoadingScreen} from './FaceAnalysisLoadingScreen';

const onComplete = () => undefined;
const onBack = () => undefined;

<FaceAnalysisLoadingScreen onBack={onBack} onComplete={onComplete} />;
