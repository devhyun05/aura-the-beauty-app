# 아우라딘 제품 추천 시스템 고도화 종합 검토 보고서

- 작성: 2026-07-14, Claude (Fable 5) + Codex(gpt-5.6-sol, xhigh) 교차검증
- 검증 방법: 실코드 정독(4,200줄 auradin_agent 전체 + 레거시 1,558줄), 서빙 카탈로그 정량 감사(618개), 오프라인 실행 프로브 5종, 백엔드 테스트 54건 실행, 외부 리서치, Codex 2회 교차 리뷰
- 근거 표기: 모든 주요 주장에 `파일:줄` 또는 실행 결과를 붙임. [확실]=코드/실행으로 확인, [추론]=강한 근거 기반 추정, [가설]=검증 필요
- **문서 지도** (역할별 읽기 안내): 의사결정 메모 = §0·§13·§14·§18 / 검증 레지스터 = §3(첨부 판정)·§5(F1~F18) / 설계 상세 = §6~§10 / 리서치 = §12 / 리뷰 기록 = §16 / **재현 계약 = 부록 B**. 구현 착수 시 R2·manifest·이벤트 스키마는 각각 별도 RFC로 분리 권장(§13·§6.2·§7.2가 초안).

---

## 0. 요약 (TL;DR)

1. **추천 시스템이 2개 공존한다.** 신규 Auradin(`/search/sessions`, 카탈로그 618개 기반)과 레거시(`/products/recommendations`, Naver 라이브 + 74점 가산식). **얼굴분석 완료 화면은 여전히 레거시로 라우팅된다** (바인딩 정본: [recommendationRoutes.tsx:21-33](apps/mobile/src/app/navigation/routes/recommendationRoutes.tsx#L21-L33) — `ProductRecommendationRouteScreen`이 `reportId ?? selectedFaceAnalysisReport?.id`로 레거시 화면 연결; 라우트명 매핑은 [routeTypes.ts:16](apps/mobile/src/app/navigation/routeTypes.ts#L16)). 첨부 검토 파일은 레거시+구문서를 분석한 것 — 신규 서빙 경로와 무관한 주장 다수.
2. **가장 치명적인 문제는 알고리즘이 아니라 데이터다.** 화장품 추천의 핵심축인 색상(colorFamily)이 카탈로그의 17.5%에만 존재하고(신뢰 가능 10.4%), undertone 15.4%(eligible 1.3%), 공식몰/올영 상세 근거 0건. 시맨틱 인덱스는 hash-fallback(실질 어휘 매칭)이다.
3. **점수 체계의 실질 랭킹 신호는 명목 가중치의 절반이다.** rule(0.40)·liveOffer(0.10) 축은 랭킹셋 내 상수임을 실측 확인 — 순위는 preference 0.22 + evidence 0.20 + hash-semantic 0.08이 결정한다.
4. **보고서(퍼스널컬러) 넛지가 "참고"가 아니라 지배 신호가 된다.** weight 0.5 소프트 선호가 정규화 과정에서 소거되어, title 추론(저신뢰) undertone 항목이 +0.21 점프해 TOP2를 독점하는 것을 실측 확인.
5. **Codex 교차검증(gpt-5.6-sol xhigh, 2회)이 12건을 추가 발견 — 핵심 6건은 실행 재현, 나머지는 코드 확인으로 검증**(각 발견에 증거 등급 태그): 대표적으로 ① 브랜드명 "투쿨포스쿨"의 "쿨"이 undertone=cool로 오추론(cool 59개 중 42개가 이 브랜드 — 리포트 넛지와 결합 시 특정 브랜드 밀어주기), ② "2만원 **이상**"을 `≤2만`으로 반전하는 의도 파서, ③ results 후 답변 재전송이 중복 반영되는 세션 비멱등, ④ 기획/리필 플래그 제품 126/618이 큐레이션에서 미필터.
6. 최우선 실행 = **4 마일스톤(§14)**: M1 정확성 핫픽스(파서 방향·부정, undertone 오염 정화, 모순 가드, 세션 멱등화, **R2a cap-only**, Data Manifest 최소 실행체) ∥ M2 데이터 승격(Release Manifest→오퍼 갱신→플래그 정책) ∥ M3 관측성(이벤트 수집 — 조기 병렬 개시) → M4 점수 후속 개편(R2a 캘리브레이션·profile 결합). 그 뒤 R1(표면 전환, 패리티 게이트 통과 후)·실임베딩·커버리지 확장.

---

## 1. 현재 구현 상태 (실코드 기준 지도)

### 1.1 두 개의 추천 시스템

| | 신규 Auradin | 레거시 |
|---|---|---|
| API | `POST/GET /search/sessions*` ([search_sessions.py](services/backend/app/api/search_sessions.py)) | `GET /products/recommendations` ([products.py:140](services/backend/app/api/products.py#L140)) |
| 데이터 | 큐레이션 카탈로그 618개 (`catalog_items_mvp_20260708.jsonl`) + 발견 슬롯만 라이브 Naver | Naver Shopping API 라이브 검색 (+DB 폴백) |
| 점수 | 가중합 0~1: rule 0.40 / semantic 0.08 / preference 0.22 / evidence 0.20 / liveOffer 0.10 ([ranking.py:61](services/backend/app/services/auradin_agent/ranking.py#L61)) | 74점 베이스 가산식(색 +12·cap36, finish +4, skinType +4, feature +3, tone +7, 웜쿨충돌 -8, 62~99 클램프) ([shopping_products.py:690-739](services/backend/app/services/shopping_products.py#L690-L739)) + Titan 임베딩 65:35 결합 + `_color_match_adjustment` 재가산 |
| 파이프라인 | intent 파싱 → 하드필터 사전 적용 → 랭킹 → floor 게이트 → MMR(자카드+동일브랜드 0.34) → 3역할(anchor/diverse/discovery) → 구조화 근거 → 비동기 enrich(라이브 발견 + LLM 카피) | 카테고리 질의 생성 → Naver 검색 → 스펙 추출(term match) → 가산 점수 → 임베딩 재랭크 |
| 질문 | 정보이득 기반 되묻기(entropy×coverage×confidence×uxPriority×actionability, [question_engine.py:190](services/backend/app/services/auradin_agent/question_engine.py#L190)) | 없음 |
| 모바일 진입 | 홈 퀵액션 '제품 추천' → `AuradinSearchScreen`, 리포트 첨부 시 `context.personalColor` 릴레이 ([attachments.ts](apps/mobile/src/features/recommendation/attachments.ts)) | **얼굴분석 완료 → `ProductRecommendation` 화면** (바인딩: [recommendationRoutes.tsx:21-33](apps/mobile/src/app/navigation/routes/recommendationRoutes.tsx#L21-L33)), 홈·참고메이크업추출 화면에서도 진입 |

**함의**: "보고서 기반 추천"의 실사용 경로는 레거시다. Auradin의 §9 정직성 계약(undertone hard 금지, 근거 헤지)이 레거시 경로에는 적용되지 않는다 — 레거시는 보고서 텍스트에서 추출한 term으로 색상 ±12점을 확정적으로 가감한다.

### 1.2 사용자 행동 데이터의 현주소 [확실]

- 존재: `user_product_likes`(찜) 하나뿐 ([products.py:187-229](services/backend/app/api/products.py#L187-L229)).
- Auradin 세션 상태에 결정 로그(`state["logs"]`: 질문 gain 분석, 후보 수, 점수 스냅샷)가 남지만 **TTL 15분 세션과 함께 소멸**(postgres 저장 시에도 만료 정리 대상). 뷰/클릭/구매링크 클릭/유사탐색 이벤트 로깅은 백엔드에 전무.
- 첨부 검토 §16의 `ProductInteractionEvent`/`SearchRefinementEvent`는 **계획 문서에만 존재, 미구현**.

### 1.3 테스트 환경 [확실]

- 백엔드 auradin 테스트 스위트 실행 결과 54 passed (golden/serving/question/refine/report_profile/sessions API). 전체 292+ 테스트 인프라, 골든 스위트(`run_auradin_mvp_golden_eval.py`), 라이브 신뢰성 하네스(`scripts/auradin_live_reliability.py`, 10질의×3라운드 게이트) 존재.
- 데이터 수집 스크립트 27종 존재(공식몰 7종 수집기, Naver 수집, 정제, 병합, 벡터 인덱스 빌드) — 단발 실행형이며 스케줄러/오케스트레이션 없음.

---

## 2. 데이터 현황 정량 감사 (서빙 스냅샷 20260708, 618개) [확실 — 직접 실행]

| 항목 | 값 | 판정 |
|---|---|---|
| 총 제품 / 구매가능 | 618 / 618 (100%) | 이미지·링크·가격은 완비 (is_purchasable 게이트 덕) |
| 카테고리 | lip 117 / cheek 113 / base 103 / shadow 101 / brow 96 / liner 88 | 균형 양호 |
| 브랜드 | 17개 (화이트리스트), 상위5 점유 34.3% | 브랜드 폭이 좁음 — 편중은 없으나 다양성 상한 |
| **colorFamily** | 존재 17.5% / eligible 10.4% / conf≥0.6 12.5% | **치명적 결핍** — 화장품 핵심축 |
| finish | 38.2% / 13.4% / 18.1% (shadow 16.8%, brow 2.1%, liner 5.7%) | 립·치크만 사용 가능 수준 |
| texture | 89.3% / 55.7% | 유일하게 건강 |
| undertone | 15.4% / **1.3%** / 5.3% | 사실상 근거 없음 — §9의 hard 금지가 옳았음을 데이터가 증명 |
| intensity | 5.2% / 0% | 의도적 0 (섀도명에서 비결정적 — 메모리 계약) |
| 근거 소스 | naver_live_offer 618, title 추론 531, **공식몰/올영 상세 0** | evidence 축의 변별력이 좁음 (0.40~0.57 범위) |
| 시맨틱 인덱스 | hash-fallback (dim 1024, 2,505청크) | 코사인 max ~0.14 — 실질 어휘 중첩 매칭 |
| 중복 | 정규화 (브랜드+상품명) 중복 2그룹 4행 | 양호 (선행 dedup 파이프라인 효과) |
| 가격 | 중앙 16,500원, 1천~74,440원 | 질문 priceTier 분포 건강 |
| 최신성 | 전량 2026-07-08 수집 후 정지 | **자동 갱신 없음 — 가격·품절 부패 진행 중** |

**우선순위 판정**: 추천 품질에 결정적인 결손은 ① lip/cheek/shadow의 colorFamily(현 12~21%), ② shadow의 finish(16.8%), ③ 공식몰 상세 근거(0건 — evidence 축과 §6 확정 근거의 원료), ④ 데이터 신선도(가격/품절). undertone 확장은 소스가 막혀 있고(올영 안티봇 차단 확인됨) 대체 수단(N/C/W 호수 코드 등)의 한계까지 이미 감사됨 — 무리한 확장 금지 (AURADIN.md §2의 잠긴 결정과 일치).

---

## 3. 첨부 알고리즘 검토 파일 주장별 판정

첨부: `아우라딘_알고리즘_검토.md` (세션 인사이트 정리). 총평: **문서 기반 분석으로서 레거시 경로와 계획 문서에는 정확하나, "현재 아우라딘 엔진"이라는 프레이밍이 잘못됨** — 신규 서빙 경로(auradin_agent)는 전혀 다른 점수 체계다.

| # | 첨부 주장 | 판정 | 근거 |
|---|---|---|---|
| §1 | 전체 구조 Intent→Retrieval→Rule→Semantic→Question→MMR→카드→로깅 | **부분적으로 타당** | 신규 경로 골격과 유사하나 "Interaction Logging"은 미구현. 실제 순서도 다름(질문이 랭킹 후, semantic은 랭킹 내 축) |
| §2 | Rule Score 74점 베이스, 색+12/finish+4/skinType+4/feature+3/tone+7, 62~99 클램프 | **실제 코드와 부분 일치 (경로 오인)** | 레거시 [shopping_products.py:701-739](services/backend/app/services/shopping_products.py#L701-L739)와 정확히 일치. 신규 서빙 경로에는 존재하지 않음 |
| §2 | 가중치는 학습값 아닌 휴리스틱, 클릭/저장 로그로 조정해야 | **타당함** | 양 시스템 공통. 단 로그가 없어 현재는 실행 불가 (§16 판정 참조) |
| §3 | 상품 spec 추출이 가장 큰 빈틈, term matching 의존, Enrichment 파이프라인 약함 | **타당함 — 본 검토의 정량 감사가 수치로 확증** | colorFamily 17.5%, 공식 근거 0건 (§2 표). 단 "수집 시점 LLM enrichment" 제안은 이미 부분 구현됨(16-에이전트 속성 추출, merge_attrs) — "없다"가 아니라 "커버리지가 부족하다"가 정확 |
| §4 | `_profile_text`/`_target_terms` 설계 좋음 (건성→촉촉 확장 등) | **타당함 (레거시 한정)** | [shopping_products.py:600-633](services/backend/app/services/shopping_products.py#L600-L633). 신규 경로의 리포트 반영은 undertone 소프트 선호 1개뿐으로 훨씬 좁음 |
| §5 | Titan 임베딩, rule 65% + semantic 35% | **실제 코드와 부분 일치 (경로 오인)** | 레거시 [shopping_products.py:23,881-887](services/backend/app/services/shopping_products.py#L881-L887). 신규는 hash-fallback 인덱스 + semantic 0.08 |
| §5 | Search Profile Text로 LLM 변환 후 임베딩 제안 | **근거 부족 (현시점)** | 임베딩 백엔드 자체가 hash인 상태에서 입력 텍스트 최적화는 순서가 틀림. 실임베딩 전환 후 A/B 대상 |
| §6 | 색상 이중 가산(`COLOR_MATCH_BONUS` + `_color_match_adjustment`) 문제 | **타당함 (레거시 한정, 실재 버그성 설계)** | rule 내 가산([:733](services/backend/app/services/shopping_products.py#L733)) 후 semantic 결합 뒤 재가산([:1063-1064](services/backend/app/services/shopping_products.py#L1063-L1064)). 색상 target 존재 시 최대 ±10이 두 번 |
| §7-8 | Question Engine gain 공식(entropy×coverage×confidence×uxPriority×actionability), 균등 분할 우수성 | **타당함** | [question_engine.py:190-196](services/backend/app/services/auradin_agent/question_engine.py#L190-L196)과 일치 — 상수(0.1/0.18 threshold, 3개 제한)도 정확 (Codex 검증으로 초기 판정 완화). 실불일치는 '후보 3개 이하 종료'(코드에 없음)와 점수갭 종료의 hard-lock 전제(첨부 미언급) 둘뿐 |
| §7 | confidence 산출 기준 미정의 → 소스별 표 제안 | **부분적으로 타당** | 신규 카탈로그에는 필드별 confidence + hardFilterEligible이 이미 존재(첨부가 이를 모름). 제안 표 자체는 수집 파이프라인 SSOT로 유용 |
| §9 | 질문 종료 조건 (3개 제한, noop 시 threshold 0.1→0.18 등) | **타당함** | [question_engine.py:350-361](services/backend/app/services/auradin_agent/question_engine.py#L350-L361) 실측 일치. 추가로 점수갭 즉답 종료(gap≥0.04)도 존재 — 첨부 미언급 |
| §10 | expectedCandidateCount "N개 남아요" 퍼널 | **부분적으로 타당** | 구현됨. 단 ① soft 질문은 `_expected_count`가 전체 후보 수 반환([question_engine.py:242-245](services/backend/app/services/auradin_agent/question_engine.py#L242-L245)) — 변별력 0, ② 수치가 전체 후보가 아닌 상위 40개 창 기준(F15, Codex 발견) — "N개 남아요"의 N이 실후보와 불일치 |
| §11 | LLM 역할 계약(lockedFilters 우선, optionalHints만) 필요 | **타당하나 이미 구현이 제안보다 앞서 있음** | 신규 경로는 LLM이 카피 재작성만 담당, 충실성 게이트로 검증([enrichment.py:84-110,392-437](services/backend/app/services/auradin_agent/enrichment.py#L84-L110)). `parse_intent_optional`류 LLM 의도 보조는 아예 없음(순수 룰 파서) — 첨부가 걱정한 결정론 훼손은 현재 0 |
| §12-13 | MMR은 하드필터 통과 후보 내 후처리, 자카드+동일브랜드 가산 | **타당함** | [ranking.py:279-320](services/backend/app/services/auradin_agent/ranking.py#L279-L320) 일치 (브랜드 가산 0.34). 단 시그니처가 colorFamily/finish/texture 3개뿐이라 속성 결손 시 브랜드 다양성으로 퇴화 — 첨부 미지적 |
| §14-15 | 현재=Naver 중심, 목표=Curated Catalog SSOT + 라이브는 offer check | **잘못된 결론 (이미 실현됨)** | 신규 경로가 정확히 이 구조로 서빙 중(카탈로그 SSOT + 발견 슬롯만 라이브). 첨부는 이를 미래 과제로 기술 |
| §16 | Interaction 로깅 스키마 존재, LTR 기반 | **실제 코드와 불일치** | 미구현. 존재하는 것은 찜 테이블과 휘발성 세션 로그뿐 (§1.2) |
| §17 | evidence 필드를 자연어로 번역해 노출 | **타당함 (이미 구현)** | 구조화 근거 matchedOn/inferred/caveat + Bedrock reasonCopy + 충실성 게이트가 그 구현체 |
| §18 | 얼굴 원본 데이터 경계 | **타당함** | 신규 경로 입력은 personalColor 문자열 릴레이뿐 — 원본 사진·landmark 미전달 확인 |
| §19 | 현재 API vs 목표 세션 API 구분 필요 | **잘못된 결론 (낡음)** | `/search/sessions` + refine까지 이미 서빙. 다만 "레거시 API 유지+별도 확장" 방향성 자체는 현실과 일치 — 문제는 두 표면의 정책 불일치(본 보고서 §6.1) |
| §20 리스크 "mock UI와 backend 연결 약함" | **잘못된 결론 (낡음)** | 모바일 AuradinSearchScreen이 실API로 배선·테스트됨 |

**요약**: 타당 7, 부분 타당 4, 경로 오인(부분 일치) 2, 불일치·낡음 4, 근거 부족 1. 첨부의 최대 공헌은 §3(스펙 추출이 병목)이고, 최대 결함은 ① 신규/레거시 경로 혼동, ② 미구현(로깅)을 구현으로 기술, ③ 이미 구현된 것(카탈로그 SSOT, LLM 계약, 구조화 근거)을 미래 제안으로 기술한 것. **첨부가 놓친 실질 결함**은 본 보고서 §5(신규 발견)에 정리 — preference 정규화 지배, rule 축 상수화, floor semantic 사망, 다양성 슬롯의 선호 모순, 타이브레이크 편향.

---

## 4. 실코드와 기존 문서의 차이 (요지)

1. AURADIN.md(북극성)는 현행 구현과 대체로 일치 — 예외: §10의 "서빙=20260706/337개(3종)"은 낡음(현행 20260708/618개/6종). 문서 자체가 "숫자는 빌드타임 산출물 참조"를 원칙으로 세워 피해 최소화.
2. 3개 구계획 문서(AKINATOR/MVP_PREPROCESSING/SEARCH_AGENT)는 ARCHIVED 배너대로 낡음. 첨부 검토가 이 문서들을 "현재"로 읽은 것이 오인의 근원.
3. 첨부 검토 §16의 이벤트 스키마, §19의 recommendation-sessions API 명세는 RAG 쇼핑 플랜 문서에만 존재 — 코드에 없음.

---

## 5. 점수 계산 로직의 빈틈 — 신규 발견 (첨부 검토에 없던 것. F1~F6 본 검토, F9~F18 Codex 발견을 본 검토가 재검증 — 각 항목에 증거 태그)

### F1. rule·liveOffer 축이 랭킹 내 상수 [확실 — 실측]
하드필터는 랭킹 전에 전부 적용되므로([retrieval_service.py:69-75,142](services/backend/app/services/auradin_agent/retrieval_service.py#L69-L75)) 생존 후보의 `_rule_score`는 전원 1.0(무필터면 전원 0.55), `is_purchasable` 게이트로 liveOffer도 전원 1.0. 실측("립 추천해줘", 117후보): ruleScore 유니크 1, liveOfferScore 유니크 1.
- **영향**: 명목 가중치의 50%가 순위에 기여 0. 실질 신호는 preference 0.22 + evidence 0.20 + semantic 0.08 → 상대비 0.44/0.40/0.16. matchRate 표시는 "조건을 걸수록" 상승(브로드 73% vs 카테고리 잠금 84%)해 매치 품질이 아닌 필터 수를 반영.
- **보완**: (a) 표시/게이트용 점수와 순위용 점수를 분리 인지하고 가중치 재설계(§13 개선안), (b) rule 축을 "부분 일치 소프트화"(하드필터를 '필수'와 'lte 근접도' 등 연속 신호로 분해)하거나 제거하고 preference/evidence 재배분.

### F2. 단일 소프트 선호의 정규화 소거 — 리포트 넛지가 지배 신호로 [확실 — 실측]
`_preference_score`는 matched/total로 정규화 → 선호가 1개면 weight가 소거되어 1.0/0.0 바이모달 ([ranking.py:150-174](services/backend/app/services/auradin_agent/ranking.py#L150-L174)). 실측: "립 추천해줘"+봄웜 → undertone=warm 2개(모두 eligible=False·conf 0.62 title 추론)가 0.83으로 TOP2 독점, 비매칭 전원 0.62대(스윙 +0.21 = evidence 전 범위 초과). TOP3 교체 2/3.
- **영향**: §3/§9 계약("리포트는 참고, hard 금지")이 필터 단계에서만 지켜지고 점수 단계에서 붕괴. 저신뢰 title 추론 톤이 추천을 결정. 사용자 직접 입력(색 0.9, finish 0.9)과 리포트(0.5)의 위계도 소거됨.
- **보완**: 소스별 기여 상한 — 확정 설계와 수치는 §13 R2의 의사코드가 정본(최종점수 단위, cap_report 0.02 초기값). "리포트 넛지는 동점 근처에서만 순위를 바꾼다"를 골든 테스트로 강제.

### F3. floor 게이트의 semantic 팔 사망 + 질의-무관화 [확실 — 실측]
`passes_floor`의 semantic≥0.5는 hash 임베딩 max ~0.14라 발동 불가. 실측 브로드 립: floor 통과 110/117 전원 evidence≥0.45 경유(semantic 경유 0). evidence는 질의와 무관한 상수 → floor는 "관련성 게이트"가 아니라 "수집 품질 게이트"로 동작 중. `_has_hard_topic_lock` 폴백이 이를 부분 보정하나 브로드 질의에서는 공집합.
- **보완**: 실임베딩 전환 시 s_floor 재캘리브레이션 필수(0.5는 hash 기준 무의미, Titan 기준도 별도 측정 필요). 전환 전에는 floor에서 semantic 팔 제거가 정직하다.

### F4. 동점 타이브레이크 = brandName 내림차순 [확실]
`sorted(..., key=(score, brandName), reverse=True)` ([ranking.py:245](services/backend/app/services/auradin_agent/ranking.py#L245)) — 저신호 질의에서 한글 자모 늦은 브랜드가 체계적 우대. 결정론 확보 목적이면 item id 해시 등 중립 키가 옳다.

### F5. 다양성 슬롯이 명시 선호와 모순되는 제품을 노출 [확실 — 실측]
"촉촉한 립" → discovery 슬롯에 **매트 립스틱**(rate 62), "올리브영 매트 립" → diverse 벨벳·discovery **글로시**. MMR엔 소프트 선호 모순 가드가 없고, UI는 이를 "비슷한 후보"로 라벨링 ([ResultsView.tsx:231](apps/mobile/src/features/recommendation/screens/views/ResultsView.tsx#L231)) — 의미 불일치. λ 극단(0.05↔0.95) 간에도 3슬롯 중 1개만 변화(다이얼 체감 미약, step 0.15).
- **보완**: diverse/discovery 후보에 "명시 소프트 선호의 정면 모순 값 금지" 소프트 가드(모순 시 다음 후보), 또는 UI 라벨을 "다른 결 후보"로 정직화 + 모순 속성을 카드에 명시("보송 매트 — 요청과 다른 마감").

### F6. `_build_result`의 사후 드랍 [Codex 반박 수용 — 이론적 결함으로 강등]
役 배치 후 imageUrl/purchaseUrl/price 결손 제품을 드랍([session_manager.py:203-207](services/backend/app/services/auradin_agent/session_manager.py#L203-L207)). 당초 라이브 픽 발동 가능성을 우려했으나 Codex 검증: 라이브 픽은 `_build_result`를 거치지 않고 enrichment에서 별도 append되며 `build_mvp_catalog_item`이 동일 3조건을 강제 — **현행 경로에서 실발동 없음**. 하이진 수정(역할 배치 전 필터)만 백로그로.

### F9. undertone 데이터가 브랜드명으로 오염 [확실 — Codex 발견, 본 검토 재현 확정. 최상 심각도]
`title_keyword_extractor._residual_text`가 브랜드를 제거하고도 다음 줄에서 원본 title 전체를 다시 검색 텍스트에 붙여([title_keyword_extractor.py:104-147](services/backend/app/services/auradin_agent/title_keyword_extractor.py#L104-L147)) 브랜드명 substring이 속성으로 오추론된다. 재현: **undertone=cool 59개 중 42개(71%)가 투쿨포스쿨**("쿨" 매칭, matchedToken 확인), 립 cool 7개 중 6개. F2와 결합 시 "여름쿨 리포트 → 특정 브랜드 독점 부스트"라는 체계적 브랜드 편향이 된다.
- **보완**: 추출기 수정(residual 텍스트만 검색) + 기존 카탈로그에서 matchedToken이 브랜드명 부분열인 undertone 무효화 배치 + 회귀 테스트("투쿨포스쿨 제품의 undertone은 브랜드명에서 오지 않는다").

### F10. 의도 파서가 가격 방향·부정·복합 표현을 반전/무시 [확실 — Codex 발견, 재현 확정]
실측: `"2만원 이상 립"` → `priceKrw lte 20000`(방향 반전!), `"1만원대"` → `lte 10000`, `"립 말고 블러셔"` → 립 잠금, `"매트는 싫고 글로시"` → matte·glossy 둘 다 선호 등록. 원인: 가격 regex의 방향 키워드가 optional([intent_parser.py:127-141](services/backend/app/services/auradin_agent/intent_parser.py#L127-L141)) + 부정 문맥 없는 substring 수집.
- **보완**: ① lte는 방향 키워드(이하/까지/미만/안쪽) 필수, "이상/부터/넘는" → gte, "N만원대" → [N만, N+1만) 범위, 방향 없으면 soft 힌트로 강등. ② "말고/빼고/제외/싫" 직전 term은 스킵 또는 avoidValues로. 전부 순수 룰 확장 — LLM 불요, 테스트 용이.

### F11. 질문 하드 필터가 eligibility를 무시하고 unknown을 하드 컷 [확실 — Codex 발견, 재현 확정]
질문 엔진은 hard_ratio≥0.45면 hard 질문으로 승격하지만, 실제 `matches_filter`는 개별 제품의 `hardFilterEligible`을 안 본다. 재현: 립 glossy 45개 중 27개가 eligible=False인데 hard 통과. 더 깊은 문제: hard 답변이 **속성 미상 제품 72/117을 제거** — §1 "unknown ≠ negative" 원칙이 질문 경로에서 위반된다. §9("soft-only 필드를 hard filter로 쓰지 않는다") 계약 위반.
- **보완**: 질문 filterDelta 적용 시 eligible=False·값 미상 제품은 제거 대신 preference 미매칭 처리(soft 강등), 또는 hard 유지하되 후보 급감 시 unknown 복원 폴백. 어느 쪽이든 골든에 "매트 선택 후에도 미상 제품이 결과 후보에 잔존 가능"을 명시.

### F12. semantic 검색 실패 시 floor fail-open [확실 — Codex 발견, 발동 조건은 설정·아티팩트 의존 (2회 교차 보정)]
빈 semantic dict면 전 후보 semanticScore=0.5 → floor(≥0.5) 전면 개방 ([ranking.py:220](services/backend/app/services/auradin_agent/ranking.py#L220)). 발동 조건(양측 검증 종합): ① `backend="embedding"` 명시 설정의 실패 경로(embedding_failed/missing/incompatible), ② **auto에서 인덱스 파일 부재 → lexical 폴백 → 토큰 무중첩 시 `{}` 반환**(Codex 재반박 수용 — 어휘 백엔드는 빈 dict 가능). 현행 배포(인덱스 존재+hash)에서는 미발동이나, §6.2의 "카탈로그만 갱신하고 벡터를 빠뜨린 스냅샷"이 정확히 이 경로를 운영에서 재점화한다. 수정: `build_semantic_scores` 반환을 `status=ok|unavailable|no_match`로 분리해 실패와 중립을 구분.

### F13. 세션 비멱등 + 질문 단계 refine 퍼널 스킵 [확실 — Codex 발견, 재현 확정]
results 후 마지막 답변 재전송 → answers 3→4, questionCount 3→4 중복 반영(결과 시 `lastQuestion` 미소거, [session_manager.py:291-298](services/backend/app/services/auradin_agent/session_manager.py#L291-L298)). 질문 단계에서 dial refine 호출 → 질문 건너뛰고 results 전이(재현). 모바일 UI는 이 순서를 만들지 않으나(다이얼은 결과 화면에만) 네트워크 재시도·중복 탭이 자연 발생시킬 수 있음.
- **보완**: results 진입 시 lastQuestion=None, answer는 (questionId, optionId) 중복 시 no-op, refine은 phase=results에서만 허용.

### F14. qualityFlags 제품이 큐레이션에서 미필터 (라이브와 비대칭) [확실 — Codex 발견, 재현 확정]
기획/리필/미니/도구 플래그 126/618(20%)이 서빙에 포함 — 라이브 발견은 플래그 1개라도 있으면 컷하는 것과 비대칭([enrichment.py:716-717](services/backend/app/services/auradin_agent/enrichment.py#L716-L717) vs [catalog_loader.py:79](services/backend/app/services/auradin_agent/catalog_loader.py#L79)). 재현: 브로드 립 TOP10에 "기획/단품" 2개.
- **보완**: 정책 결정 필요 — 전면 컷(-126개, 카테고리별 후보 감소 감수)이냐 순위 강등+카드 표기냐. 본 검토 권고: 기획/단품(49)은 표기 유지(구매가치 있음), 리필(44)·미니(25)·도구(35)는 컷 또는 별도 옵션.

### F15. 질문 통계가 상위 40개 창 기준 + noop 답변이 시맨틱 질의 오염 [확실 — Codex 발견]
후보 117개여도 질문 분포·"N개 남아요"는 상위 40개 기준([question_engine.py:158](services/backend/app/services/auradin_agent/question_engine.py#L158)) — UI 수치가 전체 후보와 불일치. noop 답변도 `attribute=None` 형태로 시맨틱 질의 텍스트에 직렬화되어 순위에 미세 영향([retrieval_service.py:86-90](services/backend/app/services/auradin_agent/retrieval_service.py#L86-L90)). best attribute의 옵션이 2개 미만이면 차선 속성 시도 없이 질문 포기도 확인.

### F16. 반복 refine 시 rule/preference 상태와 시맨틱 질의 불일치 [확실 — Codex 발견]
refine 필터는 attribute별 교체·누적되나 시맨틱 질의에는 최신 refinePrompt 1개만 직렬화 ([retrieval_service.py:144-145](services/backend/app/services/auradin_agent/retrieval_service.py#L144-L145)) — 반복 refine 후 두 신호가 서로 다른 조건 집합을 봄.

### F17. MMR이 Top3만 필요한데 floor 통과 전체를 O(n³) 재랭킹 [확실 — Codex 발견]
`build_slice_result`가 `mmr_rerank`에 top_n 미전달([ranking.py:385](services/backend/app/services/auradin_agent/ranking.py#L385)). 618개 규모에선 체감 낮으나 카탈로그 확장 시 지연 직결 — `top_n=12` 정도로 캡 (역할 배치에 충분).

### F18. 라이브 발견 픽이 floor·MMR을 우회 [확실 — Codex 발견]
라이브 후보는 `rank_candidates`만 거치고 floor·기존 결과와의 MMR 없이 append([enrichment.py:798-839](services/backend/app/services/auradin_agent/enrichment.py#L798-L839)), semanticScore는 일괄 0.5, 질의 사다리가 카테고리-only까지 후퇴하면 soft 선호 무시 상품 유입 가능. 하드 필터는 통과시키므로 §9 위반은 아니나 §5 랭킹 계약의 부분 우회.

### F7. (레거시) 색상 이중 가산 + 프로필 없으면 순위=검색순 [확실 — 코드]
첨부 §6 지적이 실재([shopping_products.py:733](services/backend/app/services/shopping_products.py#L733), [:1063-1064](services/backend/app/services/shopping_products.py#L1063-L1064)). 또한 프로필 없으면 `max(82, 96-2·index)` — 점수가 Naver 검색 순위의 선형 변환일 뿐인데 82~96% "매치율"로 표시됨 ([:697](services/backend/app/services/shopping_products.py#L697)) — 과잉 확신 표시.

### F8. 세션 이원화(in-memory dict + postgres)의 워커 간 불일치 가능성 [추론]
`_SESSIONS` 모듈 전역 + postgres 재수화 병행 ([session_manager.py:31,745-758](services/backend/app/services/auradin_agent/session_manager.py#L745-L758)). 멀티워커에서 get은 재수화하지만 in-memory 상 오래된 상태와 최신 postgres 상태의 경합(동시 answer/refine)에 락 없음. 현재 단일 워커 배포라면 무해 — 배포 형상 확인 후 판단.

(레거시 전용 결함 — `_score_product_match`의 cap 불균형(색 36 vs finish 12), index 페널티가 프로필 있는 경우 max 6점으로 미미한 점, `_semantic_reason`이 72% 문턱으로 사후 설명을 덧붙이는 점 — 은 레거시 유지 여부 결정(§13 R1) 이후에만 의미가 있어 상세 생략.)

---

## 6. 데이터 수집·자동 업데이트 파이프라인 설계 (최우선 과제)

### 6.1 현재 자산 [확실]
수집기 27종이 이미 존재: Naver(`run_auradin_naver_collection.py`), 공식몰 7종(`collect_auradin_{official,gsshop,lotteon,hmall,ably,wconcept,chicor}_metadata.py`), 올영(차단 확인 — 브라우저 수집기 있으나 안티봇에 막힘), 정제(`refine_auradin_seed_derivation.py`), 병합(`merge_official_into_seed.py`), 벡터 인덱스(`build_auradin_vector_index.py`), 커버리지 리포트(`report_auradin_crawl_coverage.py`). **없는 것: 스케줄링, 증분 갱신, 검증 게이트의 자동 연결, 실패 알림, 버전 롤백 규약.**

### 6.2 MVP 파이프라인 (즉시 구현 — 기존 스크립트 오케스트레이션)

전체 흐름 한 장:

```text
수집(Naver 오퍼 + 공식몰 속성) → 정제/정규화 → 중복 판정(identity 사다리) → 속성 추출(룰+LLM, 필드별 conf)
→ merge → 커버리지/품질 리포트 → 청크·벡터 인덱스 재생성 → 골든 스위트 → [manifest 원자 승격] → 서빙
                                                  (게이트 실패 시 승격 거부 — 서빙은 이전 스냅샷 유지)
```

핵심 설계 원칙: **RUN_DATE 스냅샷 = 불변 버전**. 2026-07-15 M1에서 `AURADIN_RUN_DATE` 단독 선택은 폐기하고, `active_snapshot.json → immutable snapshot manifest → catalog/chunk/vector`를 단일 resolver가 검증하는 구조로 전환했다. 롤백도 환경변수 한 개 변경이 아니라 이전 manifest를 같은 activation CAS·재시작 절차로 다시 활성화하는 작업이다.

```
[주 1회 오퍼 갱신 트랙 — 가볍고 안전]
1. Naver API로 기존 618개 offer 재조회 (상품명+브랜드 질의, 기존 매칭 키 재사용)
2. diff 산출: 가격 변동 / 품절(검색 미노출) / 링크·이미지 변경
3. 검증 게이트: 가격 급변(±60%) → 사람 검토 큐, 미노출 2회 연속 → collectionStatus=stale
4. catalog_items_mvp_{NEW_RUN_DATE}.jsonl 생성 (속성 불변, liveOffer/updatedAt만 갱신)
5. 골든 스위트 + 커버리지 리포트 통과 시 RUN_DATE 승격 (게이트 실패 시 승격 거부 — 기존 서빙 버전 유지. 이는 롤백이 아님 — 롤백 정의는 아래 manifest 절)

[월 1회(초기 2주 1회) 확장 트랙 — 신규 제품 + 속성]
1. 브랜드 화이트리스트 × 카테고리 질의로 Naver 신규 후보 수집 (기존 normalize_naver_item 재사용)
2. 정규화 이름 키(normalize_product_name)로 기존/신규 판별 → 신규만 속성 추출로
3. 속성 추출: 공식 상품명 기반 룰(기존 title_keyword_extractor) + LLM 보조(6.4)
4. 필드별 confidence 기록 — **전체 confidence 폴백 금지** (시드 조작 함정 재발 방지: 감사에서 field-specific confidence만 인정하기로 확정된 계약)
5. merge → refine → 중복 제거 → 커버리지 리포트 → 사람 스팟체크 큐(신규 eligible 속성 표본 30개)
6. 벡터 인덱스 재빌드(전체 — 618~수천 규모에선 증분 불필요) → 골든 통과 시 승격
```

- **원자 승격 — 두 manifest 분리 (Codex 2차 + 후속 피드백 반영)**: 데이터와 코드는 승격 단위가 다르므로 분리한다.
  - **Data Manifest**: catalog/chunks/vector 인덱스/임베딩 모델 ID/체크섬/스키마 버전 — RUN_DATE 승격의 원자 단위. 카탈로그만 새 RUN_DATE로 만들고 벡터를 빠뜨리면 auto 백엔드가 조용히 lexical로 강등되어 추천 의미가 바뀐다(운영에서 F12를 재점화하는 설계 실수).
  - **Release Manifest**: dataManifestId + appCommitSha + 랭킹/파서/질문엔진 버전 — 배포와 데이터 승격이 따로 진행될 때 **런타임이 시작·승격 시 호환성을 검사해 불일치면 거부**한다. 이벤트 로그의 버전 귀속(§7.2)도 이 ID를 참조.
  - 승격 거부는 "failed promotion"이고, **롤백은 승격 후 문제 발견 시 이전 manifest로 되돌리는 별도 절차**로 정의한다.
- **M1 선행 구현 상태(2026-07-15)**: Data Manifest의 최소 실행체를 M2보다 먼저 착지했다. `snapshot_manifest.py`가 manifest/아티팩트 SHA, catalog↔chunk↔vector ID 집합, 중복 ID, 벡터 차원·유한값·metadata를 시작 시 fail-closed로 검증한다. `promote_auradin_snapshot.py`는 전처리 sidecar(runDate·seed SHA·경로·행수) 검증→move journal 기반 부분 이동 재개→dated immutable 파일 준비와 active pointer lock/CAS를 분리한다. 골든 러너는 보고서를 먼저 저장하고 1건이라도 FAIL이면 non-zero로 종료한다. 결함이 있던 20260715 후보는 폐기 대상으로 격리했고, 수정된 **20260716 후보**(catalog 618/chunk 2,454/vector 2,454, manifest SHA `928fdef244c66038ae3e0f284d15bbecf2133e8b5653b55ce77e5f44c6ac7823`)를 prepare·골든 6/6까지 검증했다. 다만 승인 증거 파일과 그 SHA, 실제 worker 수·파일시스템 형상·restart 방식이 없으므로 active pointer는 20260708을 유지한다. Release Manifest는 여전히 M2 후속 범위다.
- **오퍼 재매칭 identity 사다리 (Codex 2차 반영)**: ① 동일 listing ID → ② 브랜드 동일 + 핵심 상품명 + variant 시그니처(용량/수량/리필/미니) + 2위 후보와의 margin → 애매하면 기존 오퍼 유지. 정규화 이름 키는 결정적 보장이 아니라 확률적 폴백이다. 상태는 `fetch_failed / no_match / ambiguous / unavailable`을 구분 — API 장애 2회를 품절로 오판하지 않게.
- **기준 데이터(SSOT)**: 카탈로그 스냅샷. Naver는 오퍼(가격/링크/이미지/실재성) 전용, 공식몰은 속성 근거 전용 — 현행 §2 계약 유지.
- **제품 ID 안정성**: 현행 `catalogItemId`(+`sourceCandidateId`) 유지. 신규 판별은 ID가 아니라 정규화 이름 키로 (같은 상품의 리스팅 교체에 견딤 — enrichment의 `_normalized_key` 로직 재사용).
- **색상 옵션 vs 별도 제품**: shadeOptions 배열(현행 28.6%만 보유)로 통합, 옵션 단위 행 분리 금지 (20260706 dedup 교훈: 1+1/몰 변형이 41%였음).
- **실행체**: 초기엔 cron 1개(로컬/EC2) + `make auradin-refresh` 타깃이면 충분. Airflow류는 트랙이 3개 이상 되고 실패 분기가 복잡해질 때만.
- **실패 처리**: 트랙 전체가 실패해도 서빙은 이전 RUN_DATE로 계속 — 파이프라인과 서빙의 결합이 이미 느슨함(이 구조는 유지 가치가 큼). 알림은 커버리지 리포트 diff를 로그+간단 웹훅으로.
- **비용**: Naver API 무료 쿼터 내(618건 재조회 = 질의 ~650회/주). LLM 추출은 신규분에만 — 월 수백 건 × 짧은 프롬프트 = 수 달러 수준.
- **법적 경계**: 올영 안티봇 우회 금지(§9 계약 "보안 챌린지는 우회 없이 blocked"), 이미지 링크는 Naver API 제공분 사용(핫링크), 원본 재호스팅 금지.

### 6.3 지표 (파이프라인 대시보드 최소셋)
스냅샷별: 총 제품수 / 카테고리·브랜드 분포 / 필드별 eligible 커버리지 / 구매가능율 / 가격변동·품절 건수 / 골든 통과율 / 신규·퇴출 제품 수. 이미 `report_auradin_crawl_coverage.py`가 골격 — 스냅샷 간 diff 출력만 추가.
**커버리지 너머의 품질 KPI (단기 추가)**: 속성 정밀도·재현율(표본 30~50개 사람 감사 기준 — §6.4 스팟체크의 정례화), 사람 감사 정확도 추이, 브랜드-오염류 계통 오류의 재발 카운트(F9 회귀 지표). 커버리지만 올리고 정밀도가 떨어지는 퇴행을 잡는 유일한 수단.

### 6.4 속성 추출 전략 (커버리지 병목 해소)
- 1순위(즉시): **공식몰 수집 확대** — gsshop/lotteon/hmall/wconcept/ably 수집기는 있으나 현행 서빙 시드에 공식 근거가 0건. 상세 페이지의 색상명·마감 표기는 title보다 신뢰도 계층이 한 단계 위(§6 확정 근거 원료).
- 2순위(즉시~단기): **LLM 배치 추출** — 상품명+옵션명+공식 설명을 입력, `{colorFamily, finish, texture, undertone, confidence, evidence_span}` JSON 강제, **evidence_span이 입력에 실재하지 않으면 자동 리젝**(reasonCopy 충실성 게이트와 동일 원리). [Shopify(ICLR 2025 발표)](https://shopify.engineering/leveraging-multimodal-llms)·[JD Oxygen AIIC](https://arxiv.org/pdf/2606.28070)·[Rakuten BEATS](https://arxiv.org/pdf/2606.04909)의 2025-26 운영 사례가 같은 방식(수집 시점 LLM enrichment + 사람 스팟체크)을 대규모로 채택 — "표준"이 아니라 **사례 기반 방향성**으로 인용. 카테고리별 라벨 정의서를 프롬프트에 고정하고, eligible 승격은 conf≥0.70 + 스팟체크 통과 배치만.
- 3순위(단기): **이미지 기반 색 추출 실험** — 제품/스와치 이미지에서 colorFamily 추출은 멀티모달 LLM으로 가능하나, 조명·연출 변수로 정확도 검증 전 eligible 승격 금지 (실험 트랙).

---

## 7. 사용자 기록 기반 개인화 루프 설계

### 7.1 전제 [확실]
현재 신호는 찜(user_product_likes)뿐. Auradin 세션 로그는 15분 TTL로 소멸. **개인화 이전에 로깅이 선행 과제다.**

### 7.2 1단계 — 이벤트 로깅 (즉시, 개인화 없이도 가치)
```sql
-- MVP 필수 계약 반영본 (이 SQL이 구현 기준 — 아래 "스키마 범위"와 1:1 일치)
create table if not exists auradin_events (
  id bigserial primary key,
  client_event_id text not null,           -- 재시도 멱등성 — 유니크는 (owner_subject, client_event_id) 복합
                                           -- (클라이언트 생성 ID는 사용자 간 충돌 가능)
  schema_version smallint not null default 1,
  owner_subject text not null,             -- 익명 식별 계약 확정 후 사용 (dev fallback 공용 subject 금지)
  session_id text, turn_id text, result_set_id text,   -- 세션·턴·결과셋 연결 (만료 후에도 이벤트 잔존)
  event_type text not null check (event_type in (
    'session_start','question_answered','impression','product_open',
    'save','unsave','purchase_click','refine_dial','refine_prompt','hide','unhide')),
  product_id text, category text, rank int, role text, match_rate int,
  data_manifest_id text not null,           -- 귀속 정본 (Data Manifest)
  release_manifest_id text not null,        -- 귀속 정본 (Release Manifest)
  catalog_run_date text, ranker_version text,  -- 조회 편의용 중복 컬럼 (정본은 manifest ID)
  payload jsonb,                            -- scoreSnapshot(components), filterDelta, dial 등 구조화 값만.
                                            -- **raw query 원문은 저장하지 않는다** — 파싱된 filterDelta/softPreferences로 대체
                                            -- (파서 개선용 원문 수집은 별도 opt-in 트랙). 앱 버전/플랫폼/locale/동의도 payload에.
  occurred_at timestamptz not null,         -- 클라이언트 발생 시각
  received_at timestamptz not null default now(),  -- 서버 수신 시각
  experiment_id text, variant text,         -- Future Extension: nullable 예약 (B7 A/B 시작 시 사용)
  unique (owner_subject, client_event_id)
);
create index if not exists idx_auradin_events_owner_time on auradin_events (owner_subject, occurred_at desc);
create index if not exists idx_auradin_events_session on auradin_events (session_id);
create index if not exists idx_auradin_events_manifest on auradin_events (data_manifest_id);
create index if not exists idx_auradin_events_received on auradin_events (received_at);
-- 보존 계약(비파티션 테이블 기준): 보존기간(예: 180일) 경과분을 received_at 인덱스 기반
-- **주기 배치 DELETE**로 정리. 파티션 드랍 방식은 볼륨 임계 도달 시 PARTITION BY RANGE(received_at)
-- 재설계와 함께만 채택(현 규모에선 과설계). 사용자 삭제 요청 시 이벤트와 파생
-- user_taste_profile을 **한 트랜잭션으로** 삭제(원자성 — 프로필만 남는 유령 상태 금지).
```
- 수집 지점: 백엔드는 results/answer/refine 처리 시 서버사이드로 기록(이미 state["logs"]에 있는 것을 영속화), 모바일은 product_open/purchase_click/save만 fire-and-forget POST 1개(`POST /search/events` 배치).
- **스키마 범위 (MVP 필수 vs 확장 분리)**:
  - **MVP 필수** — 나중에 소급 추가가 불가능한 계약 필드만: `clientEventId`(unique — 재시도 멱등성), `occurredAt/receivedAt` 분리, `schemaVersion`, 세션 연결(`sessionId/turnId/resultSetId`), **manifest ID 귀속(정본) + run_date/랭커 버전(편의 중복)**. 이벤트 **타입 정본은 위 SQL enum의 11종** — session_start, question_answered, impression, product_open, save, unsave, purchase_click, refine_dial, refine_prompt, hide, unhide (요약 시 "탐색 5종 + 세션 2종 + 부정 신호"로 부르되 목록의 정본은 SQL). 이벤트 기록 실패는 추천 응답을 막지 않는 fail-open.
  - **Future Extension** — 실험 ID/variant만 nullable 컬럼으로 예약(B7 A/B 시작 시 사용), **그 외(앱 버전/플랫폼/locale/동의)는 payload에 저장하고 컬럼 예약은 하지 않는다**. *멱등성·시간·버전·세션 연결을 "확장"으로 미루는 것은 반대* — 이 넷이 빠지면 축적된 이벤트가 학습·A/B에 못 쓰게 되어 재로깅 비용이 발생한다.
  - 주의: 현행 인증의 dev fallback subject가 공용 식별자가 될 수 있어([security.py:168](services/backend/app/core/security.py#L168) 확인 필요) 익명 식별 계약을 먼저 정의할 것. **순서: A9(세션 멱등화·식별 계약) → A5(이벤트 수집)** — 비멱등 세션 위에 쌓은 이벤트는 학습 데이터 중복으로 오염된다.
- 첨부 검토 §16의 스키마 제안(scoreSnapshot 동봉)은 **수용** — LTR 전환 시 재로깅 불필요하게 만드는 유일한 시점이 지금이다.
- 개인정보: 이벤트에 얼굴 데이터·리포트 원문 금지. personalColor 문자열도 이벤트에는 저장하지 않고 세션 컨텍스트 참조만.

### 7.3 2단계 — 최소 개인화 루프 (ML 인프라 없음)
```
이벤트 → 야간 배치(또는 요청 시 온더플라이 조회) → user_taste_profile 갱신 → profileScore 계산 → anchor 선정에만 제한 반영
user_taste_profile: owner_subject → {
  attrAffinity: {finish: {matte: w, glossy: w}, colorFamily: {...}, texture: {...}},   -- 지수감쇠 카운트
  brandAffinity / brandAvoid, priceBand(p25~p75 of engaged), updatedAt
}
```
- 가중치: save=3, purchase_click=2, product_open=1, hide=-3 (초기값 — 실험 대상). 반감기 30일 지수감쇠(단기 세션 의도와 장기 취향 분리: 세션 내 신호는 현행 질문/refine이 이미 담당하므로 프로필은 **세션 간** 신호만).
- 주입 방식 (단일 설계 — 문서 전체에서 이것만 유효): **프로필은 softPreferences에 넣지 않는다.** 분리된 `profileScore` 성분으로 계산해 **anchor 선정 단계에만** 반영한다. (기각된 대안: softPreferences에 weight≤0.3 주입 — ① R2 합산에 개입해 명시 선호를 희석하고, ② 라이브 발견 질의 사다리([enrichment.py:625](services/backend/app/services/auradin_agent/enrichment.py#L625))에 프로필이 새어 들어가 "diverse/discovery 미적용" 원칙이 구조적으로 깨짐.) 명시 세션 선호와 같은 attribute의 프로필 신호는 억제(프롬프트>리포트>프로필 위계). 음수 선호는 음수 weight가 아니라 `avoidValues`로. 근거 칩 "내 활동 참고" 노출 + 끄기 진입점 유지.
- 콜드스타트: "이벤트 5건"이 아니라 **복수 세션·복수 일자에 걸친 actionable 이벤트 5건 이상**으로 정의(단일 세션 반복 클릭 방지 cap 포함). 미달 시 주입 0 — 신규 사용자 회귀 없음.
- 삭제권: `DELETE /search/events` (owner_subject 전체 삭제) + 프로필 리셋 버튼.
- 필터 버블 방어: profileScore는 anchor 선정에만 기여하고 diverse/discovery 슬롯은 프로필 미적용(3역할 구조가 이미 다양성 쿼터임 — 구조적 이점). 이 격리는 profileScore가 공통 랭킹 축 밖에 있기에 구조적으로 보장된다.

### 7.4 측정
개인화 유/무는 owner_subject 해시 짝홀로 A/B: save율, purchase_click율, refine 사용률(불만 대리지표), 결과 무액션 이탈률. 오프라인으로는 "과거 save 제품이 재현 질의에서 상위 랭크되는가"(leave-one-out) 정도만 — 데이터가 적을 때 오프라인 지표 과신 금지.

### 7.5 고도화(중장기, 데이터 축적 후)
이벤트 수천 세션 축적 + scoreSnapshot 로깅이 전제: pairwise LTR(LightGBM-rank, 특징 = 현행 components + 프로필 affinity), position bias는 rank 필드로 보정(IPS는 트래픽 규모 도달 전 무리). 세션 기반 시퀀스 모델은 카탈로그 618개·트래픽 소규모에서 부적합 판정(§12).

---

## 8. 질문·결과 이미지 방안

### 8.1 현재 상태 [확실]
- 질문: colorFamily 옵션은 **이미 색 스와치 타일**(클라이언트 `COLOR_FAMILY_HEX` 매핑, [auradinSearchService.ts:139-152](apps/mobile/src/features/recommendation/services/auradinSearchService.ts#L139-L152)). finish/texture/priceTier/channel은 중립 단색 타일(라벨 의존).
- 결과: 제품 imageUrl 필수(§8 계약 "없으면 표시 안 함") + palette 도트.

### 8.2 우선순위 제안
1. **[즉시] finish/texture 질문에 자체 제작 예시 이미지** — 마감감(글로시/매트/벨벳/쉬머) 4종 × 정적 asset. 제품 사진이 아니라 **추상 질감 스와치**(그라디언트/텍스처)로 제작해 특정 제품 연상·발색 오해를 차단. 저작권 無, 로딩 즉시, 일관성 최상. 질감은 텍스트만으로 가장 전달이 어려운 속성이라 의사결정 개선 효과가 가장 큼.
2. **[즉시] 질문 선택지에 대표 제품 썸네일 금지** — 옵션은 속성이지 제품이 아님. 특정 제품 사진을 붙이면 답변이 그 제품 호불호로 오염된다(질문 응답 왜곡).
3. **[단기] colorFamily 스와치를 단색 → 3색 그라데이션**(계열 내 밝기 범위)으로 — "핑크"의 폭을 보여줘 계열 선택 정확도 개선. 여전히 자체 제작.
4. **[단기] 결과 카드 palette 강화** — shadeOptions 보유 제품(28.6%)은 호수 스와치 노출. 화면 색상≠실발색 고지 문구 1줄 유지(§9 정직성).
5. **[부적합 판정] 생성형 AI 발색/시연 이미지** — 실제 제품 발색을 보장 못 해 §9(발명 금지)와 정면 충돌. 무드/분위기 참고 이미지 용도로도 MVP에선 유지비 대비 효과 불명. 사용자 얼굴 시뮬레이션은 AR 기능의 영역(추천 카드에서 혼입 금지).
6. **[중장기] 발색 예시(팔 스와치 등) 외부 이미지** — 저작권·일관성 문제로 공식 제공분만, 수집 파이프라인에 라이선스 필드 추가 후에.

---

## 9. 보고서 기반 필터 점검 결과 + 테스트 시나리오

### 9.1 점검 결과 [확실]
- **전달 경로**: 리포트 첨부 → `context.personalColor`(문자열) 클라이언트 릴레이 → `personal_color_to_soft_preferences` → undertone 소프트 선호 1개(weight 0.5, conf 0.55) → 랭킹 preference 축 + "웜톤 참고" 칩. reportId 서버 로드는 미배선(주석으로 명시된 의도적 현황, [search_sessions.py:38-40](services/backend/app/api/search_sessions.py#L38-L40)).
- **정상 동작 확인**: 봄/가을→warm, 여름/겨울→cool, 뉴트럴·불명→주입 없음(편향 방지), hard filter 경로 도달 불가(mode/locked 키 자체가 없음 — 구조적 안전). 단위 테스트 존재(test_auradin_report_profile.py).
- **문제 1 (치명)**: F2 — 넛지가 지배 신호가 됨 (실측 +0.21 스윙, TOP2 독점). **수정 전에는 "보고서 필터를 실서비스에 켜도 되는가"에 대한 답은 '조건부 아니오'** — 연결은 되나 의도(참고)대로 동작하지 않음.
- **문제 2**: 사용자가 리포트 결과에 동의하지 않을 때 세션 내 해제 수단 없음(첨부 칩 제거는 새 검색 필요 — 모바일 UX 확인 필요) + 리포트 톤과 프롬프트 톤이 충돌하면("쿨톤 립" + 웜 리포트) 둘 다 softPreferences로 들어가 **정면 상쇄가 아니라 합산 혼합**됨 — 우선순위 규칙(프롬프트>리포트) 미구현.
- **문제 3**: personalColor 외 리포트 신호(피부타입→마감 선호 등, 레거시 `_target_terms`가 하던 확장)가 신규 경로에 없음 — 의도적 축소이나, 레거시 폐기 시 이 로직의 이관 여부를 결정해야 함.

### 9.2 테스트 시나리오 (골든 스위트 추가분)
| 시나리오 | 기대 |
|---|---|
| 동일 프롬프트, 리포트 유/무 | 합격 기준은 §15 강화 3기준(A1). TOP3 교체 수·스윙 폭은 **관찰 지표**로만 기록 — 현행 2/3 교체가 수정 전 기준선 |
| "쿨톤 립" + 웜 리포트 (충돌) | 프롬프트 톤 우선, 칩에 충돌 고지 |
| personalColor="", "알 수 없음", 오타, 영문 | 주입 0, 에러 없음 |
| 뉴트럴 리포트 | 주입 0 |
| 신뢰도 낮은 값(향후 confidence 필드 추가 시) | conf<문턱 주입 0 |
| 리포트 + 조건 다수(색+가격+채널) | 후보 소멸 없음(soft라 구조적 보장) — 회귀 가드 |
| 동일 세션 질문 답변 후에도 리포트 칩 유지 | appliedFilters에 source=report 지속 |
| 전후 비교 리포트 | 순위 변경 제품의 components.answerScore diff로 원인 설명 가능해야 |

---

## 10. "비슷한 제품" 기능 점검

### 10.1 실체 [확실]
제품별 "비슷한 제품" 버튼은 없다. 존재하는 것: ① 결과 화면 "비슷한 후보" 리스트 = MMR diverse/discovery 슬롯, ② refine 다이얼(더 비슷하게/더 다양하게) = λ±0.15 재랭킹, ③ 자유 refine 프롬프트.

### 10.2 발견 문제 (실측)
- F5(§5): "비슷한 후보"에 명시 선호 모순 제품 노출(촉촉 요청에 매트). 유사도 시그니처가 colorFamily/finish/texture 3속성뿐 + 커버리지 결핍으로 실질 "브랜드 다양성"으로 퇴화 (sim(anchor,diverse)=0.0~0.33 실측).
- 다이얼 체감 미약: λ 0.05↔0.95 전 구간에서 3슬롯 중 1개만 교체. step 0.15로는 1탭당 변화가 거의 없음. 포화 고지(refineNotice)는 잘 구현됨.
- 품절/링크는 구매가능 게이트로 방어됨. 중복 리스팅은 enrichment `_normalized_key`로 방어됨(라이브 픽 한정) — 큐레이션 내 중복은 시드 dedup에 의존(현행 2그룹 잔존).

### 10.3 개선안
1. [즉시] F5 가드 + 라벨 정직화("다른 결 후보", 모순 속성 명시 칩).
2. [단기] **제품 상세에 "이 제품과 비슷한 것" 액션 추가** — 신규 검색이 아니라 현행 rankedCache 내 재랭킹으로 구현 가능: 기준 제품의 속성 시그니처를 임시 soft preference로 하는 λ=0.9 재랭킹(세션 조건 유지, 재검색 0 — refine 인프라 재사용). 사용자 의향 선택("색 유지/더 저렴/다른 브랜드")은 이 액션의 파라미터 3개로 — 첨부 검토의 단일 유사도 지적과 부합.
3. [단기] 유사도 시그니처에 priceTier·category·(있으면) shade 계열 추가 — 단, 속성 커버리지 확장(§6.4)과 병행해야 의미.
4. 탐색 반복 왜곡: dial은 λ만 조절하고 후보셋 불변이므로 반복 왜곡 구조 없음 [확실]. refine 프롬프트 반복은 같은 attribute 교체 규칙으로 오염 방지 + 실패 시 스냅샷 복원 구현 확인 ([session_manager.py:481-566](services/backend/app/services/auradin_agent/session_manager.py#L481-L566)).

---

## 11. 전체 흐름 추가 취약점 (위 절들에 없는 것)

1. **질문이 이미 진술된 속성을 되물을 수 있는 구조** — analyze_attributes가 softPreferences를 제외 사유로 안 봄. 실측에선 미발생(priceTier가 이겼음)이나 분포에 따라 가능. 저비용 가드: 프롬프트-소스 soft pref 속성을 asked 취급. [추론]
2. **soft 질문의 "N개 남아요"가 전체 후보 수 고정** ([question_engine.py:242-245](services/backend/app/services/auradin_agent/question_engine.py#L242-L245)) — 색감 질문 선택지 간 변별 정보 0. 매칭 제품 수로 교체 권장.
3. **entropy 비정규화** — 값 종류 많은 속성(colorFamily 11종)이 구조적으로 유리. 현재는 coverage가 낮아 상쇄되나 색 커버리지가 오르면 색 질문 과다 위험. log2(k) 정규화 검토.
4. **enrich 총 타임아웃 12s** — 라이브 발견+카피 직렬로 모바일 체감 지연. results 우선 서빙 + 카피 후속 패치가 이상적이나 현행 폴링 구조에선 복잡도 증가 — 보류(측정 먼저).
5. **동시성**: F8(세션 이원화). 단일 워커면 무해.
6. **관측성**: 결정 로그가 세션에 있으나 만료 소멸(§7.2가 해결). 잘못된 추천 자동 발견은 골든+라이브 하네스가 커버하나 "정합성"(추천이 조건과 맞는가)만 보고 "품질"(사용자가 좋아하는가)은 못 봄 — 이벤트 로깅이 유일한 해법.
7. **신규 브랜드/카테고리 추가 용이성**: 브랜드는 화이트리스트+파이프라인 재실행으로 낮은 비용. 카테고리는 intent_parser 용어/질문 라벨/카탈로그 수집까지 4곳 수정 — 문서화 필요.

---

## 12. 딥 리서치 결과와 적용 판정 (2026-07 기준)

| 기술 | 판정 | 근거·적용 방식 |
|---|---|---|
| 수집 시점 LLM 속성 추출(카탈로그 enrichment) | **지금 즉시 적용** | [Shopify](https://shopify.engineering/leveraging-multimodal-llms)·[JD](https://arxiv.org/pdf/2606.28070)·[Rakuten BEATS](https://arxiv.org/pdf/2606.04909) 등 대규모 운영 **사례** (표준 주장 아님). 기존 16-에이전트 추출 자산과 evidence_span 게이트로 §6.4 구현 |
| 하이브리드 검색(BM25/어휘 + 실임베딩, RRF 결합) | **간단 실험 후 적용** | WANDS 벤치 +7.4% NDCG는 [단일 벤더 기술 블로그 사례](https://www.digitalapplied.com/blog/hybrid-search-bm25-vector-reranking-reference-2026)로, 실험 조건 미공개 — **사례 기반 방향성**이며 효과는 자체 골든 스위트로 판정. 현행 hash를 Titan v2(또는 Cohere multilingual — 한국어는 Cohere 우세 가능)로 교체 + 기존 FileVectorIndex(어휘)와 RRF |
| 정보이득 기반 질문 선택 | **현행 유지 (2026년 연구 방향과 정합)** | [Entropy Guided Diversification and Preference Elicitation](https://arxiv.org/abs/2603.11399)(2026-03)은 entropy·정보이득 기반 질문의 직접 근거. [When and How to Ask](https://arxiv.org/abs/2607.06765)(2026-07)는 단계별 유도 전략, [Dialogue to Discovery](https://arxiv.org/abs/2606.24194)(2026-06)는 attribute-aware 동적 질문이 중심 — 셋이 "같은 구조"는 아니고, 공통점은 "무조건 묻지 않고 상황·후보 분포로 ask/no-ask를 정하는" 방향성. 아우라딘 설계가 이 방향과 정합하다는 판단까지만 유효 |
| 지수감쇠 선호 프로필(경량 개인화) | **준비는 즉시, 실반영은 축적 후** | §7.3. ML 인프라 불요. 프로필 계산·새도 로깅(계산만 하고 미적용)은 A5 가동 직후 시작 가능하나, 사용자 랭킹 실반영(B7)은 복수 세션·복수 일자 데이터 축적 후 |
| Learning to Rank (pairwise) | **데이터 축적 후** | scoreSnapshot 동봉 로깅(§7.2)이 선행조건. 수천 세션 전 학습은 노이즈 |
| 세션 기반 시퀀스 추천(SASRec류) | **현재 부적합** | 카탈로그 618개·비로그인 트래픽 소규모 — 표현력 대비 과적합·운영비 손해 |
| LLM 실시간 재랭킹/의도 파싱 | **현재 부적합(계약 충돌)** | §9 결정론 계약 훼손 + 지연. 결정론 파서가 커버 못 하는 표현은 로깅으로 수집해 룰 확장이 먼저 |
| MMR 대체(DPP 등) | **현재 부적합** | Top3 규모에서 MMR 대비 이득 미미, 복잡도만 증가. F5 가드가 실질 개선 |
| 멀티모달(이미지 임베딩, CLIP류) | **장기 연구 과제** | 색 추출 실험(§6.4 3순위)만 단기 편입. 전면 도입은 데이터·평가 기반 부재 |
| Counterfactual 평가/IPS | **장기 연구 과제** | 트래픽 규모 미달. 지금은 A/B(해시 짝홀)와 골든 회귀로 충분 |
| denoising implicit feedback | **단기 참고** | 2606.19658 — hide/우발클릭 구분은 §7.3 가중치 설계에 반영(클릭<저장<구매클릭 위계, 단독 클릭은 저가중) |

---

## 13. 점수 계산 개선안 (역할 분리 유지: 후보생성 → 하드필터 → 랭킹 → floor → MMR → 역할)

**설계 원칙**: 복잡도 추가 없이 F1~F6을 닫는다. 축 개수는 늘리지 않는다.

### R1. 표면 통합 결정 (선행 의사결정 — 코드보다 먼저, Codex 2차로 패리티 게이트 추가)
얼굴분석 완료 화면의 기본 랜딩을 레거시에서 **Auradin(personalColor 첨부 자동 부착)으로 전환**하는 방향. 단, 단순 교체가 아님 — 실측 패리티 갭:
- **찜 영속성**: 레거시는 서버 `user_product_likes` 연동, Auradin 보관함은 **화면 로컬 useState**(재마운트 시 소실, [AuradinSearchScreen.tsx:81](apps/mobile/src/features/recommendation/screens/AuradinSearchScreen.tsx#L81)) — 전환 시 명백한 퇴행.
- **과거 리포트 진입**: 리포트 상세 → 레거시로 `reportId` 전달 경로 존재. Auradin은 현재 선택된 분석의 personalColor 릴레이에 의존 — 과거 리포트 컨텍스트 유실 가능 (Codex 확인).
- **탐색 기능**: 복수 보고서 선택기, 룩 픽커, 카테고리 탭, 정렬, Top3 초과 브라우징 ([ProductRecommendationScreen.tsx:171-183](apps/mobile/src/features/recommendation/screens/ProductRecommendationScreen.tsx#L171-L183)). (정정: 초안의 "카테고리 세트 출력"은 부정확 — DTO에 sets가 있으나 현행 화면은 미렌더링. 실퇴행과 잠재 기능을 구분할 것.)

**권고**: feature flag 뒤에서 전환하되 **패리티 게이트** 통과를 전제로 — ① Auradin 보관함 서버 영속화(기존 like API 재사용), ② reportId/personalColor 전달 계약, ③ 과거 리포트 진입 E2E, ④ category/sort/룩 기능의 유지 또는 명시적 폐기 결정. 레거시는 게이트 통과 전까지 존치 + 핫픽스 최소셋(F7/C12 matchRate 왜곡 — 점수 숫자를 근거 칩으로 대체, 색 이중가산 제거). 롤백은 "라우팅 1줄"이 아니라 flag 전환 + 보관함 데이터 이관 고려가 필요함.

### R2a. preference 축 수정 (F2) — 2026-07-15 M1 구현 계약

**1안(W_cap 정규화)은 기각.** 초안의 `0.5 + 0.5·Σ(matched w·conf)/W_cap` (W_cap=max(Σw, 2.0))는 Codex 검증에서 결함 확인: ① 효과량 오산(리포트 매치의 중립 대비 변화는 +0.069 pref → 최종 +0.015 — 초안의 "±0.03"은 match↔avoid 전폭), ② Σw=2 전후 분모 불연속, ③ 선호 추가 시 기존 선호 희석, ④ 상품 attribute confidence 미반영(오염 데이터도 동일 강도 가점), ⑤ **골든 시뮬레이션에서 9질의 중 3건 Top3 변화 + 글리터 anchor matchRate 84→76으로 `>=80` 단언 파손**([test_auradin_glitter_slice.py:100](services/backend/tests/test_auradin_glitter_slice.py#L100)).

**채택안: 소스별 capped delta (Codex 제안 수용).** M1 구현 범위는 prompt/question/refine 묶음과 report뿐이다. profile 신호와 anchor 재배치는 런타임 입력 자체가 없어 이번 구현에서 제외했으며, profile 도입 RFC가 열릴 때 별도 결합한다.

**정책이 수치보다 먼저다**: *리포트는 공통 랭킹의 제한된 영향 신호이고 명시 사용자 입력을 이길 수 없다.* M1은 cap-only를 잠정 채택했다. 이는 영향폭 제한이지 구조적 tie-breaker 보장은 아니며, 아래 cap 수치는 초기값(가설)이다.

**단위 규약 (구현 기준)**: 아래 모든 cap·Δ는 **최종 랭킹 점수 단위**다. preference 성분(answerScore) 내부에서 구현할 때는 `w_pref(0.22)`로 환산한다(`Δ_component = Δ_final / w_pref`). ~~기존 F2 절의 "최종점수 0.03~0.05" / R2 초안의 "성분 내 0.03"~~ 단위 혼선은 이 규약으로 통일.

의사코드 (구현 계약):

```text
CAP_PROMPT  = 0.11   # 프롬프트/질문/refine 소스 합산 상한 (성분 포화 ±0.5 상당)
CAP_REPORT  = 0.02   # < 점수갭 즉답 임계 0.04, < evidence 최종 기여폭 0.034
                     # 주의: cap은 '영향폭 제한'이지 tie-breaker 정책의 구조적 보장이 아니다
                     # (base 차 <0.02인 쌍은 재배열 가능 — ε=0.02 window의 소프트 근사). 구조화는 RFC 열린 항목.
# k_s는 전부 1.0으로 시작 — 스케일 조절은 cap이 담당 (자유도 최소화)

def preference_deltas(item, prefs):            # prefs: prompt계열 + report (profile은 입력에 없음)
  resolved = resolve(prefs)                    # ① 같은 (attr,value) 중복: 상위 소스만 유지
                                               # ② 같은 attr에 상위 소스 존재 → report 신호 전부 억제
                                               # ③ 같은 값이 positive이자 avoid → 해당 신호 무효
  d = {"prompt": 0.0, "report": 0.0}
  for p in resolved:
    s = +1 if matches(item, p.values) else (-1 if matches(item, p.avoidValues) else 0)
    d[src(p)] += s * p.weight * p.requestConf * itemAttrConf(item, p.attribute)   # 무매치·속성미상 = 0 (unknown≠negative)
  Δ_prompt = clip(d["prompt"] * W_PREF, -CAP_PROMPT, +CAP_PROMPT)   # 최종점수 단위로 환산 후 clip
  Δ_report = clip(d["report"] * W_PREF, -CAP_REPORT, +CAP_REPORT)
  return Δ_prompt, Δ_report

final_score = clamp01(w_rule·rule + w_sem·sem + W_PREF·0.5 + w_ev·evidence + w_lo·liveOffer
                      + Δ_prompt + Δ_report)

def passes_floor(item):                        # boolean — profile은 이 함수의 입력 자체가 아님
  return matched_explicit_pref(item)           #   prompt/refine/question 소스 positive 매치 ≥ 1
      or semantic(item) >= s_floor
      or evidence(item) >= evidence_floor      #   ⇒ report-only 매칭으로는 절대 열리지 않음
      or (has_hard_topic_lock and matched_known_hard_constraint)
```

구현 직렬화는 `preferenceBase(0.5·W_PREF)`, `promptPreferenceDelta`, `reportPreferenceDelta`, `scoreBeforeClamp`를 분리한다. 기존 `answerScore`는 `clamp01(0.5+(Δ_prompt+Δ_report)/W_PREF)` 표시 호환 파생값일 뿐 합산 성분이 아니다. confidence는 부재/None만 0.5이고 명시 0.0은 그대로 0.0이다.

검산 예제 3종 (현행 카탈로그 값 기준):

| 예제 | 계산 | Δ (최종점수) |
|---|---|---|
| ① "립 추천해줘" + 봄웜 리포트 | report warm(w0.5·conf0.55) × item warm(itemConf0.62) = 0.171 → ×0.22 = 0.0375 → clip | **+0.02** (현행 +0.21의 1/10 — 동점 근처만 재배열, F2 해소) |
| ② "쿨톤 립" + 웜 리포트 (충돌) | undertone에 prompt 소스 존재 → report 억제(Δ_report=0). prompt cool(w0.6·conf0.7) × item cool(0.62) = 0.26 → ×0.22 | **+0.057** (cool 매치 항목만, 리포트는 무력) |
| ③ "글리터는 싫어" (avoid) | avoid glitter(w0.8·conf0.65) × item glitter(0.7) → sign −1 → −0.364 → ×0.22 | **−0.08** (진짜 감점 — 현행은 avoid 0.5배 감산이 정규화에 뭉개짐) |

- 장점: 소스 위계가 구조적으로 보장, 불연속 없음, avoid-only·중복·범위 밖 weight의 처리 규칙 명시 가능.

**R2 RFC 열린 항목 (M1을 막지 않는 후속)**:
1. **tie-breaker 구조화 방식**: cap-only(현재 구현) vs report delta를 base 차 ≤ε 후보군에만 적용 vs base 버킷화+report 2차 정렬키. 각각 경계 아티팩트·복잡도 트레이드오프를 별도 골든으로 비교한다.
2. **profile/anchor 결합 의미론**: profile 입력을 실제 도입할 때만 CAP_PROFILE, anchor 스왑, diverse/discovery 재계산, MMR 순서 보존 여부를 RFC로 확정한다. 현 M1 코드에는 profile 경로가 없다.
- **matchRate 재보정 결정 필요**: 이 변경은 기존 matchRate 분포를 바꾸므로 "골든 무회귀"가 아니라 **새 baseline 승인**(스냅샷 재생성 + 사람 스팟 승인)으로 처리한다 — 조용한 통과 금지.
- 롤백: 함수 1개 원복 + 이전 baseline 복귀.

### R3. 죽은 축 정리 (F1) — 실임베딩 전환과 짝
- 단기(전환 전): SCORE_WEIGHTS 재배분 `rule 0.40→0.15, preference 0.22→0.35, evidence 0.20→0.30, semantic 0.08→0.10, liveOffer 0.10→0.10`은 **표시 matchRate 체계를 흔들므로 골든 전면 재작성 비용** — 단독으로는 비권장. R4(실임베딩)와 묶어 1회에 처리.
- rule 축은 "부분 하드매치"가 아니라 **가격 근접도**(lte 한도 대비 여유), **채널 일치 강도** 같은 연속 신호로 재정의할 때만 존치 가치. 아니면 표시용으로 강등.
- matchRate 표시 재정의 — **1순위 권고: 숫자 자체를 폐기하고 근거 칩(matchedOn/inferred/caveat)+카피 중심으로 전환.** 현행 matchRate는 사용자가 이해하는 "잘 맞는 정도"가 아니라 랭킹 점수의 선형 변환이고(F1 — 조건을 걸수록 무조건 상승), 어떤 재정의 공식도 이 오해 소지를 완전히 없애지 못한다. 숫자 유지가 UX상 필요하면 차선: `표시 = f(answerScore, evidenceScore, 확정근거 수)`로 순위 점수와 분리. 모바일 §8 계약(matchRate 필드) 변경이 필요하므로 R1 패리티 게이트와 같은 릴리스에서 결정.

### R4. 시맨틱 실화 (F3 동반 해소)
`build_auradin_vector_index.py`로 Titan v2(1024d) 인덱스 생성 → `auradin_retrieval_backend=embedding` + 인덱스 modelId 일치 확인. s_floor는 골든 질의 분포에서 재캘리브레이션(관련/무관 질의 세트의 분리점). 한국어 품질이 미달하면 Cohere embed-multilingual 대안 실험. 비용·지연: 인덱스는 2,505청크 × 1회 배치, 서빙은 질의당 임베딩 1콜 — **지연·비용 수치는 Bedrock 왕복 실측 전 가설이며 도입 게이트에 계측 포함** (Seoul 리전은 apac 크로스리전 추론 프로파일 필요 여지도 확인). 롤백: env 스위치.

### R5. 소품 수정 묶음
- F4: 타이브레이크 `(score, stable_digest(item_id))` — **Python 내장 `hash()` 금지**(프로세스별 salt로 재시작마다 순서가 바뀜). SHA-256(item_id) 앞 8바이트 등 고정 다이제스트 사용. stable hash도 브랜드 균등을 보장하진 않으며, 목표는 "재현 가능 + brandName과 무상관"이다.
- F5: diverse/discovery 선정 시 명시 soft pref 모순 값 스킵(다음 후보) + UI 라벨 "다른 결 후보".
- F6(백로그 — A3 강등과 동일 판정): 구매가능 필터의 역할 배치 전 이동은 현행 경로 미발동이므로 재현 경로가 생길 때만.
- §11.1: 프롬프트-소스 soft pref 속성을 질문 후보에서 제외.
- §11.2: soft 질문 expectedCandidateCount = 해당 값 매칭 수.

### 가중치 산정 원칙 (요청 원칙 준수)
현 구현 수치(CAP_PROMPT 0.11, CAP_REPORT 0.02 — 모두 최종점수 단위)는 **초기값(가설)**이다. 확정은: ① 골든 baseline 재승인(점수 체계가 바뀌므로 "무회귀"가 아니라 새 스냅샷의 명시적 승인), ② 20질의 스팟 정성 평가(사람), ③ 이벤트 로깅 가동 후 save/purchase_click 기반 그리드 서치(자동)의 3단계. CAP_PROFILE은 profile RFC 전까지 현행 값이 아니다. 학습 대상(LTR 가중치)은 §7.5 전제 충족 전 금지.

---

## 14. 단계별 로드맵

**우선순위 프레이밍 (4차 정합화 — 심각도와 의존성을 분리한 4 마일스톤)**: "수일 내 A1~A10"은 전달 단위가 아니다. 각 항목은 **사용자 영향 심각도**(그 자체로 얼마나 해로운가)와 **의존성 순서**(무엇의 선행조건인가)가 다르며, 아래 마일스톤이 전달 단위다.

| 마일스톤 | 구성 | 순서 근거 |
|---|---|---|
| **M1 정확성 핫픽스** | A7+A8+A4+A2+A9, F11·F12·F15·F16·F18, **R2a**, Data Manifest 최소 실행체 | 켜진 오동작을 닫고 A8 산출물을 조용한 강등 없이 검증하는 범위 |
| **M2 데이터 승격** | Release Manifest → A6(오퍼 갱신) → A10(플래그 세분화·동기 필터) | M1에서 만든 Data Manifest/승격 도구를 운영 배포 계약으로 완성 |
| **M3 관측성** | A5 이벤트 수집(+새도 프로필) — 선행: A9·식별 계약·manifest ID | 달력 시간이 걸리는 축적 작업 — M1과 병렬 개시 |
| **M4 점수 개편** | R2a 후속 캘리브레이션·profile 결합 RFC, 이후 B2/B8 게이트 승격 | M1의 cap-only 안전 패치 뒤 온라인 근거로 수치·구조를 재결정 |

### A. 항목 상세 (성공 기준·롤백 — 마일스톤 배정은 위 표)
| 항목 | 위치 | 효과 | 성공 기준 / 롤백 |
|---|---|---|---|
| A1. R2a preference capped delta | ranking.py `preference_deltas` | 리포트 넛지 지배 제거 (F2) | 소스별 cap·report-only floor 차단·최소 효과·0.0 confidence 보존 테스트 완료. 사람 baseline 재승인은 activation 전 잔여 게이트 / 함수 원복+이전 baseline |
| A2. F4 타이브레이크 중립화 | ranking.py:245 | 브랜드 편향 제거 | 재시작·프로세스 무관 동일 순서(결정론) + brandName-순서 상관 제거 (§15-B: 영향 시나리오 expected diff 승인) / 1줄 원복 |
| ~~A3. F6 구매가능 필터 위치~~ | (백로그 강등 — Codex 2차 수용) | F6은 현행 경로 미발동(이론적) | 재현 경로 생길 때 활성화 |
| A4. F5 모순 가드+라벨 | ranking `assign_roles` + ResultsView | "촉촉→매트" 노출 제거 | 프로브 재실행 0건 / 플래그 |
| A5. 이벤트 로깅 테이블+서버 기록 | 신규 auradin_events | 모든 후속 작업의 원료 | 멱등 재시도·fail-open·지연 비영향 포함(§15) / **write feature flag 비활성화** (테이블 드랍 금지 — 수집 데이터 파괴, 스키마 결함은 migration으로 교정) |
| A6. 오퍼 갱신 트랙(주1회) 가동 | 기존 수집기 오케스트레이션 | 가격·품절 부패 중단 | 스냅샷 승격 1회 성공 / RUN_DATE 원복 |
| A7. 파서 방향·부정 수정 (F10) | intent_parser `_parse_price_lte` + soft 수집 | "2만원 이상"→gte, "말고/싫고" 반의도 제거 | §15 가격·부정 **전체 회귀 행렬**(이하/미만/이상/초과/N만원대/범위/이중부정/기존 글리터 문장) / 룰 원복 |
| A8. undertone 오염 정화 (F9) | title_keyword_extractor + 카탈로그 배치 | 브랜드명→톤 오추론 42건 + `쿨링` 오탐 1건 제거 | **브랜드명 단독·쿨링 단독 근거 cool 모두 0건 + 브랜드 밖 정당 cool positive control 3/3 유지**(과삭제 방지) / 스냅샷 원복 |
| A9. 세션 멱등화 (F13) | session_manager (lastQuestion 소거, 중복 answer no-op, refine phase 가드) | 재시도·중복탭 오염 방지 | 재전송 시 answers 불변 + stale question·재시작 후 retry(§15) / 원복 |
| A10. qualityFlags 정책 적용 (F14) | M2 catalog/chunk/vector 동기 필터 | 리필·미니·도구·케이스 제거 | 현 플래그로 base가 103→51(50.5%) 감소하고 bundle과 기획/단품이 중첩되므로 M1 적용 금지. 플래그 세분화 후 전체 아티팩트 동기 필터+카테고리 floor를 통과할 때만 승격 / manifest 롤백 |

### M1 실행 상태 (2026-07-15)

| 범위 | 상태 | 실행 증거 |
|---|---|---|
| Stage 0 A9 | 구현 완료(실 PostgreSQL fixture만 미검증) | answer/create/refine 멱등·CAS·TTL/retention·스키마 v2·모바일 request key. fake-CAS·HTTP 계약은 통과했지만 실제 PostgreSQL 3-way 경합/migration은 `UNVERIFIED_REAL_POSTGRES` |
| Stage 1 | 구현 완료 | 가격/부정/카테고리 property 10,000건, canonical residual(`부담돼`·OR 접속어·`은은한`/`진하지 않은` 잔여 0), F12 상태, F15 차순위 질문 |
| Stage 2 | 수정 후보 준비 완료, activation 보류 | **20260716** catalog 618 / chunk 2,454 / vector 2,454, A8 invalid cool 43→0(브랜드 42+쿨링 1)·positive control 3/3, 골든 6/6. active=20260708 |
| Stage 3·4 | 구현 완료, baseline 사람 승인 대기 | A2/A4/F11/F18/R2a 집중 계약 테스트와 live `A4→floor→curated-seeded MMR→brand+product dedupe→role fill/cache` 통합 경로 적용. 리포트 실측 최대 스윙 0.02·Top3 불변 |

activation 보류는 구현 미완료가 아니라 §6.2의 안전 게이트다. 실제 배포 형상과 승인자가 정해질 때 `approval evidence 파일 SHA(골든 6/6+A8 PASS+coverage 분류+approvedBy/conclusion) + approved manifest SHA + expected active SHA + worker/filesystem/restart/persistence`를 모두 제공해야 포인터 변경이 허용된다. 해당 증거가 없는 activation은 실제 실행에서 `activation preflight is incomplete`로 exit 1임을 확인했다.

### B. 단기 고도화 (1~3주)
B1. R1 표면 통합(의사결정 선행) · B2. R4 실임베딩+s_floor 재캘리브레이션 · B3. §6.4 속성 커버리지 확장(공식몰 수집 재가동 + LLM 배치 추출, lip/cheek/shadow의 colorFamily부터) · B4. 확장 트랙(월1회) + 커버리지 diff 대시보드 · B5. finish/texture 질감 스와치 asset · B6. 상세 "이 제품과 비슷한 것" 액션(§10.3-2) · B7. §7.3 최소 개인화 루프(A5 데이터 2주 축적 후) · B8. R3 가중치 재배분+matchRate 재정의(B2와 동시)

### C. 중장기 (데이터·트래픽 축적 후)
C1. pairwise LTR · C2. 세트 출력(§4 lookContext) — 레거시 세트 수요 대체 · C3. 멀티모달 색 추출 승격 · C4. 리포트 신호 확장(피부타입→마감, confidence 필드) · C5. A/B 인프라 정식화

의존성: A5→B7→C1, B3→(색 질문 활성화, F5 실효), B2→B8, R1→C2.

---

## 15. 테스트 계획 (Codex 2차 반영으로 강화)

- **골든 정책 (문서 전체 단일 규칙 — "의도된 출력 변화 범위" 기준 3분류)**: "점수식 변경/비점수 변경" 이분법은 부정확하다 — A2 타이브레이크·A4 가드·A7 파서·A10 플래그 정책은 점수식을 안 건드려도 **출력을 의도적으로 바꾸는** 변경이라 무조건 무회귀를 요구하면 버그 수정 자체가 실패 판정된다.
  - **A. 동작 보존 변경** (리팩터링, 성능, 로그 추가, 세션 저장 방식 교체): 전체 **무회귀**.
  - **B. 의도된 국소 동작 변경** (A2 타이브레이크, A4 모순 가드, A7 파서 수정, A9 세션 멱등화, A10 플래그 정책): 영향 시나리오는 **expected diff를 사람이 승인**, 비영향 시나리오는 무회귀.
  - **C. 체계 변경** (A1/R2 점수 개편, B8 가중치, B2 실임베딩·s_floor, 대규모 카탈로그 재생성): 전체 **baseline 재승인**(골든 스냅샷 재생성 + 사람 검수).
- **평가 사다리 (실험 전략)**: ① 골든 회귀(지금 — 유일한 자동 평가) → ② 오프라인 자동 지표(지금 가능: Constraint Satisfaction(결과가 하드 조건을 100% 충족하는가), Preference Satisfaction(명시 soft 선호 충족률), 다양성·브랜드 중복 — 정답 라벨 불필요) → ③ 오프라인 리플레이·Top-k Hit/Recall(행동 로그 축적 후 — 라벨이 생겨야 가능, 지금은 불가) → ④ 새도 프로필(개인화를 계산만 하고 미적용 상태로 로깅) → ⑤ 온라인 A/B(노출·버전 로깅 선행). 각 단계는 앞 단계 통과가 전제.
- **회귀 축**: 골든은 **질의 corpus 9개**(pytest 단언 32건과 구분 — Codex 지적 수용) + 신규 시나리오 — §9.2 리포트 8종, F5 모순 가드, 극한값, 세션(일부/전체 답변, noop 연속), 파이프라인(승격 게이트 실패 시 서빙 불변). 극단 케이스는 기대 상태를 명시(결과 0건 vs 완화 vs 질문 전환 중 무엇이 성공인지) — "브랜드 제외"는 파서·랭커가 브랜드 속성을 아직 미지원이므로 지원 추가와 함께만 테스트 가능.
- **성공 기준 강화 (Codex 2차 수용)**: A1은 "TOP3 교체 ≤1" 같은 통과-쉬운 기준 대신 ① 소스별 delta 상한 검증 ② **report-only 매칭의 floor 개방 금지(동작 테스트) + profileScore→floor 코드 경로 부재(구조 검증 — profile은 애초에 floor에 연결되지 않음)** ③ 최소 효과 하한(기능 무동작 감지)을 함께. A5는 "row inserted"가 아니라 재시도 멱등·삭제·순서 뒤바뀜·DB 장애 fail-open·지연 비영향. A8은 "cool 0건"이 아니라 "브랜드-단독-근거 cool 0건 + 브랜드 밖 정당 cool positive control 유지"(과삭제 방지). A10은 TOP10이 아니라 전체 서빙 후보에서 배제 flag 부재 + 카테고리 커버리지 floor. A7은 가격·부정 회귀 행렬(이하/미만/이상/초과/N만원대/1만5천/1~2만원/경계가/이중부정/"글리터 강한 아이섀도우 말고 은은한 쉬머" 기존 문장 보존).
- **점수 breakdown 검증**: components 합산=score 재검 + 추천 이유(matchedOn)가 기여 속성과 일치 자동 대조. 단 `components`는 API 응답에 미노출([session_manager.py:213-218](services/backend/app/services/auradin_agent/session_manager.py#L213-L218)) — **내부(파이썬 레벨) 테스트로 명시**하거나 diagnostics 계약 확장이 선행.
- **다양성/편향 지표**: TOP3 브랜드 중복률, 카테고리별 브랜드 노출 지니계수(주간 배치, auradin_events 기반), 결과 중복률(정규화 키), 품절·깨진 링크 비율(주간 오퍼 트랙 부산물).
- **온라인**: save율·purchase_click율·refine율·무액션 이탈률 — 개인화·가중치 변경의 최종 판정 지표. 오프라인 지표 개선만으로 UX 개선 단정 금지.

---

## 16. Codex 교차 리뷰 결과와 재검증 판정

### 16.1 리뷰 1 (구현 이해 + 첨부 반박 + 프로브 공격) — 완료

Codex(gpt-5.6-sol xhigh, read-only, 저장소 직접 분석)의 판정과 본 검토의 재검증:

| Codex 의견 | 본 검토 판정 | 근거 |
|---|---|---|
| A1 동의 + "기여 0이 아니라 필터 후 순서 기여 0" 정정 | **수용** | 정확한 표현. malformed op 경로는 API 미생성 확인 — 반례 아님 |
| A2 동의 + "±0.22가 아니라 중립 대비 ±0.11, 페어 차 0.22" 정정 | **수용** | 관측치(0.83 vs 0.62)는 페어 차 — 표현 교정. 유해성 판단("undertone 미상 108개가 명시 cool 7개와 동급 페널티") 보강도 수용 |
| A3 "현 스냅샷 동의, 일반 명제 반박" + fail-open 발견 | **수용(보정 포함)** | fail-open은 실재하나 backend=embedding 설정에서만 발동 — 본 검토 재현으로 조건 축소 (F12) |
| A5 반박 (라이브 픽은 `_build_result` 미경유) | **수용 — F6 강등** | knowledge_chunk_builder의 동일 3조건 확인 |
| C1 브랜드명 undertone 오염 | **수용 (재현: cool 59중 42가 투쿨포스쿨)** | 최상 심각도로 채택 (F9) |
| C2 질문 hard filter의 eligibility 무시 | **수용 + 본 검토가 심화** | eligibility 문제 위에 "unknown 하드 컷(72/117 제거)"이 더 큰 계약 위반 (F11) |
| C3 파서 방향 반전·부정 무시 | **수용 (4케이스 전부 재현)** | F10, 즉시 수정 대상 |
| C4 semantic 실패 fail-open | **일부 수용** | 코드 실재, 발동 조건 제한적(재현으로 확인) — Codex가 일반화 과장 (F12) |
| C5 세션 비멱등·퍼널 스킵 | **수용 (재현: answers 3→4)** | F13. 단 모바일 UI 정상 경로에서는 미발생 — 심각도 '중' |
| C6 postgres/메모리 이원화·CAS 부재 | **수용 (보류 조건부)** | 코드 사실. 실위험은 배포 워커 수에 의존 — 배포 형상 확인 전 '중' (F8 갱신) |
| C7 MMR O(n³) | **수용** | F17. 현 규모 체감 낮음 — 우선순위 '하', 1줄 수정 |
| C8 라이브 픽 floor/MMR 우회 | **수용** | F18 |
| C9 상위 40개 창 + noop 시맨틱 오염 | **수용** | F15 |
| C10 반복 refine 시맨틱 불일치 | **수용** | F16 |
| C11 qualityFlags 미필터 (126/618) | **수용 (재현: 브로드 립 TOP10에 기획 2개)** | F14. 단 전면 컷은 후보 20% 감소 — 유형별 정책 분리 권고로 조정 |
| C12 레거시 matchRate 역설 (프로필 첨부만으로 20점↓) | **수용** | F7에 병합. R1(표면 통합) 논거 강화 |
| B3 정정 ("첨부의 상수·3개 제한은 정확했다") | **수용 — §3 판정 완화** | 첨부 §9의 수치는 정확. 불일치는 '후보 3개 이하 종료'(코드에 없음)와 '전체 후보 기준 N개 남아요'(top-40 창) |
| B4 정정 ("로깅 전무는 과함 — 세션 logs가 postgres에 잔존") | **일부 수용** | 정확하나 만료 정리로 소멸하고 이벤트 정규화도 아님 — "LTR-가용 로깅 부재"로 표현 교정 |
| TOP3: ①저신뢰 메타데이터 증폭 ②hard 계약 불신 ③세션 비멱등·동시성 | **①② 수용, ③은 4위로 조정** | ③은 UI 정상 경로 미발생·단일워커 가정에서 완화됨. 본 검토의 3위는 '데이터 커버리지+신선도'(§0-2) — 사용자 체감 상한을 결정 |

**의견 충돌 해결 기록**: (1) C4 심각도 — 실행 재현으로 발동 조건을 좁혀 Codex 일반화를 기각, 코드 수정 필요성은 인정. (2) C11 대응 — Codex는 컷 우선, 본 검토는 유형별 분리(기획/단품은 구매가치 존재) — 후보 20% 감소의 커버리지 비용을 근거로 조정. (3) TOP3 순위 — Codex는 코드 결함 중심, 본 검토는 데이터 병목을 공동 1위로 유지(§2 정량 근거).

### 16.2 리뷰 2 (개선 설계 + 테스트 계획 + 최종 모순 점검) — 완료

Codex 총평: "진단과 방향은 타당하나 초안은 최종 아님 — R2 공식, RUN_DATE 승격, R1 교체, A8 정화 규칙, 이벤트 계약은 구현 전 수정 필요." 본 검토의 재검증과 처리:

| Codex 의견 | 본 검토 판정 | 처리 |
|---|---|---|
| R2 W_cap 공식 결함 5종 + 골든 시뮬레이션 3/9 파손 실증 | **수용 (재검증: 글리터 `>=80` 단언 실재 확인)** | R2를 소스별 capped delta로 교체, baseline 재승인 절차 명시 (§13) |
| R1 패리티 갭 — 보관함 로컬 상태·과거 reportId 유실·"세트 출력" 표현 부정확·"1줄 롤백" 과장 | **수용 (보관함 useState 직접 재확인)** | R1을 feature flag + 패리티 게이트 4항목으로 재작성 (§13) |
| A7 부정 처리를 substring이 아닌 절(span) 단위로, 가격 연산자 lt/lte/gt/gte/range 전 경로 일관 | **수용** | §13 R5·§15 회귀 행렬 반영 |
| A8 무효화 규칙의 과삭제 위험 — evidence span 단위 제거 + curated/live 공용 추출기 | **수용** | A8 성공 기준 교체 (§15) |
| 파이프라인: catalog/vector 원자 승격 누락(조용한 lexical 강등), 오퍼 재매칭 identity 사다리, 실패 상태 4분법, "게이트 거부≠롤백" | **수용 — 본 검토가 놓친 최대 설계 구멍** | §6.2 재작성 |
| 개인화: profileScore 분리(softPreferences 주입 기각), A9→A5 순서, 이벤트 스키마 필수 필드, dev fallback subject 경고 | **수용** | §7 재작성 |
| C4 재반박 ("auto+lexical 무매치도 빈 dict") | **수용 — 본 검토 보정을 재보정** | F12 발동 조건을 "embedding 실패 + auto lexical 무매치"로 확대, 반환 계약 status 분리 제안 채택 |
| "모든 발견 실행 재현" 과장, 우선순위 서술 불일치, "낮은 회귀 위험" 라벨 오류, 비용 확정치 표현 | **수용** | 증거 태그 원칙 유지·강화, 병렬 P0 2트랙 프레이밍, §14 라벨 수정, 비용은 가설 표기 |
| A3(F6) 즉시 항목 제외 | **수용** | 백로그 강등 |
| qualityFlags 유형 분리 정책 | **동의 확인** | 유지 |
| TOP3 조정(세션 비멱등 4위) | **동의 확인 + "단 A9는 A5 선행조건" 보강 수용** | 반영 |

**리뷰 2에서 기각·보류한 것**: 없음에 가까움 — 유일한 부분 보류는 이벤트 스키마의 실험(variant/exposure) 필드로, A/B를 시작하는 B7 시점까지는 nullable로 두되 스키마에는 지금 포함(재로깅 방지). Codex의 지적 중 "±60% 가격 검사가 정상 세일을 검수 대상으로 만든다"는 결함이 아니라 의도된 사람-검토 큐 동작이므로 표현만 유지하되, "오매칭 변형의 20% 차이는 통과한다"는 지적은 identity 사다리로 해소됨.

---

## 17. 남은 불확실성

1. R2 소스별 cap 값(k_s, cap_s)·가중치 재배분·s_floor의 구체 값 — 실험으로만 확정 가능 (초기값 명시됨). R2 적용 시 골든 baseline 재승인 범위(9질의 corpus 전체 재검수)도 미정.
1-b. Auradin 보관함 서버 영속화 방식 — 기존 `/products/{id}/like` API 재사용 가능 여부(레거시 product upsert 계약과 Auradin catalogItemId 호환) 확인 필요 (R1 패리티 게이트 선행 항목).
2. Titan v2의 한국어 화장품 도메인 임베딩 품질 — 골든 A/B 전 미지수 (Cohere 대안 준비).
3. 배포 워커 수(F8 실위험 여부) — 배포 형상 확인 필요.
4. 레거시 화면의 세트 출력에 대한 실사용 수요 — R1 결정의 입력.
5. 공식몰 수집기의 현재 차단 상태 — 마지막 실행(20260706) 이후 사이트 변경 가능.

## 18. 최종 결론 (요구 질문에 대한 직답)

1. **가장 먼저 고칠 것**: §14의 4 마일스톤 — M1 정확성 핫픽스(A7: "2만원 이상"이 ≤2만으로 동작 중, A8: undertone 브랜드 오염, A4, A2, A9) ∥ M2 데이터 승격(manifest 계약→A6→A10) ∥ M3 관측성(A5 — 조기 병렬) → M4 점수 개편(A1/R2). 성격 구분: **A7·A8·A1은 현재 사용자 결과를 잘못 바꾸고 있는 정확성 결함**, A9는 이벤트 수집 전에 닫아야 할 상태 정합성 결함(배치 근거는 심각도가 아니라 의존성), manifest·A6·A5는 데이터 운영과 학습 기반을 만드는 병렬 선행 작업이다.
2. **점수 로직의 가장 위험한 빈틈**: F2(단일 소프트 선호 정규화 소거) × F9(오염된 undertone 데이터)의 결합 — 저신뢰·오염 데이터가 지배 신호로 증폭되는 구조. 그다음 F11(질문 하드 컷이 unknown≠negative 위반), F1(죽은 축의 매치율 왜곡).
3. **데이터 vs 알고리즘**: **데이터가 더 치명적**. 색상 17.5%·공식 근거 0건인 상태에서는 어떤 랭킹 개선도 상한이 낮다. 단 F2는 데이터와 무관하게 지금 수정 가능하므로 병행.
4. **자동 파이프라인 최소 범위**: 주1회 오퍼 갱신 트랙(§6.2) — 기존 스크립트 오케스트레이션 + 승격 게이트. 신규 인프라 불요.
5. **최소 비용 개인화**: 서버사이드 이벤트 영속화 → 지수감쇠 속성 선호 → **분리된 profileScore로 anchor 선정에만 반영** (softPreferences 주입 금지 — §7.3 단일 설계).
6. **우선 추가할 이미지**: finish/texture 질감 스와치(자체 제작). 생성형 AI 발색 이미지는 금지 영역.
7. **보고서 필터 실서비스 가능 여부**: 연결·안전장치(hard 금지)는 건전하나 **F2 수정 전에는 '참고' 계약 위반 상태** — 수정 후 §9.2 통과 시 가능.
8. **비슷한 제품 기능**: 의도(다양성 슬롯)대로 동작하나 명칭과 실질이 불일치하고 선호 모순 노출(F5) — 가드+라벨 수정 필요. 제품별 유사 탐색은 부재하며 rankedCache 재랭킹으로 저비용 구현 가능.
9. **지금 적용 가치 있는 최신 기술**: 수집 시점 LLM 속성 추출(즉시), 실임베딩+하이브리드 RRF(실험 후), 경량 선호 프로필(로깅 후).
10. **지금 적용하면 위험한 기술**: LLM 실시간 재랭킹/의도 확정(결정론 계약 파괴), 세션 시퀀스 모델·LTR(데이터 미달), DPP류 다양성(과설계).
11. **구현 순서**: **§14의 M1~M4 마일스톤 표가 유일한 정본이다** — 이 절은 독자적 순서를 만들지 않는다. 요지만: M1(정확성 핫픽스)·M2(데이터 승격)·M3(관측성 — A5는 선행조건 충족 즉시 조기 병렬 개시)은 병렬, M4(점수 개편)는 M1 완료 후. 그 뒤 R1 패리티 게이트 → B2/B3/B8 게이트 승격 → 실험 인프라 후 온라인 A/B(프로필 실반영은 여기서).

---

## 부록 B. 재현 계약 (증거 레지스터)

본 보고서의 [확실] 판정은 아래 환경·명령으로 재현된다. 스크립트와 Codex 리뷰 원문은 `reports/auradin/recsys_review_evidence_20260714/`에 보존.

### B.1 검증 기준 환경

- **커밋**: `7724e1c249f83b2bc3e9600cb46c046a847d1358` (브랜치 aura-cosmetic-search-engine). **소스·데이터 무변경.** 작업트리 미추적 파일: ① `아우라딘_알고리즘_검토.md`(검토 입력), ② 본 보고서, ③ `reports/auradin/recsys_review_evidence_20260714/`(증거 스크립트·Codex 리뷰 원문 — **현재 미추적 로컬 파일, 커밋 전 상태**. Git 보존을 완성하려면 커밋 필요).
- **데이터 아티팩트 SHA-256 (전체)**:
  - `data/auradin/catalog/catalog_items_mvp_20260708.jsonl` → `81d990c0fca4bdf65f82b3ea7fb3df130eba32341bcb07365eada3ee9dbee73f`
  - `data/auradin/knowledge/product_knowledge_chunks_mvp_20260708.jsonl` → `d8cd174e97646513f25020321944fefd45035a43f462607248c801013f3b3cde`
  - `data/auradin/vector/product_knowledge_vector_index_mvp_20260708.json` → `ff8e988e1e8de83bd59c11385b54a63a6de65caa532a85406f5f5d9f50a30930` (metadata.modelId=hash-fallback)
- **파이썬: 반드시 저장소 `.venv` 사용** (`./.venv/bin/python`, 3.13) — 시스템 python3에는 pydantic/pytest가 없어 실패한다. 스크립트는 `Path(__file__)` 기반으로 저장소 루트를 계산하므로 checkout 위치 무관.

### B.2 재현 명령과 근거 매핑

| 증거 | 명령 | 뒷받침 항목 |
|---|---|---|
| 카탈로그 정량 감사 (618개, colorFamily 17.5%/eligible 10.4%, undertone 15.4%/1.3%, qualityFlags 126, 공식 근거 0) | `./.venv/bin/python reports/auradin/recsys_review_evidence_20260714/catalog_audit.py` | §2 표 전체 |
| 점수 거동 프로브 (rule/liveOffer 상수, 리포트 넛지 +0.21 스윙·TOP2 독점, floor semantic 0건) | `./.venv/bin/python reports/auradin/recsys_review_evidence_20260714/probe_scoring.py` | F1, F2, F3 |
| MMR/역할/다이얼 프로브 (촉촉→매트 discovery, λ 극단 1슬롯 변화) | `./.venv/bin/python reports/auradin/recsys_review_evidence_20260714/probe_mmr_refine.py` | F5, §10 |
| Codex 발견 재현 (투쿨포스쿨 cool 42/59, "2만원 이상"→lte, 세션 answers 3→4, 립 glossy eligible=False 27/45) | `./.venv/bin/python reports/auradin/recsys_review_evidence_20260714/verify_codex_claims.py` | F9, F10, F11, F13, F14 |
| 백엔드 테스트 | 저장소 루트에서 `cd services/backend && ../../.venv/bin/python -m pytest tests/test_auradin_golden.py tests/test_auradin_phase1_serving.py tests/test_auradin_question_engine.py tests/test_auradin_refine.py tests/test_auradin_report_profile.py tests/test_auradin_search_sessions_api.py -q` → **54 passed** (본 검토 및 외부 리뷰가 각각 독립 재실행으로 확인) | §1.3 |
| Codex 교차 리뷰 원문 | `codex_review1_final.md`, `codex_review2_final.md` (gpt-5.6-sol, xhigh, read-only) | §16 |

주의: 프로브는 서빙 카탈로그 스냅샷(20260708)에 결정적으로 의존한다 — 다른 RUN_DATE·재생성 카탈로그에서는 수치가 달라지며, 그 경우 판정의 유효성은 커밋·체크섬 기준으로 재평가해야 한다.
