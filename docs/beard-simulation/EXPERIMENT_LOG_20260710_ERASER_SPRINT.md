# 텍스처 보존 지우개 스프린트 (2026-07-10)

계획: `~/.claude/plans/floofy-crafting-meerkat.md` (과제 C-2)
불변식: pytest(자 테스트 포함) / 입술 픽셀 diff 0 / 시각 관문 육안 기록 / 홀드아웃 원샷.

---

## Step 0 — 지표 구축 + 베이스라인 · **통과 (스펙 정정 포함)**

### 만든 것

`engine/texture.py` (신규) + `tests/test_texture_metrics.py` (자 테스트 11 passed)

- `grain_map` — 고주파 휘도의 국소 RMS (텍스처 에너지)
- `relative_grain_map` — grain ÷ 국소 평균 휘도 = **텍스처 대비**
- `coherence_map` — 구조 텐서 코히런스 (0=등방성, 1=한 방향)
- `waxiness_score` = 지대의 상대 grain ÷ **본인 깨끗한 피부**(이마·윗볼)의 상대 grain
- `streak_score` = 같은 비율을 코히런스로
- `chroma_delta_ab` = 지대의 (a,b) 색도가 본인 피부 색도에서 벗어난 거리 (L 제외 — 턱 그늘은 정상)

`eval/measure_waxiness.py` — 실제 합성물(사용자가 보는 그림)에서 측정, 시각 관문 이미지 동시 생성.

### 자 테스트가 즉시 잡은 버그

평평한 이미지에서 `None`이 나와야 하는데 **1.0**이 나왔다. `GaussianBlur`의 float32 반올림이 완전
평면에서도 grain ~1e-5를 남기고, 그 잡음이 **분자·분모에 똑같이** 들어가 확신에 찬 1.0으로 나눠 떨어진다.
유효성 문턱을 실측 기반(clean-skin grain 0.57~4.06)으로 `0.05`에 재설정.

### 지표 결함 두 개를 발견하고 수정

1. **밝기 교란.** 절대 grain은 밝기에 비례한다. 수염 지대는 정의상 이마보다 어두우므로 **원본 psd05가
   0.32**로 "이미 지워진 것처럼" 측정됐다. → `relative_grain_map`(국소 대비)로 교체. 민감도가
   강한 stage 기준 **11% → 46%** 로 개선. 자 테스트로 고정(`test_waxiness_is_not_fooled_by_a_darker_zone`).
2. **둔감.** 보정 강도는 마스크 값에 비례하는데, 관대한 지대(`union>0.3`)로 재면 약하게 보정된 픽셀이
   중앙값을 지배한다. → **core 지대(`union>0.5`)** 로 변경.

### 결정적 발견 ① — 지우개는 **약한 마스크에선 아무것도 안 한다**

C1 마스크(hard 평균 0.01, shadow 0.02~0.06)로 재면 원본↔strong 차이가 거의 없다(waxiness 1.932→1.825,
ΔE 10.64→10.63). 육안으로도 psd04 네 패널이 사실상 동일.

> **뭉갬은 잠복 상태다.** 넓고 확신 있는 마스크(=우리가 채택한 CLIPSeg raw@0.06)를 먹이는 순간 터진다.
> 그래서 베이스라인은 **출시할 마스크**로 측정한다(`--masks clipseg`). 약한 C1로 재면 안 망가진 지우개를
> "고치고" 나중에 진짜로 터뜨리게 된다.

CLIPSeg 마스크로 재측정하니 psd04 waxiness **1.938 → 0.887**(strong), 육안으로도 콧수염·턱이 평평해짐.

### 결정적 발견 ② — 가설을 세우고 **스스로 기각**

두 갈래를 분리 측정 (strong 기준, 9장):

| 갈래 | waxiness 변화 |
|---|---|
| `hair` (고주파 감쇠) | **9/9 전부 감소** — 예상대로 빼기만 함 |
| `shadow` (Telea-on-low) | **6/9 증가** (pic2 8.91→9.68) |

계획서는 이를 "방향성 색 스미어"로 규정했다. `coherence_map`을 만들어 검증한 결과:

> **Δstreak = −0.000 (9장 평균), 상승은 3/9.** 방향성이 전혀 아니다. **가설 기각.**

진짜 원인을 추적했다. `beard_shadow_corrector.py:48`에서 low 레이어를 **uint8로 양자화**해
`cv2.inpaint`에 넣고 다시 float으로 받는다. 마스크 **밖**(Telea가 입력을 보존하는 영역)의 잔차를 재니:

| | 값 |
|---|---|
| 양자화 잡음 RMS | **0.577** (= 1/√3, 균일 양자화 오차의 이론값) |
| low 레이어 자신의 고주파 | 0.29 ~ 0.35 |
| **주입비** | **1.7 ~ 2.0×** |

> **uint8 왕복이 low 레이어 고유 결의 2배에 달하는 등방성 잡음을 주입한다.**
> 이것이 "Telea가 grain을 올린다"의 실제 메커니즘이며, 계획서의 "방향성 스미어" 서술은 틀렸다.

`cv2.inpaint`는 8비트만 받으므로 이 왕복은 피할 수 없다 — **단, 원본 crop(이미 uint8)에 직접 적용하면
추가 양자화가 0**이다. 즉 Step 1의 A(가는 털 inpaint를 full-res crop에 적용)와 Step 2(low-layer inpaint
제거)가 이 버그를 **구조적으로 함께 제거**한다.

### 베이스라인 (dev 9장, CLIPSeg 마스크, core>0.5)

| 지표 | orig | mild | medium | strong |
|---|---|---|---|---|
| waxiness (평균) | 3.632 | 3.514 | 3.405 | 3.206 |
| streak (평균) | 1.635 | 1.630 | 1.618 | 1.606 |
| chroma ΔE (평균) | 10.329 | 10.288 | 10.262 | 10.250 |

사진별 strong waxiness: pic1 5.691 / pic2 9.208 / pic3 2.888 / pic4 4.329 / psd01 1.481 /
psd02 0.945 / psd03 2.771 / psd04 **0.887** / psd05 **0.653**

처리시간: segment 1.744s/장(CLIPSeg 포함), correct 0.263s/stage (최대 0.709s).

### 게이트 판정 — **통과, 단 스펙을 정정**

계획서의 게이트는 "원본≈1.0, strong<medium<mild"였다. 둘 다 문자 그대로는 미달이다:

- 원본 waxiness는 0.71~8.91로 퍼진다. **이 스펙이 틀렸다** — 털은 텍스처이므로 원본은 1.0보다 **높은 게
  정상**이고, 지표는 애초에 양방향으로 설계됐다(>1 털 잔존 / <1 피부 뭉갬 / ≈1 목표).
- 단조감소는 6/9. 깨진 3장(pic2, psd01, psd03)의 원인은 **지표 결함이 아니라 위의 양자화 잡음 주입**이다.
  즉 지표는 진실을 말하고 있었다.

지표는 (a) hair 갈래의 평탄화를 9/9 포착, (b) shadow 갈래의 잡음 주입을 포착, (c) 방향성 가설을 반증할
만큼 예리하다. **판별력 확인 → 통과.** 스펙 정정 사유를 여기에 기록하고 진행한다(무인 규약).

**남은 한계(정직하게)**: waxiness는 진짜 피부 결과 아티팩트를 구분하지 못한다. 반드시 `streak`·시각 관문과
함께 읽어야 한다. Step 3의 re-grain은 **등방성**이어야 하며 그건 `streak`으로만 검증된다.

### 시각 관문 (육안 확인 기록)

- `outputs/waxiness/step0_baseline/wax_psd04.jpg` — strong에서 콧수염·턱 중앙이 평평해지고 광택이 도는
  반면, 턱선의 굵은 털은 그대로 남는다. 지우개가 **약한 곳을 지우고 강한 곳을 못 지우는** 역설.
- `outputs/waxiness/step0_baseline/wax_psd05.jpg` — 턱 잔털이 눈에 띄게 줄지만 원본 대비 피부가 미끄럽다.
- (C1 마스크 버전 `step0_clipseg` 이전 실행) psd04 네 패널이 사실상 동일 — 약한 마스크에서 지우개 무력.

### 커밋

- (이 커밋) Step 0: texture.py + 자 테스트 11 + measure_waxiness + 베이스라인

---

## Step 1 — CorrectionContext + A(가는 털 inpaint) · **채택**

### 계획 수정 (증거 기반)

계획서의 A는 `masks.hard`에서 형태학으로 얇은 구조를 뽑으라고 했다. **틀렸다.**
`hard`는 털을 담고 있지 않다 — (a) `segment_beard`가 strand_cov>0.28에서 통짜 덩어리로 solidify하고
(`beard_segmentation.py:302-309`), (b) 출시 구성은 `hard = max(c1.hard, clipseg_region*0.9)`로 **넓은
지대**로 덮어쓴다. 덩어리를 형태학으로 얇게 하면 **가장자리 껍질**이 나오지 털이 아니다.

→ 대신 **crop 휘도에서 black-hat으로 직접** 얇고 어두운 구조를 찾는다(C1이 쓰는 연산자, 커널 fw×0.010).
마스크의 출처·의미와 무관하게 동작한다. 지대(`union>0.2`) 안으로 제한하고 보호막 제외.
strand 커버리지 >35%면 "털이 아니라 덩어리"로 보고 A를 건너뛴다(stats에 기록).

### 구현

- `CorrectionContext`(신설): black-hat 1회 + Telea 2회만 보유. 주파수 분리는 **stage별로 유지** —
  blur는 선형이나 strand 가중치가 비선형이라 hoist하면 결과가 달라진다.
- A는 **원본 uint8 crop**에 Telea(radius fw/150) → 양자화 주입 0.
- low = blur(**strand 제거된** base) → 털의 어둠이 low에 안 섞인다(스미어 #3 제거).
- `_stubble_inpaint` 삭제. `stubble_inpaint` 인자는 하위호환 유지(strand_strength 기본값 선택용).
- `pipeline.py`: context를 사진당 1회 빌드해 사다리 전체에 관통. configs에 `strand_strength` 0.35/0.65/0.90.

### 게이트 — 전부 통과

| 항목 | 기준 | 실측 |
|---|---|---|
| 기존 테스트 무수정 통과 | 전부 | **51 passed** |
| context 시간 | ≤1.5s | **0.262s/장** (최대 0.704) |
| stage 시간 | ≤0.3s | **0.022s/stage** (기존 0.263 → **12배**) |
| 입술 불변식 | diff 0 | **9장 전부 0** |
| 스미어 육안 감소 | 필수 | **아래 참조** |

### 시각 관문 (육안 기록) — `outputs/waxiness/compare_step0_step1/cmp_psd04.jpg`

STEP0(before) 확대: **턱 아래에 직선 경계를 가진 납작하고 어두운 직사각형 블록**이 있다.
`union>0.22`를 이진화해 팽창시킨 inpaint 마스크의 경계가 그대로 이미지에 찍힌 것 — 이것이 사용자가
말한 "피부를 뭉갠다"의 정체다. 블록 내부는 결이 사라지고 광택이 돈다.

STEP1(after): **그 블록과 직선 경계가 완전히 사라졌다.** 피부 결과 잔털이 경계 없이 이어진다.

수치도 일치: psd04 strong waxiness **0.887 → 1.041** (과평탄화에서 목표 1.0으로 복귀),
psd02 0.945→0.922, psd05 0.653→0.675. streak 변화 없음(1.606→1.601) — 새 아티팩트 없음.

pic 세트는 waxiness가 소폭 상승(털이 더 남음): `_stubble_inpaint`의 넓은 점 제거(`hard>0.45`)를
훨씬 보수적인 strand 마스크(ROI의 0.2~1.2%)로 교체했기 때문. **이 스프린트의 목표는 결 보존**이고
"덜 지움"은 Step 2/3 이후 강도 재조정으로 다룬다.

### 남은 것

`low_inpainted`는 아직 uint8 왕복 Telea다(양자화 주입 0.577 RMS 잔존). Step 2에서 통째로 제거된다.

---

## Step 2 — B: Telea-on-low 삭제, 국소 피부색으로 크로마 이동 · **채택**

### 구현

- `beard_segmentation.local_skin_reference()` 공개 래퍼 신설 — shadow 채널이 "이 픽셀이 피부에서
  얼마나 먼가"를 재는 바로 그 목표장을 corrector가 "어디로 옮길까"에 재사용. 정의가 하나로 통일된다.
- `low_inpainted`(Telea) 삭제 → `local_ref_lab` 목표장.
  - **크로마(a, b)는 `shadow_w`만큼 완전히** 이동 (수염의 시각적 서명 = 푸른 잿빛 cast)
  - **L은 `shadow_w × luma_shift_frac(0.75)`만** 이동 — 턱은 원래 그늘져 있고, L을 끝까지 당기면
    얼굴의 입체 모델링이 지워져 붙인 자국처럼 보인다.
- Lab 변환은 **float 경로 전용**(`_bgr_to_lab`/`_lab_to_bgr`). uint8을 거치면 Step 0에서 특정한
  0.577 RMS 양자화 주입이 되살아난다.
- configs에 `luma_shift_frac: 0.75`.

### 게이트 — 전부 통과

| 항목 | 기준 | 실측 |
|---|---|---|
| `test_correction_moves_shadow_toward_skin` | **무수정** 통과 | ✅ (51 passed) |
| chroma ΔE 개선 | 필수 | **10.329 → 8.080** (strong), **9/9 사진 전부** |
| 턱그늘 육안 보존 | 필수 | ✅ 아래 |
| 입술 불변식 | diff 0 | **9장 전부 0** |
| 새 방향성 아티팩트 없음 | — | streak 1.639 ≈ **원본 1.635** |

### 지표 변화 (strong, dev 9장 평균)

| | baseline(Step0) | Step 1 | **Step 2** |
|---|---|---|---|
| waxiness | 3.206 | 3.240 | **2.802** (목표 1.0) |
| chroma ΔE | 10.250 | 10.217 | **8.080** |
| 단조감소 | 6/9 | 6/9 | **7/9** |
| context 시간 | — | 0.262s | **0.104s** |
| stage 시간 | 0.263s | 0.022s | 0.024s |

**Step 1까지 ΔE는 10.33→10.22로 꿈쩍도 안 했다.** Telea-on-low는 색을 고치는 게 아니라 뭉개기만 했던 것.
Step 2에서 처음으로 색 얼룩이 실제로 교정된다(−2.25). psd03 19.03→13.36, pic2 18.05→10.51.

### 시각 관문 (육안 기록)

- `outputs/waxiness/compare_step1_step2/cmp_psd04.jpg` — STEP2에서 콧수염·턱의 푸른 얼룩이 옅어지고
  **턱→목으로 이어지는 그늘은 그대로 남는다**(얼굴형 왜곡 없음). 피부 모공 결이 살아 있다.
- `.../cmp_psd03.jpg` — 인중·턱의 cast가 확연히 감소(ΔE 19.03→13.36), 잔털은 유지, 결 보존.
- `.../cmp_psd05.jpg` — 변화 미미(원래 ΔE 5.96으로 낮음). 이상 없음.

### 남은 문제 (정직하게)

- waxiness가 여전히 2.80(strong)으로 1.0에서 멀다. 두 요인이 섞여 있다:
  (a) **털이 아직 많이 남음** — pic 세트 4.4~6.5. CLIPSeg raw heat가 약해 마스크가 옅다.
  (b) **결 손실** — psd04 1.04, psd05 0.71로 이미 1.0 근처/이하. 여기가 Step 3(re-grain)의 대상.
  즉 두 세트가 정반대 방향으로 1.0을 벗어난다. **평균 하나로 판단하면 안 된다.**
- `high_out = high * (1 - hair_w)`(최대 85% 감쇠)는 그대로다. 되살리는 단계가 없다 = Step 3.

---

## 다음 (사람 판단 대기)

Step 3 — C: re-grain. 본인 피부에서 채취한 결을 에너지 부족분만큼 등방성으로 재주입.
**단, 계획대로 여기서 멈추고 사람이 before/after를 보고 Step 3 필요 여부를 결정한다.**
Step 0~2만으로 충분하면 `regrain:false`로 출시하고 위험한 결 합성을 아예 생략할 수 있다.

---

## Step 2.5 — 3중 희석 제거 + 참조장 색 클램프 (2026-07-10 오후, 사용자 검수 후)

### 계기 (사용자 관찰 2건 → probe로 정량 확인)

사용자: "뭉개지진 않았는데 아예 안 지워진 것처럼 보여" + "턱에 원래 없던 검은 그림자".
probe(psd04 strong, zone core 24,665px)로 두 관찰이 **같은 원인**임을 확인:

- **회색 띠 = 점묘 평균 노출.** 원본 zone 평균 gray 118.6, 눈이 읽는 '털 사이 피부'는 138.2.
  털 대비를 지우면 평균만 남는다. 평균은 +4 밝아졌는데 지각적으로는 어두워 보임.
- **밝힘·지움이 3중 곱으로 희석.** 확신도(마스크값)가 corrector 가중치와 합성 알파에 **두 번**
  곱해짐 + luma 0.75 부분 이동. 합성 알파 zone core 평균 0.571 → 필요한 회복 19.6L 중 4.0L만 반영.
  털 대비 순감 ~30%. 턱선 가장자리(heat 감쇠, 알파 p10 0.33)가 가장 약하게 지워짐.
- **단위 함정:** Lab L과 gray가 같은 피부에서 ~8 어긋남. 게이트/진단은 gray로 통일해 측정.

### 변경 (커밋 1개)

1. `confidence_ramp` 신설 (`beard_segmentation.py`, smoothstep 0.15→0.60): 확신도는 **어디를**
   지울지 정하고, 세기는 stage가 정한다. 0.15 미만 반점은 0, 0.60 이상은 1로 포화.
2. `derive_soft_blend`: union을 램프로 포화 후 페더링, `max(blur, core)`로 얇은 띠 중심부 처짐 방지.
   합성 알파는 공간 이음새 전담 — 확신도 이중 곱 제거. (psd04 core 알파 0.571 → 0.994)
3. `correct_crop`: shadow_w/hair_w도 램프 confidence 사용.
4. **점묘 빚 상환**: 감쇠된 high층의 국소 평균(blur)을 zone confidence만큼 제거 — 띠의 평균 어둠은
   (이동된) low층 책임으로 넘기고 high층은 순수 결만 나른다.
5. `luma_shift_frac` 0.75 → 1.0 (전 config): 국소 참조장이 조명 그라디언트를 이미 보존하므로
   전량 이동해도 턱 그늘은 유지됨(시각 관문으로 확인).
6. `_STRAND_PERCENTILE` 94 → 90: 가는털 인페인트 그물 확대.
7. **참조장 색 클램프** (`_MAX_REF_AB_DEV = 6.0`): psd05에서 국소 참조장의 chroma가 전역 피부
   모델에서 지대 전체적으로 11~13 ab-unit 이탈(정상 사진 ≤6, psd04 max 5.8) → full-strength가
   이를 주황 얼룩으로 인쇄(ΔE 5.96→10.37 회귀). 참조장의 밝기(조명)는 신뢰, 색은 전역 모델
   반경 6으로 제한. 클램프 후 psd05 strong ΔE 6.56 (원본 대비 +0.6, 잡음 하한 수준).

### 결과 (dev 9장, --masks clipseg)

| 지표 (strong) | Step 2 | Step 2.5 |
|---|---|---|
| psd04 zone 최종 gray | 122.6 (주변 대비 −15.6, 띠) | 130.6 (**주변 ring 125.6보다 +5**) |
| chromaDeltaAb 평균 | 8.08 | **3.94** (9장 중 8장 개선, psd05만 +0.6) |
| 합성 알파 (psd04 core) | 0.571 | 0.994 |
| waxiness strong | 2.80 평균 (털 잔존이 지배) | psd02 0.27 / psd04 0.20 / psd05 0.43 — **이제 결 손실이 지배** |

- pytest 53개 전부 green (새 자 테스트 2: `confidence_ramp` 의미, 합성 알파 포화;
  새 불변식 1: `test_zone_mean_darkness_is_not_printed_into_the_result`). 기존 불변식 무수정.
- 입술 불변식: 9장 × 3 stage, protect 영역 픽셀 diff 0.
- 타이밍: context 0.103s/장, correct 0.041s/stage — 예산 안.

### 시각 관문 (outputs/waxiness/step25b_refclamp/, 직접 관찰 기록)

- `wax_psd04.jpg` — MILD 자연스럽고 수염 옅어짐. MEDIUM 수염 거의 소멸, 검은 띠 없음, 톤 자연.
  STRONG 수염 완전 제거되나 지대가 **결 없는 매트한 면**이라 패치 티가 남음(턱 아래 경계).
- `wax_psd03.jpg` — MILD 양호. MEDIUM/STRONG 인중·턱이 평평한 단색 면 + 볼 대각 경계 노출.
- `wax_psd05.jpg` — 주황 얼룩(클램프 전 step25_saturate에 존재) 대부분 소멸. STRONG 인중
  양끝에 옅은 매트 패치.
- `wax_pic2.jpg`(클램프 전) — 굵은 털이 많은 사진은 톤만 바뀌고 털이 남아 어색. pic 세트는
  털 대비 자체가 커서 85% 감쇠로도 잔존 — 마스크가 아니라 감쇠 한계.

### 판정

**채택.** 사용자가 지적한 두 증상(안 지워짐, 검은 그림자)은 해소. 남은 결함은 단일 종류로
수렴: **지운 자리에 결이 없다** (waxiness 0.2~0.8, 매트 패치·경계 티). 이것이 Step 3
re-grain의 정확한 입력 상태다 — 계획의 예측("지움이 세지면 왁스가 실제로 생긴다")대로.

---

## Step 3 — re-grain: 본인 피부 결 재주입 (2026-07-10 저녁)

### 구현

- `texture.build_grain_field`: 본인의 깨끗한 피부에서 32px 결 조각을 떠서(고역통과·단위 RMS 정규화)
  지터 반겹침 격자 + Hann 윈도우 + 시드 고정 회전/반전으로 퀼팅 → 단위 RMS 결 필드. 전 과정 시드
  고정 = 결정론(테스트로 강제).
- 주입은 **에너지 기준**(계획 F5): rms_add = √(max(target² − current², 0)). 결이 남은 곳은 자체
  제한, 평탄한 곳만 채움. target = 깨끗한 피부의 결 대비 × 국소 밝기(그늘에선 조용한 결).
  휘도에만, conf_zone 게이트로 지대 안에만.
- **F4 리스크 적중**: 이마·윗볼 기준 패치는 lower-face 크롭 밖 → `skin_ref_mask`가 실사진에서
  0px, 결 뱅크 생성 실패로 재주입이 조용히 무효(1차 측정에서 수치 무변화로 발각). 계획된 폴백
  구현: 크롭 안에서 탐지기가 수염 아님이라 한 피부(union<0.10, 경계 9px 침식)를 채취원으로.
- 자 테스트 3종: 결정론(두 번 실행 byte 동일), 지대 밖 무변화, 평탄화된 지대의 결 에너지 회복
  (1.3배 이상, 과주입 1.6배 미만). 55개 전부 green.

### 자의 기준점 발견 (중요)

주입이 목표에 정확히 도달했는데도 waxiness가 0.46~0.70에 머무는 사진들이 있었다. 원인은 코드가
아니라 **자의 기준점**: waxiness는 이마 결을 1.0으로 삼는데, 이마 결은 턱 주변 피부의 결보다
psd02 2.3배 / psd05 2.3배 / psd04 1.3배 굵다(측정). 눈이 지대를 비교하는 대상은 이마가 아니라
**바로 옆 피부**다. 이마 기준으로 1.0을 채우면 턱만 모래처럼 거칠어진다(지각적으로 오답).
→ 기존 자는 무수정 유지, 보조 자 `waxinessLocal`(기준점 = 탐지기가 수염 아님이라 한 인접 피부)
을 추가해 병기.

### 결과 (dev 9장, strong)

| | 이마 기준 waxiness | 인접 피부 기준 waxinessLocal |
|---|---|---|
| psd02 | 0.46 | **1.02** |
| psd04 | 0.70 | **0.90** |
| psd05 | 0.52 | **1.18** |
| psd03 | 0.87 | 1.27 |
| 게이트(≥0.7) | 6/9 (psd04 0.696은 잡음 하한 내) | **9/9** (최저 0.895) |

- streak(방향성): 평균 1.650(주입 전 1.688) — 주입 결이 등방성임을 확인, 악화 없음.
- 입술 불변식·기존 불변식 무수정 green. 타이밍: context 0.138s/장, correct 0.046s/stage.

### 시각 관문 (outputs/waxiness/step3b_regrain_fallback/, step3c_localanchor/, 직접 관찰)

- `wax_psd04.jpg` — MEDIUM(0.79)이 매우 자연스러움: 수염 소멸, 결 살아있음, 띠 없음. STRONG도
  결 회복, 다만 턱 아래 경계가 아직 약간 보임.
- `wax_psd02.jpg` — 전 stage 자연스러움. STRONG에서 턱 모서리 그늘을 밝힌 옅은 자국.
- `wax_psd05.jpg` — 턱 자연. 인중 양끝 옅은 패치와 좌측 크롭 경계 이음새 잔존.

### 판정

**채택** (근거: 인접 피부 기준 9/9 + 시각 관문에서 매트 패치 소멸 + 등방성 무악화 + 결정론).
이마 기준 게이트는 6/9로 미달이나, 미달 사진 전부가 "이마가 유난히 거친" 사진이며 인접 피부
기준으로는 1.0 안팎 — 기준점 편향으로 기록하고 자는 병기 체제로 유지.

### 남은 결함 (다음 작업, 홀드아웃 전에)

1. **턱선 이음새**: strong에서 지대 경계가 턱 그늘/실루엣을 가로지르는 곳에 옅은 경계 노출
   (psd02 턱 모서리, psd03 볼 대각, psd05 크롭 좌측 경계).
2. **pic 세트 굵은 털 잔존**(waxiness 4~6): 85% 감쇠 한도(MAX_HIGH_ATTENUATION 계약)의 한계.
   가는털 인페인트 확대 또는 감쇠 한도 재론 필요.
홀드아웃(원샷)은 위 2건 처리 후 최종 설정으로만 소진한다.

---

## Step 4 — 이음새 제거: 안쪽 램프 alpha + CLIPSeg 스무딩 + 톤 단차 자 (2026-07-10 밤, 사용자: "medium이 낫다, 이음새는 둘 다 거슬린다")

### 진단 (측정 → 가드 사각지대 발견)

- 기존 `seam_score` 가드(Sobel gradient 비율)는 medium/strong 전 장 **통과(~1.0, 임계 1.35)**.
  그런데 눈엔 이음새가 보임. 원인: 사용자가 보는 이음새는 **매끄러운 저주파 톤 단차**인데
  Sobel은 고주파만 봄 → 가드가 못 잡는 종류. (규율 1: 자에도 테스트 → 새 자 필요)
- `probe_seam` 시각화로 두 원인 확정: ① 턱선을 따라 보정이 밝힌 띠가 손 안 댄 목과 만나는 톤 단차,
  ② CLIPSeg 저해상도(352px) heat를 크롭으로 확대할 때 생기는 ~30px 격자(alpha 포화가 드러냄).

### 변경

1. `derive_soft_blend`: 바깥 Gaussian → **경계 안쪽 거리 램프**. 기존 alpha는 경계에 0.5가 놓여
   보정이 경계에서 절반 걸림(=단차). 거리 변환으로 경계에서 0, 안쪽 `feather_ratio×fw`에서 1로
   smoothstep → 경계에 단차 없음. 램프가 heat 값이 아니라 거리에만 의존하므로 **블록도 내부로
   인쇄 안 됨**. (제품 경로 pipeline.py도 이 함수를 써서 함께 수혜)
2. `clipseg_masks`(출시 예정 마스크): heat를 임계 전에 Gaussian(σ=fw/45)으로 스무딩 → 격자 소멸.
3. `texture.tonal_seam_step`(신규 자, report-only): 편집 발자국(result−original)의 **경계 링을
   따라 평균**(등방성 grain은 상쇄, coherent 단차만 생존) → 경계 안/밖 좁은 밴드의 톤 점프(회색조).
   공간 블러를 안 써서 step(수 px)과 ramp(수십 px)을 구분. 자 테스트 2종(하드 단차 감지, 램프 통과).

### 결과 (A/B: 같은 보정, alpha만 교체, medium)

| 사진 | 톤 단차 OLD→NEW | 감소 |
|---|---|---|
| psd02 | 20.8 → 15.6 | −25% |
| psd03 | 47.0 → 17.2 | **−64%** |
| psd05 | 22.0 → 10.1 | −54% |

### 시각 관문 (outputs/seam_ab/, 직접 관찰)

- `ab_psd03_medium.jpg` — OLD은 하관 전체가 밝은 타원 패치 + 뚜렷한 경계(가장 심함). NEW은 경계
  소멸, 결 연속, 자연스러움. **이 스프린트에서 가장 큰 육안 개선.**
- `ab_psd05_medium.jpg` — OLD 좌측 턱의 밝은 얼룩·경계가 NEW에서 melt.
- `ab_psd02_medium.jpg` — OLD 턱 모서리 경계가 NEW에서 완화(하관이 목보다 약간 밝은 잔여는 남음).

### 판정

**채택.** 톤 단차 자 신설로 가드 사각지대를 메웠고(규율 1·2), A/B에서 3장 전부 수치·육안 동시
감소. 입술 불변식 9×3 diff 0, 56 테스트 green. medium이 사용자 선택 stage이며 그 이음새가 목표였음.

### 남은 한계 (기록)

- pic 세트(성긴 긴 털): waxiness 4~6로 털이 남음. 85% 감쇠 한도(제모 질감 계약)의 구조적 한계 —
  판정(detection)이 아니라 지우개 감쇠 상한 문제. 별도 논의 대상, 이번 이음새 작업 범위 밖.
- 톤 단차 자는 report-only(GuardCheckId enum 고정). 배선은 Step 5 이후 별도.

---

## Step 5 — 홀드아웃 원샷 (2026-07-10 밤). 단 한 번 소진, 튜닝 금지.

동결 설정: stage=medium(사용자 선택), 마스크=채택 CLIPSeg raw-sigmoid(thr 0.06)+C1 strands,
엔진=Step 2.5/3/4 커밋 기본값. 게이트는 **보기 전에** `run_holdout_eraser.py`에 사전 등록.

### 결과 (hold1~3, medium)

| id | orig ΔE | waxLocal | chroma ΔE | seam | guard |
|---|---|---|---|---|---|
| hold1 | 10.66 | 1.23 | 6.52 | 8.7 | pass |
| hold2 | 19.58 | 1.98 | 13.41 | 11.7 | pass |
| hold3 | 11.23 | 1.13 | 6.27 | 4.6 | pass |

- **G1** waxLocal∈[0.7,1.6]: **2/3** (≥2 필요) PASS — hold2만 1.98(밀집 수염 잔존).
- **G2** chroma<원본: **3/3** PASS. **G3** seam≤18: **3/3** PASS. **G4** 가드 통과: **3/3** PASS.
- **G5** 시각(outputs/waxiness/holdout_oneshot/, 직접 관찰):
  - hold1 — medium 자연, 콧수염·턱 잔털 옅어짐, 이음새·매트 없음, 입술 유지.
  - hold3 — medium 자연, 턱 수염 확연 감소, 결 보존, 이음새 없음.
  - hold2(최다 털) — 콧수염 일부 감소하나 잔존(waxLocal 1.98). **인공 결함(이음새·매트·입술)은
    전무** — 기록된 "굵은/밀집 털 85% 감쇠 상한" 한계이지 회귀 아님.

### 판정: 채택 (일반화 확인)

수치 4/4 + 시각 3/3(결함 기준). 홀드아웃은 소진됨 — 이후 이 3장으로 어떤 임계도 조정하지 않는다.
남은 유일한 미달(hold2 완전 제거 실패)은 dev pic 세트와 동일한 밀집-털 감쇠 상한 문제로, 별도 과제.

## 스프린트 총괄 (Step 0~5, 커밋)

fb5c420 자 구축 → e38111c 가는털 context → 6cd8393 크로마 이동 → 206ed1c 3중 희석 제거 →
be1822d 결 재주입 → (Step4) 안쪽 램프 이음새 제거 → (Step5) 홀드아웃 원샷 채택.
결과: 검은 띠·주황 얼룩·매트 패치·이음새 4대 결함 해소, 색 얼룩 ΔE 10.3→3.9(dev strong),
이음새 psd03 −64%, 홀드아웃 일반화 확인. 미해결: 밀집/굵은 털 완전 제거(감쇠 상한).

---

## 2026-07-11 후속: 유령 자 + 오라클 대체 스파이크 (커밋 cc46a01)

배경: 사용자가 "감쇠" 결과를 기각("아예 안 지워진 것처럼 보임") → 유령 자(GhostResidual,
black-hat 2스케일) 구축 → 기준선: medium의 최암부 털 생존율 r=1.00 (dev 8/9장). 원인은
점수 버그가 아니라 감쇠 방식의 상한 + 유효 지지 부족(Codex 검증). 포토샵 레퍼런스 2건
(사용자 제공)과 수렴: 감쇠 → **어두운 픽셀 키잉 후 대체**로 방향 전환, 반증 스파이크 실행.

### 스파이크 결과 (pic2·pic4·psd03·psd05, 승인된 p95 광역 키)

- 유령 게이트: B/C가 4장 중 3장 통과 (r 1.00→0.00~0.05). pic4(흐린 사진)만 0.375 부분 실패.
- 립코너 컨트롤 표류 0.0 (스코프 밖 바이트 동일), 턱 그늘 변화 ≤1.5%.
- **블라인드 판정(X/Y/Z, 매핑 봉인)**: 사용자 Y 전승 — Y=B(pic2·psd03), Y=C(pic4·psd05).
  A(현 medium)는 한 번도 선택 안 됨. → 대체>감쇠 방향은 블라인드로 검증됨.
- **그러나 품질 기각**: "수염을 뭉개기만 했지 색이 퍼져 남음. 지우개로 문지른 느낌.
  전부 사라진 것도 아님. 서비스 불가."

### 잔존 얼룩 프로브 (기각 원인 정량)

재건 목표 S(강건 2차 + NC 잔차) 대비 B의 저주파 잔존율:

| 사진 | 얼룩 px (지대 대비) | 필요 밝힘 L Q50/Q90 | B 잔존율 |
|---|---|---|---|
| pic2 | 5,547 (51%) | 18.2 / 52.4 | **1.00** |
| psd03 | 37,103 (65%) | 22.0 / 60.5 | **1.10** |
| psd05 | 22,381 (40%) | 6.3 / 12.6 | **0.87** |
| pic4 | 2,488 (26%) | 3.9 / 7.0 | **0.96** |

- 기각 3요인 코드 확정: ① B는 저주파 L 무편집(compose(lab_low[...,0])), C 리프트 캡
  7~12L vs 필요 최대 60L → "색 퍼짐" 방치. ② 키가 지대의 5~16%만 커버 → 못 지운 털.
  ③ 키 페더 안에서만 고주파 교체 → 어두운 저주파 위 밝은 조각 = 모틀링("문지른 느낌").
- 유령 자는 black-hat 기반이라 저주파 얼룩에 맹목 → 저주파 잔존 자(lowResidual) 신설 필요.

### 다음 방향 (Codex gpt-5.6-sol xhigh 교차검증 진행)

털별 키잉 → **지대 전체 2밴드 재건**: 저주파 L·ab를 외곽 클린 링에서 보간(증거 가중,
턱 그늘 보존), 고주파는 초과 증거 연속 가중으로 도너 결 교체. 포토샵 레퍼런스와 동일 구조.

### Codex 5차 교차검증 판정 (gpt-5.6-sol xhigh, 2026-07-11 수신)

**총판정: 조건부 GO** — "zone-wide **target reconstruction** GO / zone-wide **unconditional
replacement** NO-GO. S confidence·beard evidence·anatomy preservation·saturated high core·
two-sided low ruler를 갖춘 guarded reconstruction으로만 진행."

- 진단 3건(저주파 방치·키 과소커버·모틀링) 코드로 확인. psd03 잔존율 1.10의 원인
  (Lab 왕복 비선형 누화)까지 규명.
- **신규 버그**: 위쪽 확장 키가 합성 직전 strict zone 7×7 dilation(`allowed`)에 절단 —
  0.03fw 확장 실질 무효, keyPx 과대 보고, 확장 영역 털은 편집·채점 모두 누락.
- 멘토라비알 폴드: 외곽 링만으로 내부 홈 복원은 식별 불가능 → landmark 구조 밴드로
  macro 음영(σ≈0.08–0.12fw) 보존, hair-scale만 보정. 이거 없으면 "밝은 에어브러시 패치" 기각 예고.
- 캡 재설계: 고정 7/12L 폐기, 완전 uncapped 금지, 사진별 Q97/Q99 winsorize + 저신뢰 w 축소
  + S 이상 시 명시적 abstain. L 보정은 delta에만 (D=max(S−O,0)).
- quilt 자체가 "비늘·도장" 무늬의 공범: 90° 회전 제거, 동일 y-band donor, SSD 결정론 선택.
- lowResidual 자 게이밍 6종 지적 → two-sided(잔존+overLift)+sConfidence+structureBand
  자 스위트로 확장, 최종 결과 재저역통과 측정, N/A는 fail.
- pic4: OOD 선언 전 원인 계측 먼저; 최후엔 시뮬레이션 거부 게이트.
- 순서: 증거 커밋 → 자 동결 → 키 버그 수정 → 고주파/quilt → 가드된 재건 → 4장 진단 →
  dev 9장 → 블라인드 ③("서비스 허용" 기준) → 통과 시에만 S1a 배선 후 이식.

프로브 요약: `probe_low_residual_20260711.json`. 실행 계획: `.claude/plans/floofy-crafting-meerkat.md` 승인본.

### 아카이브 AI 생성 사진 선별 (2026-07-11, 사용자 제공 68장)

`아카이브/` — AI 생성 아시아 남성 수염 사진. 사용자 조건: 실사 같은 것만, 이질감 있으면 금지.
시리즈 대표 5장 직접 관찰(01, 2-3, 3-10, 4-2, asian_stubble_40s):

- 넘버 시리즈(01~20, 2-x, 3-x, 4-x PNG 51장): 실사 셀피 스타일(자연광·지하철·밤거리·카페),
  수염 점묘·피부 결 자연 → **사용 가능** (사용 시 장별 재확인).
- asian_* JPG 17장: 스톡 사진풍(완벽 조명·왁시한 피부) → **보류** (왁스감 판정 오염 위험).

용도 제한: **미라벨 시각 강건성 검증 전용**. GT 없음 → 오라클 스파이크/게이트 튜닝 금지.
dev 9장 통과 후 일반화 눈 검증(체크포인트 ③ 보조 자료)에만 사용.

---

## 2026-07-11 스파이크 v2: 가드된 지대 재건 (spike/zone_recon.py)

Codex 조건부 GO 설계 구현 + 반복 디버그 8회 (각 반복은 시각 관문 또는 수치 프로브로 원인
특정 후 수정 — 임계 완화는 근거 기록 시에만):

1. **자 스위트 신설** (dcebe25): stainResidual·overLift(신규 초과만)·structureBand·abQ90,
   자 테스트 10종 green. overLift는 "편집이 새로 만든 초과"만 계상(원본이 S보다 밝은 곳 무죄).
2. **확장 키 절단 버그 수정** (69a47e9): cand_zone = 위쪽 확장 공유, soft/saturated 고주파
   가중(robust-z, p99 포화, hysteresis).
3. **포락선 목표**: T = min(S, E-blend). E = 보호영역 제외 국소 최대(0.05fw)의 저주파 —
   "털 사이 밝은 피부"가 목표라는 사용자 검증 통찰의 구현. 밀집 코어는 λ(털 커버리지)로
   S 쪽 블렌드. S abstain은 S가 실제 목표를 결정하는 픽셀에만 적용.
4. **희석 3대 원인 제거**: ① scope 이중 가중(w² → 지지영역 이진 scope), ② 곱셈 가드 스택
   (min-캡으로 재구성), ③ 얇은 밴드 재블러 되퍼짐(Van-Cittert 역합성곱 3회, 2×캡).
   측정: 의도 리프트 도달률 19% → ~85%.
5. **도너 위생**: L-매칭이 어두운 '클린'(=턱 그늘, GT 밖 잔털 서식지) 패치를 선호해 털을
   재도장 → black-hat 초과 패치 도너 금지. psd03 ghost 0.53→0.41, B의 1.92 이상치도 해소.
6. **코·콧구멍 정책**: 콧구멍 인접(0.025fw)은 고주파 페더 베토(콧털 스코프 밖, 사용자 확정),
   코 밑 밴드는 저주파 무편집(포락선 창이 코 그림자보다 넓어 평탄화 위험 — 시각 관문 3회로
   확정). 채점 제외는 강베토 코어만, 제외 수 기록 (pic2:0, pic4:1, psd03:18, psd05:10).

### 동결 결과 (C=v2; ghost Q90r/생존, stain 잔존율, struct)

| 사진 | A(medium) | C(v2) | 비고 |
|---|---|---|---|
| pic2 | ghost 1.00, stain 1.00 | **ghost 0.00, stain 0.67**, struct 1.00 | 얼룩 잔존은 코 밑 베토 비용 |
| pic4 | ghost 1.00, stain 1.00 | ghost 0.63✗, stain 0.65 | 흐린 사진 — 품질 게이트(거부) 후보 |
| psd03 | ghost 1.00, stain 1.02 | **ghost 0.00, stain 0.42**, struct 1.00 | 콧구멍 코어 제외 18 기록 |
| psd05 | ghost 1.00, stain 0.82 | **ghost 0.00, stain 0.29**, struct 0.99 | 전 게이트 통과 |

overLift ≤1.5L(신규 초과), 턱그늘 ≤±1.6%, 컨트롤 표류 p95 ≤1바이트(v1은 0 — Lab 왕복
스코프 확대 비용, 공개 보고).

### 시각 관문 관찰 (직접 열람, psd03_upper 4회 추적)

- v2 초기: 코 밑 "에어브러시 패치"(Codex 예고 NO-GO) + 콧구멍 베이지 도넛 → 원인 3건
  (포락선 창 폭, 콧구멍 soft 키 발화, 이진 베토 경계) 순차 제거.
- 동결본: 콧수염 완전 소거, 콧구멍 실루엣 보존, 잔여 = 코 밑 옅은 밝은 띠 + 인중 약간의
  평탄함. 판정은 블라인드 ③.

다음: 체크포인트 ③ 사용자 블라인드 판정 ("서비스 허용" 기준) → 통과 시 dev 9장 확대 +
컨트롤 추가, S1a 배선 후 프로덕션 이식.

### 체크포인트 ③ 블라인드 판정 (2026-07-11)

사용자 선택: psd03=Z, psd05=Y, pic2=X — **매핑 공개 결과 3장 전부 C(v2 가드된 재건)**.
2라운드 연속 블라인드 전승. 그러나 **서비스 허용 불가** 판정:

1. 경계 부자연 (특히 입술 쪽) — 정량 확인: 잔존 얼룩의 39~50%가 입술 링(dpr<0.03fw),
   잔존율 0.48~0.85. 원인 = 입술 인접 저주파 베토(정책)가 어두운 테로 남음.
2. 콧구멍에 뭔가 붙여놓은 느낌 — 페더 베토 구간의 "절반 도너 + 절반 원본" 혼합.
3. pic2 턱 안 지워짐 — 원인: 턱 아래 지지 링이 턱 그림자 속 → S가 어둡게 피팅 → D<2
   → 리프트 미처방 (stain 마스크에도 안 잡혀 지표 사각).
4. psd05 턱 끝 마감 이상 — chinLow 잔존 0.00 = 경계까지 완전 리프트 → 턱 그림자와 급경계.

노트: 코 밴드 잔존(0.61~0.96, 비중 37~58%)은 이번 지적에 없음 — 코 밑 잔여 어둠은
자연스럽게 읽힌다는 방증(베토 정책 유지).

다음: v2.1 설계(입술 링 scale-separated 리프트+넓은 램프, 콧구멍 전체 베토+바깥 페더,
턱 하부 페이드, S 지지 방향 가중) — Codex 교차검증 진행.

### v2.1 (Codex #6 교정 반영, 커밋 bfd5ec1) + 체크포인트 ④ 준비

- 입술 분리 리프트(D_hp 통과 + 광역 성분 0.04fw 램프인, ab 별도 가중) → 입술 링 잔존
  0.85→0.45(pic2), 0.49→0.28(psd05). 콧구멍 코어 보존 페더 + 픽셀 단위 채점(에너지 기록).
  턱은 입술과 통일된 broad gate(열별 하부 윤곽 페이드). S 지지 하부 섹터 제외(열 범위 한정).
- 회귀(같은 4장, non-blind): ghost C 0.0~0.22, 얼룩 0.24~0.71, 입술 위 어두운 테 소멸(시각),
  콧구멍 실루엣 보존. pic4는 abstain(=거부) — 의도된 프로덕션 동작.
- **체크포인트 ④ (새 dev 5장, A/C 블라인드, 절대 허용 기준)**: pic1·pic3·psd01·psd02·psd04,
  립코너 컨트롤 랜드마크(61/291) 자동 고정. 수치(C): ghost 전부 0.000, 얼룩 0.11~0.39
  (psd04 최밀집 0.112!), 컨트롤 ≤1바이트. 관찰 리스크: psd01·psd02 overLift 3.6~4.3L,
  psd02·psd04 structureBand 0.90 (폴드 부분 평탄화 가능성) — 판정은 사용자.
- pseudo-hole 격자 폴백(밀집 사진에서 격자 미적중 시 2배 밀도) 추가.

### 체크포인트 ④ 판정 (2026-07-11): 5장 전원 불합격 → 재설계 기준 발동

새 5장 A/C 블라인드(절대 허용 기준). 매핑: C = pic1·psd01·psd04의 Y, pic3의 X, psd02의 X.

| 사진 | 판정 | 결함 (사용자) |
|---|---|---|
| pic1 | C 크게 개선·자연·전반 커버, **불가** | 입 밑·양옆 갑작스런 밝은 밴드, 절반만 지워짐 ("아예 깨끗" 옵션 필요) |
| pic3 | C **불가** | 턱 밴드가 목 침범, 인중·입술밑 미커버, 인위적 모양 |
| psd01 | 둘 다 **불가** | A=원본과 동일, C=너무 얼룩덜룩(모틀) |
| psd02 | **A가 더 자연**, 둘 다 불가 | A는 거의 못 지움; C는 부자연 |
| psd04 | 둘 다 **불가** | C: 비교적 잘 덮었으나 볼·입술밑 미커버 + 너무 밝음(턱); A: 못 지움+턱 어두움 |

수치 경고(overLift 3.6~4.3L, struct 0.90)가 그대로 지각 결함으로 실현됨.
**Codex 사전등록 손절 기준 충족 → 현 구조(링-피팅 S + 포락선 + 가드 스택) 종료.
재설계: 방향성 조명장 + 랜드마크 해부 밴드 + PatchMatch 고주파, boundary-constrained.**

5라운드 인간 판정에서 도출된 재설계 요구사항:
1. 완전 소거 능력(강도 조절로 "아예 깨끗"까지) 2. 밝은 밴드/헤일로 절대 금지(조명 일관 리프트)
3. 모틀 금지(균질 질감 재건) 4. 볼·입술밑·인중 포함 지대 전체 커버(재현율)
5. 목 침범 금지(경계 규율) 6. 코 밑 자연 음영 보존(검증된 정책 유지)

### 경로 전환 (2026-07-11): 경로 B 승인 — LaMa 하이브리드

- Codex #7 판정("짙은 수염 아래 피부는 관측 불가 — 거부 없는 laser-complete는 결정론만으로
  불가, 생성형 인페인터 필요") + 사용자의 갤럭시 S25 AI 지우개 실증(삼성 = LaMa 인페인팅,
  형태 유지·질감 뭉갬) → **사용자 경로 B 승인. 계약 수정: 생성형 신경망 허용 —
  단 추론 결정론(같은 입력 bytes → 같은 출력 bytes, per-environment) 필수. diffusion 금지 유지.**
- 디스크: 승인 하에 external/CelebAMask-HQ + facial_hair_annotations 삭제(2.7GB 확보,
  둘 다 재다운로드 가능한 공개 데이터셋, 스테이지4 학생모델은 이미 기각).
- big-lama traced TorchScript 205.7MB 다운로드, SHA256 `344c77bb…` `spike/lama_manifest.json`에
  고정(로드 시마다 재검증). 라이선스: 코드/가중치 Apache-2.0, 학습 데이터 Places2 —
  **상용화 전 법무 검토 blocking item**.

### Codex #8 교차검증 (2026-07-11): 하이브리드 설계 — 조건부 GO, 블로커 4

1. `0.06 + 0.04fw 일괄 팽창` 과마스크 위험 → 팽창 폐기/축소, erase_core와 context 분리,
   jaw 클립은 팽창 전+후 이중 적용. fringeRing 기반 재팽창은 v1 금지(Goodhart).
2. **quilt donor 선정이 원본 저주파(수염 얼룩 포함)에 매칭 — LaMa 채움 위에서는 어두운
   donor를 골라 모틀 재생산.** → target_low_l 파라미터 추가(LaMa low 기준 선정) + rehipass.
3. lip byte-identity는 필요조건일 뿐 — 인접부 입술 연장/치아/이중 fold 환각 가능.
   넓은 moat 금지(soul-patch 리콜 재손실), **좁은 lip polygon 홀 + lipLeak·newBrightStructure
   자 + 환각 시 abstain**.
4. 러너 결정론 미달 → freeze(optimize_numerics=False), manifest 없으면 로드 거부,
   interop 실패 hard-fail, use_deterministic_algorithms, OMP/MKL 고정, ABA·사이즈·마스크
   토폴로지·PNG bytes 테스트. **전부 반영, tests/test_lama_runner.py 14/14 green.**
- 손절 순서 재정렬: **oracle-mask kill test 최우선**(완벽 마스크에서도 추한지) —
  실패 시 CLIPSeg 배관·자 작업 전 즉시 NO-GO.
- U_b(조명 일관 상한)는 pixel clamp가 아니라 one-sided reject/abstain 자로 유지.
- 체크포인트 ⑤ 프로토콜: ORIG 상시 노출 + A/C 블라인드, **절대 허용/불가가 1차**, 선호는 2차.
  게이트는 새 사진 보기 전 동결.

### Oracle-mask kill test (2026-07-11): 생존 — 단 승자는 LaMa-as-is

pic1·pic3·psd01·psd04, REGION GT 기반 oracle 마스크(위쪽 확장 + 좁은 lip 홀 + 콧구멍 코어
제외 + jaw 클립), LaMa ≤512 추론(1.4~5.0s/장). 패널: ORIG | LAMA(그대로) | HYB(LaMa 저대역
+ 교정 donor 고대역).

- **LAMA 팔 생존**: 입 환각 0건(Codex 최대 베팅 미발화 — 좁은 lip 홀 유효), 턱 과밝음 0건,
  LaMa 자체 모틀 0건. pic1·psd01은 즉시 서비스 후보 수준(여드름 자국까지 자연 소거).
- **HYB 팔(donor 재주입)이 오히려 실패**: psd01·psd04 격자무늬 모틀(Hann 격자 잔존,
  psd04는 donor 침식 과다로 nDonors=1 — 단일 스탬프 반복). 죽인 건 LaMa가 아니라 우리 quilt.
- grain 비율(채움 고대역 RMS / clean 피부): pic1 0.54, pic3 0.55, psd01 0.66(**결손** —
  갤럭시식 왁스), psd04 **1.53(과잉** — 짙은 문맥을 이어 그린 잔털 질감 재생성 의심).
  → grainMatch 자는 **양방향** 필수.
- pic3 인중 양옆 잔존 = GT 리콜 빈틈(LaMa 실패 아님) — 프로덕션 존(CLIPSeg ∪ soft-high)에서
  재검증. psd01 인중 상단 잔존 = 콧구멍 코어 정책 제외(의도된 불가침).
- **psd04 턱선 하드 클립이 "위는 깨끗/바로 아래 수염 링" 경계 생성 → under_jaw_margin 실험:
  jaw+0.06fw(∩ roi — 목 불가침 유지)에서 링 소멸, 턱 실루엣·이중턱 음영 보존. 0.06 채택,
  0.10과 사실상 동일(roi가 캡).**

**v1 방향 확정(Codex #9 교차검증 예정)**: 마스크 내 단일 소스 = guarded LaMa as-is
(자체 저+고대역). donor 재주입은 기본 OFF — grainMatch 트립 시의 조건부 후속 과제
(결정론 스펙트럼 grain 또는 quilt 개선). 근거: 시각 관문에서 LaMa 자체 질감 > quilt,
Codex #8 Q3의 "erase core donor w=1" 권고는 현행 quilt 품질에서 성립하지 않음.

### Codex #9 (2026-07-11): guarded-LaMa-as-is v1 승인 + 프로덕션 블로커 2

- **판정: LaMa-as-is 메커니즘 GO** (Codex 스스로 #8의 donor-high 권고를 증거로 폐기),
  HYB(quilt 재주입) NO-GO 확정. 후속 re-grain 후보는 quilt가 아니라 **결정론적
  phase-scrambled spectral grain**(clean PSD 매칭, 입력 해시로 위상 고정).
- grain 자 재정의: raw RMS는 품질 순서를 역전시킴(시각 OK pic1 0.54 < 시각 waxy psd01
  0.66). → relative_grain_map 기반 G50(log-median)·G90, 참조는 마스크에서 0.03fw 이상
  떨어진 동일 높이대·L-매칭 clean. 초기 밴드 G50 [0.75,1.30]/hard [0.67,1.50],
  G90 ≤1.45/hard 1.60 — **컨트롤로만 동결**.
- newDark(Area/Energy/Coherence OR-판정), newBright, lipLeak 추가 요구 (환각 3종 분리).
- jaw+0.06 채택 동의하되 "∩roi = R5 보장" 반박(roi는 기하 프록시, 목 세그먼테이션 아님).
- edit_mask/model_mask 분리(합성은 edit만, 모델 홀은 +0.004fw), blanket 팽창 금지,
  strict hair 성분만 0.003fw 할로.
- 러너: OMP/MKL setdefault의 외래값 묵인 금지(강제 거부), deterministic flag는
  프로세스 전역이라 동시 호출 race → forward 직렬화. 동시호출·env 불일치 테스트 추가.
- 레이턴시: dev9는 512 고정 + 랩 6~7s 허용, 단계별 타이머 분리, 448은 A/B 동등 시에만.
- 동결 순서: 게이트 즉시 동결 → 합성 컨트롤 6종으로 자 숫자 동결 → dev9 단 한 번.
  **실패한 dev9를 보고 임계값을 바꾸지 않는다.**

### 프로덕션 파이프라인 v1 + dev9 원샷 (2026-07-11)

구현: spike/hybrid_recon.py (GT-free prepare_unlabeled: C1∪CLIPSeg 합집합 필드,
zoneEditThr 0.03 — 서빙 0.5는 GT 리콜 6.6%로 사용 불가(스윕: 0.06→70%, 0.03→83%,
0.01→91%); edit/model 마스크 분리; editable = roi ∪ (얼굴오벌∩코밑) — pic3 flank
잔존의 roi-밖 가설은 오탐(배경/헤어라인 오검출)으로 반증, 실제는 in-roi 증거 구멍 +
재생성 혼재). eval/hybrid_rulers.py + 합성 positive control 8종 green (94 passed).

dev9 원샷 결과: 9/9 실행(clean 사다리 후), 판정은 hard-fail 5 / abstain 4 — 그러나
**1:1 시각 판독과 게이트 판독이 역전**: pic1·pic2·psd04 결과는 눈으로 서비스급인데
게이트는 전멸 수준. 원인 규명:

1. **clean 캘리브레이션 붕괴**: 0.03 존 팽창 제외 후 pic1 clean 2.6k px →
   black-hat 임계값 2.0/5.5 (정상 15/19)로 폭락 → 정상 질감 전체가 "털" 판독.
   → 결정론적 제외 임계값 사다리(0.03→0.06→0.12→0.20→0.50, cleanPx 바닥
   max(1500, 10% roi), 사다리 레벨 기록) 도입. psd02 prepare 탈락도 이것으로 해소.
2. **ghost 게이트 오염**: cand_zone이 jaw 클립/보호를 모름 → 정책상 편집 불가
   구역(턱밑) 후보가 survival을 지배. → 마스크 내 후보만 게이트, fringe(편집 가능
   구역 내 미커버)/policy(정책 제외) 분리 기록.
3. 수정 후에도 잔존 문제: **무라벨 hairy 사진에서 clean 참조의 '품질'이 미보장**
   (사다리는 존재만 보장) — pic2 G90=11, pic1 ghost survival 0.91은 참조 붕괴 서명.
   실물 잔존(진짜)은 콧구멍 코어 정책 구역의 반점(pic2 인중)과 pic3 flank.

**결론: 메커니즘·마스크는 전진, 측정 참조(clean) 재설계가 다음 블로커.**
dev9 결과를 보고 임계값을 조정하는 것은 금지(Codex 규율)이므로, 참조 재설계는
Codex #10 교차검증 대상. 레이턴시: warm 2.4~7.3s/장 (512, M1).

### Sprint A: 커버리지-퍼스트 v3 (2026-07-11, 사용자 승인 + Codex #10 공동 설계)

- 방향 전환: "목이든 가장자리든 최대 커버, 디테일은 위에서". R5(턱 아래 금지) 은퇴 —
  geometry 누락은 이제 coverage failure. 계획은 Codex #10과 공동 설계(신규 자 공식,
  목 코리도, ReferenceBundle 레시피, 체크포인트 프로토콜 전부 반영).
- **워크플로 병렬 구축**: 4모듈(eval/reference_bundle.py 17t, eval/silhouette_ruler.py
  12t, eval/fill_color_ruler.py 9t, spike/mask_v3.py 15t) + API 리뷰(함정 8건: bool 마스크
  계약, 풀프레임 좌표, lab_cov ab 슬라이스, 2-패스 가먼트, 시드 독립성 등). 147 passed.
- LaMa 윈도 전략: 마스크 bbox+0.12fw 타이트 윈도(pic2 scale 1.0 회복).
- **오클루더 과발화 수정**: 짙은 수염·그늘 목 피부가 볼 앵커 대비 비피부 판정
  (occluderFrac 0.545, 마스크 96% 삭제) → 증거 픽셀 제외 후 가드(0.112로 정상화).
  Codex #10 원칙("skin likelihood는 수염 마스크 hard gate 금지") 재확인.
- **실루엣 붕괴 발견·완화 경로 확정**: 얼굴+목 단일 거대 홀 → LaMa가 턱 윤곽을 뭉갬
  (jawShift 0.022fw, corr 0.69 — 실루엣 자가 정확히 hard-fail). 거대 홀 순차 분할은
  턱 회청색 밴드(fillColor abstain이 포착). **최종 구조: 얼굴측은 v2 검증 레시피
  (jaw+0.06 분할선 위) 그대로 + 목은 z-증거 밀착 밴드만 별도 패스(청소된 얼굴을
  문맥으로)** — psd04에서 목수염 소거 성공 + 회색 밴드 소멸, grain 0.756 통과.
  남은 관찰: 턱 미묘한 회색기(fillColor abstain)·부드러운 실루엣(자 hard-fail) —
  자들이 시각 관찰과 일치하게 작동.
- **dev9 v3 원샷 — 수정 라운드 필요한 결함 2건 (모두 빌더가 사전 경고한 리스크)**:
  (A) 앵커 QC 과엄격: 실사진 4/9 (pic1·psd02·psd03·psd05) reference abstain.
  (B) 작은 얼굴(pic 계열, crop fw<350 위험 영역)에서 anchor-z 과발화 → 마스크 0.5로
  폭증, newDark 0.28~0.71, ghost survival ~1.0 — 측정이 아니라 참조 전이의 문제.
  규율 준수: dev9 결과로 임계값 조정하지 않음. 수정은 reference_bundle 계층에서,
  Codex #11 교차검증 대상.

### v3 수정 라운드 (2026-07-11 심야)

- **가먼트 열별 플로어**: 행-지속성 검출이 경사 네크라인(psd04 후디)에서 None →
  코리도가 옷감 위로, CLIPSeg+z 이중 시드가 어두운 네크라인에서 동시 발화해 옷감이
  편집 마스크에 포함(오클루더는 증거 픽셀 면제라 통과). garment_floor_cols(열별
  첫 교차 + 중앙값 필터)로 해결: psd04 gY 531 검출, newDark 0.048→0.008.
- **reference_bundle 수정 (전담 에이전트, 22 tests)**:
  (A) 앵커 abstain 4/9의 원인 = 고정 0.6 코히런스 게이트가 큰 얼굴(fw~650)의 깨끗한
  볼 중앙값(0.55~0.59)보다 낮았음 + 무조건 상위 15% Maha 컷 + 이마 합산 불가 사다리.
  → 스케일 연동 게이트(0.6→0.75 램프), 이상치만 트림(chi3 p96, 15% 캡), 검증된 이마
  보충 + level-4 low_conf 강등. 9/9 번들(전부 level 1).
  (B) 소형 얼굴 z 폭발 = fw≤376에서 3px 블랙햇이 앵커에서 전부 0 → MAD=0, 시그마가
  eps 바닥 → 크롭 텍스처 전부 z 수백만. + 검은 옷에서 명암 분모 바닥.
  → p95 기반 강건 시그마 + 앵커 최저 L −70 아래 무효화. pic 발화율 0.40~0.67 →
  0.01~0.10, 합성 가닥 재현율 98~100% 유지.
- **dev9 v3b (152 tests green)**: 9/9 실행, psd 계열 dark·ghost ≈ 0, 마스크 0.06~0.25
  정상 범위, 가먼트 플로어 전 사진 작동. 잔여 이슈:
  ① **실루엣 자 판별력 부재 (핵심)**: v2급(사용자 극찬)과 에어브러시(명백 변형)를
  같은 숫자로 hard-fail(corr 0.687 vs 0.691), 수염 묻힌 턱에선 identity도 abstain
  (supportFrac<0.30). "stable point"가 털 가장자리를 포함해 **수염 소거 자체를 윤곽
  변화로 측정**. 8/9 hard-fail의 실체 → 재설계 필요(Codex #11).
  ② pic1/pic4 grain 과잉(1.68~1.75, 흐린 원본 위 선명한 채움), pic2 이상치(검은
  재킷 지배 크롭에서 참조 왜곡: G50 0.163/dark 0.21 — 시각은 양호).
