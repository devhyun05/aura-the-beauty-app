# 텍스처 보존 지우개 스프린트 (2026-07-10)

계획: `~/.claude/plans/floofy-crafting-meerkat.md` (과제 C-2)
불변식: pytest(자 테스트 포함) / 입술 픽셀 diff 0 / 시각 관문 육안 기록 / 홀드아웃 원샷.

---

## Step 0 — 지표 구축 + 베이스라인 · **통과 (스펙 정정 포함)**

### 만든 것

`engine/texture.py` (신규) + `tests/test_texture_metrics.py` (자 테스트 11 passed)

- `grain_map` — 고주파 휘도의 국소 RMS (텍스처 에너지)
- `relative_grain_map` — grain ÷ 국소 평균 휘도 = **텍스처 대비**
- `coherence_map` — 구조 텐서 코히런스 (0=등방성, 1=한 방향)
- `waxiness_score` = 지대의 상대 grain ÷ **본인 깨끗한 피부**(이마·윗볼)의 상대 grain
- `streak_score` = 같은 비율을 코히런스로
- `chroma_delta_ab` = 지대의 (a,b) 색도가 본인 피부 색도에서 벗어난 거리 (L 제외 — 턱 그늘은 정상)

`eval/measure_waxiness.py` — 실제 합성물(사용자가 보는 그림)에서 측정, 시각 관문 이미지 동시 생성.

### 자 테스트가 즉시 잡은 버그

평평한 이미지에서 `None`이 나와야 하는데 **1.0**이 나왔다. `GaussianBlur`의 float32 반올림이 완전
평면에서도 grain ~1e-5를 남기고, 그 잡음이 **분자·분모에 똑같이** 들어가 확신에 찬 1.0으로 나눠 떨어진다.
유효성 문턱을 실측 기반(clean-skin grain 0.57~4.06)으로 `0.05`에 재설정.

### 지표 결함 두 개를 발견하고 수정

1. **밝기 교란.** 절대 grain은 밝기에 비례한다. 수염 지대는 정의상 이마보다 어두우므로 **원본 psd05가
   0.32**로 "이미 지워진 것처럼" 측정됐다. → `relative_grain_map`(국소 대비)로 교체. 민감도가
   강한 stage 기준 **11% → 46%** 로 개선. 자 테스트로 고정(`test_waxiness_is_not_fooled_by_a_darker_zone`).
2. **둔감.** 보정 강도는 마스크 값에 비례하는데, 관대한 지대(`union>0.3`)로 재면 약하게 보정된 픽셀이
   중앙값을 지배한다. → **core 지대(`union>0.5`)** 로 변경.

### 결정적 발견 ① — 지우개는 **약한 마스크에선 아무것도 안 한다**

C1 마스크(hard 평균 0.01, shadow 0.02~0.06)로 재면 원본↔strong 차이가 거의 없다(waxiness 1.932→1.825,
ΔE 10.64→10.63). 육안으로도 psd04 네 패널이 사실상 동일.

> **뭉갬은 잠복 상태다.** 넓고 확신 있는 마스크(=우리가 채택한 CLIPSeg raw@0.06)를 먹이는 순간 터진다.
> 그래서 베이스라인은 **출시할 마스크**로 측정한다(`--masks clipseg`). 약한 C1로 재면 안 망가진 지우개를
> "고치고" 나중에 진짜로 터뜨리게 된다.

CLIPSeg 마스크로 재측정하니 psd04 waxiness **1.938 → 0.887**(strong), 육안으로도 콧수염·턱이 평평해짐.

### 결정적 발견 ② — 가설을 세우고 **스스로 기각**

두 갈래를 분리 측정 (strong 기준, 9장):

| 갈래 | waxiness 변화 |
|---|---|
| `hair` (고주파 감쇠) | **9/9 전부 감소** — 예상대로 빼기만 함 |
| `shadow` (Telea-on-low) | **6/9 증가** (pic2 8.91→9.68) |

계획서는 이를 "방향성 색 스미어"로 규정했다. `coherence_map`을 만들어 검증한 결과:

> **Δstreak = −0.000 (9장 평균), 상승은 3/9.** 방향성이 전혀 아니다. **가설 기각.**

진짜 원인을 추적했다. `beard_shadow_corrector.py:48`에서 low 레이어를 **uint8로 양자화**해
`cv2.inpaint`에 넣고 다시 float으로 받는다. 마스크 **밖**(Telea가 입력을 보존하는 영역)의 잔차를 재니:

| | 값 |
|---|---|
| 양자화 잡음 RMS | **0.577** (= 1/√3, 균일 양자화 오차의 이론값) |
| low 레이어 자신의 고주파 | 0.29 ~ 0.35 |
| **주입비** | **1.7 ~ 2.0×** |

> **uint8 왕복이 low 레이어 고유 결의 2배에 달하는 등방성 잡음을 주입한다.**
> 이것이 "Telea가 grain을 올린다"의 실제 메커니즘이며, 계획서의 "방향성 스미어" 서술은 틀렸다.

`cv2.inpaint`는 8비트만 받으므로 이 왕복은 피할 수 없다 — **단, 원본 crop(이미 uint8)에 직접 적용하면
추가 양자화가 0**이다. 즉 Step 1의 A(가는 털 inpaint를 full-res crop에 적용)와 Step 2(low-layer inpaint
제거)가 이 버그를 **구조적으로 함께 제거**한다.

### 베이스라인 (dev 9장, CLIPSeg 마스크, core>0.5)

| 지표 | orig | mild | medium | strong |
|---|---|---|---|---|
| waxiness (평균) | 3.632 | 3.514 | 3.405 | 3.206 |
| streak (평균) | 1.635 | 1.630 | 1.618 | 1.606 |
| chroma ΔE (평균) | 10.329 | 10.288 | 10.262 | 10.250 |

사진별 strong waxiness: pic1 5.691 / pic2 9.208 / pic3 2.888 / pic4 4.329 / psd01 1.481 /
psd02 0.945 / psd03 2.771 / psd04 **0.887** / psd05 **0.653**

처리시간: segment 1.744s/장(CLIPSeg 포함), correct 0.263s/stage (최대 0.709s).

### 게이트 판정 — **통과, 단 스펙을 정정**

계획서의 게이트는 "원본≈1.0, strong<medium<mild"였다. 둘 다 문자 그대로는 미달이다:

- 원본 waxiness는 0.71~8.91로 퍼진다. **이 스펙이 틀렸다** — 털은 텍스처이므로 원본은 1.0보다 **높은 게
  정상**이고, 지표는 애초에 양방향으로 설계됐다(>1 털 잔존 / <1 피부 뭉갬 / ≈1 목표).
- 단조감소는 6/9. 깨진 3장(pic2, psd01, psd03)의 원인은 **지표 결함이 아니라 위의 양자화 잡음 주입**이다.
  즉 지표는 진실을 말하고 있었다.

지표는 (a) hair 갈래의 평탄화를 9/9 포착, (b) shadow 갈래의 잡음 주입을 포착, (c) 방향성 가설을 반증할
만큼 예리하다. **판별력 확인 → 통과.** 스펙 정정 사유를 여기에 기록하고 진행한다(무인 규약).

**남은 한계(정직하게)**: waxiness는 진짜 피부 결과 아티팩트를 구분하지 못한다. 반드시 `streak`·시각 관문과
함께 읽어야 한다. Step 3의 re-grain은 **등방성**이어야 하며 그건 `streak`으로만 검증된다.

### 시각 관문 (육안 확인 기록)

- `outputs/waxiness/step0_baseline/wax_psd04.jpg` — strong에서 콧수염·턱 중앙이 평평해지고 광택이 도는
  반면, 턱선의 굵은 털은 그대로 남는다. 지우개가 **약한 곳을 지우고 강한 곳을 못 지우는** 역설.
- `outputs/waxiness/step0_baseline/wax_psd05.jpg` — 턱 잔털이 눈에 띄게 줄지만 원본 대비 피부가 미끄럽다.
- (C1 마스크 버전 `step0_clipseg` 이전 실행) psd04 네 패널이 사실상 동일 — 약한 마스크에서 지우개 무력.

### 커밋

- (이 커밋) Step 0: texture.py + 자 테스트 11 + measure_waxiness + 베이스라인

---

## Step 1 — CorrectionContext + A(가는 털 inpaint) · **채택**

### 계획 수정 (증거 기반)

계획서의 A는 `masks.hard`에서 형태학으로 얇은 구조를 뽑으라고 했다. **틀렸다.**
`hard`는 털을 담고 있지 않다 — (a) `segment_beard`가 strand_cov>0.28에서 통짜 덩어리로 solidify하고
(`beard_segmentation.py:302-309`), (b) 출시 구성은 `hard = max(c1.hard, clipseg_region*0.9)`로 **넓은
지대**로 덮어쓴다. 덩어리를 형태학으로 얇게 하면 **가장자리 껍질**이 나오지 털이 아니다.

→ 대신 **crop 휘도에서 black-hat으로 직접** 얇고 어두운 구조를 찾는다(C1이 쓰는 연산자, 커널 fw×0.010).
마스크의 출처·의미와 무관하게 동작한다. 지대(`union>0.2`) 안으로 제한하고 보호막 제외.
strand 커버리지 >35%면 "털이 아니라 덩어리"로 보고 A를 건너뛴다(stats에 기록).

### 구현

- `CorrectionContext`(신설): black-hat 1회 + Telea 2회만 보유. 주파수 분리는 **stage별로 유지** —
  blur는 선형이나 strand 가중치가 비선형이라 hoist하면 결과가 달라진다.
- A는 **원본 uint8 crop**에 Telea(radius fw/150) → 양자화 주입 0.
- low = blur(**strand 제거된** base) → 털의 어둠이 low에 안 섞인다(스미어 #3 제거).
- `_stubble_inpaint` 삭제. `stubble_inpaint` 인자는 하위호환 유지(strand_strength 기본값 선택용).
- `pipeline.py`: context를 사진당 1회 빌드해 사다리 전체에 관통. configs에 `strand_strength` 0.35/0.65/0.90.

### 게이트 — 전부 통과

| 항목 | 기준 | 실측 |
|---|---|---|
| 기존 테스트 무수정 통과 | 전부 | **51 passed** |
| context 시간 | ≤1.5s | **0.262s/장** (최대 0.704) |
| stage 시간 | ≤0.3s | **0.022s/stage** (기존 0.263 → **12배**) |
| 입술 불변식 | diff 0 | **9장 전부 0** |
| 스미어 육안 감소 | 필수 | **아래 참조** |

### 시각 관문 (육안 기록) — `outputs/waxiness/compare_step0_step1/cmp_psd04.jpg`

STEP0(before) 확대: **턱 아래에 직선 경계를 가진 납작하고 어두운 직사각형 블록**이 있다.
`union>0.22`를 이진화해 팽창시킨 inpaint 마스크의 경계가 그대로 이미지에 찍힌 것 — 이것이 사용자가
말한 "피부를 뭉갠다"의 정체다. 블록 내부는 결이 사라지고 광택이 돈다.

STEP1(after): **그 블록과 직선 경계가 완전히 사라졌다.** 피부 결과 잔털이 경계 없이 이어진다.

수치도 일치: psd04 strong waxiness **0.887 → 1.041** (과평탄화에서 목표 1.0으로 복귀),
psd02 0.945→0.922, psd05 0.653→0.675. streak 변화 없음(1.606→1.601) — 새 아티팩트 없음.

pic 세트는 waxiness가 소폭 상승(털이 더 남음): `_stubble_inpaint`의 넓은 점 제거(`hard>0.45`)를
훨씬 보수적인 strand 마스크(ROI의 0.2~1.2%)로 교체했기 때문. **이 스프린트의 목표는 결 보존**이고
"덜 지움"은 Step 2/3 이후 강도 재조정으로 다룬다.

### 남은 것

`low_inpainted`는 아직 uint8 왕복 Telea다(양자화 주입 0.577 RMS 잔존). Step 2에서 통째로 제거된다.

---

## Step 2 — B: Telea-on-low 삭제, 국소 피부색으로 크로마 이동 · **채택**

### 구현

- `beard_segmentation.local_skin_reference()` 공개 래퍼 신설 — shadow 채널이 "이 픽셀이 피부에서
  얼마나 먼가"를 재는 바로 그 목표장을 corrector가 "어디로 옮길까"에 재사용. 정의가 하나로 통일된다.
- `low_inpainted`(Telea) 삭제 → `local_ref_lab` 목표장.
  - **크로마(a, b)는 `shadow_w`만큼 완전히** 이동 (수염의 시각적 서명 = 푸른 잿빛 cast)
  - **L은 `shadow_w × luma_shift_frac(0.75)`만** 이동 — 턱은 원래 그늘져 있고, L을 끝까지 당기면
    얼굴의 입체 모델링이 지워져 붙인 자국처럼 보인다.
- Lab 변환은 **float 경로 전용**(`_bgr_to_lab`/`_lab_to_bgr`). uint8을 거치면 Step 0에서 특정한
  0.577 RMS 양자화 주입이 되살아난다.
- configs에 `luma_shift_frac: 0.75`.

### 게이트 — 전부 통과

| 항목 | 기준 | 실측 |
|---|---|---|
| `test_correction_moves_shadow_toward_skin` | **무수정** 통과 | ✅ (51 passed) |
| chroma ΔE 개선 | 필수 | **10.329 → 8.080** (strong), **9/9 사진 전부** |
| 턱그늘 육안 보존 | 필수 | ✅ 아래 |
| 입술 불변식 | diff 0 | **9장 전부 0** |
| 새 방향성 아티팩트 없음 | — | streak 1.639 ≈ **원본 1.635** |

### 지표 변화 (strong, dev 9장 평균)

| | baseline(Step0) | Step 1 | **Step 2** |
|---|---|---|---|
| waxiness | 3.206 | 3.240 | **2.802** (목표 1.0) |
| chroma ΔE | 10.250 | 10.217 | **8.080** |
| 단조감소 | 6/9 | 6/9 | **7/9** |
| context 시간 | — | 0.262s | **0.104s** |
| stage 시간 | 0.263s | 0.022s | 0.024s |

**Step 1까지 ΔE는 10.33→10.22로 꿈쩍도 안 했다.** Telea-on-low는 색을 고치는 게 아니라 뭉개기만 했던 것.
Step 2에서 처음으로 색 얼룩이 실제로 교정된다(−2.25). psd03 19.03→13.36, pic2 18.05→10.51.

### 시각 관문 (육안 기록)

- `outputs/waxiness/compare_step1_step2/cmp_psd04.jpg` — STEP2에서 콧수염·턱의 푸른 얼룩이 옅어지고
  **턱→목으로 이어지는 그늘은 그대로 남는다**(얼굴형 왜곡 없음). 피부 모공 결이 살아 있다.
- `.../cmp_psd03.jpg` — 인중·턱의 cast가 확연히 감소(ΔE 19.03→13.36), 잔털은 유지, 결 보존.
- `.../cmp_psd05.jpg` — 변화 미미(원래 ΔE 5.96으로 낮음). 이상 없음.

### 남은 문제 (정직하게)

- waxiness가 여전히 2.80(strong)으로 1.0에서 멀다. 두 요인이 섞여 있다:
  (a) **털이 아직 많이 남음** — pic 세트 4.4~6.5. CLIPSeg raw heat가 약해 마스크가 옅다.
  (b) **결 손실** — psd04 1.04, psd05 0.71로 이미 1.0 근처/이하. 여기가 Step 3(re-grain)의 대상.
  즉 두 세트가 정반대 방향으로 1.0을 벗어난다. **평균 하나로 판단하면 안 된다.**
- `high_out = high * (1 - hair_w)`(최대 85% 감쇠)는 그대로다. 되살리는 단계가 없다 = Step 3.

---

## 다음 (사람 판단 대기)

Step 3 — C: re-grain. 본인 피부에서 채취한 결을 에너지 부족분만큼 등방성으로 재주입.
**단, 계획대로 여기서 멈추고 사람이 before/after를 보고 Step 3 필요 여부를 결정한다.**
Step 0~2만으로 충분하면 `regrain:false`로 출시하고 위험한 결 합성을 아예 생략할 수 있다.

---

## Step 2.5 — 3중 희석 제거 + 참조장 색 클램프 (2026-07-10 오후, 사용자 검수 후)

### 계기 (사용자 관찰 2건 → probe로 정량 확인)

사용자: "뭉개지진 않았는데 아예 안 지워진 것처럼 보여" + "턱에 원래 없던 검은 그림자".
probe(psd04 strong, zone core 24,665px)로 두 관찰이 **같은 원인**임을 확인:

- **회색 띠 = 점묘 평균 노출.** 원본 zone 평균 gray 118.6, 눈이 읽는 '털 사이 피부'는 138.2.
  털 대비를 지우면 평균만 남는다. 평균은 +4 밝아졌는데 지각적으로는 어두워 보임.
- **밝힘·지움이 3중 곱으로 희석.** 확신도(마스크값)가 corrector 가중치와 합성 알파에 **두 번**
  곱해짐 + luma 0.75 부분 이동. 합성 알파 zone core 평균 0.571 → 필요한 회복 19.6L 중 4.0L만 반영.
  털 대비 순감 ~30%. 턱선 가장자리(heat 감쇠, 알파 p10 0.33)가 가장 약하게 지워짐.
- **단위 함정:** Lab L과 gray가 같은 피부에서 ~8 어긋남. 게이트/진단은 gray로 통일해 측정.

### 변경 (커밋 1개)

1. `confidence_ramp` 신설 (`beard_segmentation.py`, smoothstep 0.15→0.60): 확신도는 **어디를**
   지울지 정하고, 세기는 stage가 정한다. 0.15 미만 반점은 0, 0.60 이상은 1로 포화.
2. `derive_soft_blend`: union을 램프로 포화 후 페더링, `max(blur, core)`로 얇은 띠 중심부 처짐 방지.
   합성 알파는 공간 이음새 전담 — 확신도 이중 곱 제거. (psd04 core 알파 0.571 → 0.994)
3. `correct_crop`: shadow_w/hair_w도 램프 confidence 사용.
4. **점묘 빚 상환**: 감쇠된 high층의 국소 평균(blur)을 zone confidence만큼 제거 — 띠의 평균 어둠은
   (이동된) low층 책임으로 넘기고 high층은 순수 결만 나른다.
5. `luma_shift_frac` 0.75 → 1.0 (전 config): 국소 참조장이 조명 그라디언트를 이미 보존하므로
   전량 이동해도 턱 그늘은 유지됨(시각 관문으로 확인).
6. `_STRAND_PERCENTILE` 94 → 90: 가는털 인페인트 그물 확대.
7. **참조장 색 클램프** (`_MAX_REF_AB_DEV = 6.0`): psd05에서 국소 참조장의 chroma가 전역 피부
   모델에서 지대 전체적으로 11~13 ab-unit 이탈(정상 사진 ≤6, psd04 max 5.8) → full-strength가
   이를 주황 얼룩으로 인쇄(ΔE 5.96→10.37 회귀). 참조장의 밝기(조명)는 신뢰, 색은 전역 모델
   반경 6으로 제한. 클램프 후 psd05 strong ΔE 6.56 (원본 대비 +0.6, 잡음 하한 수준).

### 결과 (dev 9장, --masks clipseg)

| 지표 (strong) | Step 2 | Step 2.5 |
|---|---|---|
| psd04 zone 최종 gray | 122.6 (주변 대비 −15.6, 띠) | 130.6 (**주변 ring 125.6보다 +5**) |
| chromaDeltaAb 평균 | 8.08 | **3.94** (9장 중 8장 개선, psd05만 +0.6) |
| 합성 알파 (psd04 core) | 0.571 | 0.994 |
| waxiness strong | 2.80 평균 (털 잔존이 지배) | psd02 0.27 / psd04 0.20 / psd05 0.43 — **이제 결 손실이 지배** |

- pytest 53개 전부 green (새 자 테스트 2: `confidence_ramp` 의미, 합성 알파 포화;
  새 불변식 1: `test_zone_mean_darkness_is_not_printed_into_the_result`). 기존 불변식 무수정.
- 입술 불변식: 9장 × 3 stage, protect 영역 픽셀 diff 0.
- 타이밍: context 0.103s/장, correct 0.041s/stage — 예산 안.

### 시각 관문 (outputs/waxiness/step25b_refclamp/, 직접 관찰 기록)

- `wax_psd04.jpg` — MILD 자연스럽고 수염 옅어짐. MEDIUM 수염 거의 소멸, 검은 띠 없음, 톤 자연.
  STRONG 수염 완전 제거되나 지대가 **결 없는 매트한 면**이라 패치 티가 남음(턱 아래 경계).
- `wax_psd03.jpg` — MILD 양호. MEDIUM/STRONG 인중·턱이 평평한 단색 면 + 볼 대각 경계 노출.
- `wax_psd05.jpg` — 주황 얼룩(클램프 전 step25_saturate에 존재) 대부분 소멸. STRONG 인중
  양끝에 옅은 매트 패치.
- `wax_pic2.jpg`(클램프 전) — 굵은 털이 많은 사진은 톤만 바뀌고 털이 남아 어색. pic 세트는
  털 대비 자체가 커서 85% 감쇠로도 잔존 — 마스크가 아니라 감쇠 한계.

### 판정

**채택.** 사용자가 지적한 두 증상(안 지워짐, 검은 그림자)은 해소. 남은 결함은 단일 종류로
수렴: **지운 자리에 결이 없다** (waxiness 0.2~0.8, 매트 패치·경계 티). 이것이 Step 3
re-grain의 정확한 입력 상태다 — 계획의 예측("지움이 세지면 왁스가 실제로 생긴다")대로.

---

## Step 3 — re-grain: 본인 피부 결 재주입 (2026-07-10 저녁)

### 구현

- `texture.build_grain_field`: 본인의 깨끗한 피부에서 32px 결 조각을 떠서(고역통과·단위 RMS 정규화)
  지터 반겹침 격자 + Hann 윈도우 + 시드 고정 회전/반전으로 퀼팅 → 단위 RMS 결 필드. 전 과정 시드
  고정 = 결정론(테스트로 강제).
- 주입은 **에너지 기준**(계획 F5): rms_add = √(max(target² − current², 0)). 결이 남은 곳은 자체
  제한, 평탄한 곳만 채움. target = 깨끗한 피부의 결 대비 × 국소 밝기(그늘에선 조용한 결).
  휘도에만, conf_zone 게이트로 지대 안에만.
- **F4 리스크 적중**: 이마·윗볼 기준 패치는 lower-face 크롭 밖 → `skin_ref_mask`가 실사진에서
  0px, 결 뱅크 생성 실패로 재주입이 조용히 무효(1차 측정에서 수치 무변화로 발각). 계획된 폴백
  구현: 크롭 안에서 탐지기가 수염 아님이라 한 피부(union<0.10, 경계 9px 침식)를 채취원으로.
- 자 테스트 3종: 결정론(두 번 실행 byte 동일), 지대 밖 무변화, 평탄화된 지대의 결 에너지 회복
  (1.3배 이상, 과주입 1.6배 미만). 55개 전부 green.

### 자의 기준점 발견 (중요)

주입이 목표에 정확히 도달했는데도 waxiness가 0.46~0.70에 머무는 사진들이 있었다. 원인은 코드가
아니라 **자의 기준점**: waxiness는 이마 결을 1.0으로 삼는데, 이마 결은 턱 주변 피부의 결보다
psd02 2.3배 / psd05 2.3배 / psd04 1.3배 굵다(측정). 눈이 지대를 비교하는 대상은 이마가 아니라
**바로 옆 피부**다. 이마 기준으로 1.0을 채우면 턱만 모래처럼 거칠어진다(지각적으로 오답).
→ 기존 자는 무수정 유지, 보조 자 `waxinessLocal`(기준점 = 탐지기가 수염 아님이라 한 인접 피부)
을 추가해 병기.

### 결과 (dev 9장, strong)

| | 이마 기준 waxiness | 인접 피부 기준 waxinessLocal |
|---|---|---|
| psd02 | 0.46 | **1.02** |
| psd04 | 0.70 | **0.90** |
| psd05 | 0.52 | **1.18** |
| psd03 | 0.87 | 1.27 |
| 게이트(≥0.7) | 6/9 (psd04 0.696은 잡음 하한 내) | **9/9** (최저 0.895) |

- streak(방향성): 평균 1.650(주입 전 1.688) — 주입 결이 등방성임을 확인, 악화 없음.
- 입술 불변식·기존 불변식 무수정 green. 타이밍: context 0.138s/장, correct 0.046s/stage.

### 시각 관문 (outputs/waxiness/step3b_regrain_fallback/, step3c_localanchor/, 직접 관찰)

- `wax_psd04.jpg` — MEDIUM(0.79)이 매우 자연스러움: 수염 소멸, 결 살아있음, 띠 없음. STRONG도
  결 회복, 다만 턱 아래 경계가 아직 약간 보임.
- `wax_psd02.jpg` — 전 stage 자연스러움. STRONG에서 턱 모서리 그늘을 밝힌 옅은 자국.
- `wax_psd05.jpg` — 턱 자연. 인중 양끝 옅은 패치와 좌측 크롭 경계 이음새 잔존.

### 판정

**채택** (근거: 인접 피부 기준 9/9 + 시각 관문에서 매트 패치 소멸 + 등방성 무악화 + 결정론).
이마 기준 게이트는 6/9로 미달이나, 미달 사진 전부가 "이마가 유난히 거친" 사진이며 인접 피부
기준으로는 1.0 안팎 — 기준점 편향으로 기록하고 자는 병기 체제로 유지.

### 남은 결함 (다음 작업, 홀드아웃 전에)

1. **턱선 이음새**: strong에서 지대 경계가 턱 그늘/실루엣을 가로지르는 곳에 옅은 경계 노출
   (psd02 턱 모서리, psd03 볼 대각, psd05 크롭 좌측 경계).
2. **pic 세트 굵은 털 잔존**(waxiness 4~6): 85% 감쇠 한도(MAX_HIGH_ATTENUATION 계약)의 한계.
   가는털 인페인트 확대 또는 감쇠 한도 재론 필요.
홀드아웃(원샷)은 위 2건 처리 후 최종 설정으로만 소진한다.

---

## Step 4 — 이음새 제거: 안쪽 램프 alpha + CLIPSeg 스무딩 + 톤 단차 자 (2026-07-10 밤, 사용자: "medium이 낫다, 이음새는 둘 다 거슬린다")

### 진단 (측정 → 가드 사각지대 발견)

- 기존 `seam_score` 가드(Sobel gradient 비율)는 medium/strong 전 장 **통과(~1.0, 임계 1.35)**.
  그런데 눈엔 이음새가 보임. 원인: 사용자가 보는 이음새는 **매끄러운 저주파 톤 단차**인데
  Sobel은 고주파만 봄 → 가드가 못 잡는 종류. (규율 1: 자에도 테스트 → 새 자 필요)
- `probe_seam` 시각화로 두 원인 확정: ① 턱선을 따라 보정이 밝힌 띠가 손 안 댄 목과 만나는 톤 단차,
  ② CLIPSeg 저해상도(352px) heat를 크롭으로 확대할 때 생기는 ~30px 격자(alpha 포화가 드러냄).

### 변경

1. `derive_soft_blend`: 바깥 Gaussian → **경계 안쪽 거리 램프**. 기존 alpha는 경계에 0.5가 놓여
   보정이 경계에서 절반 걸림(=단차). 거리 변환으로 경계에서 0, 안쪽 `feather_ratio×fw`에서 1로
   smoothstep → 경계에 단차 없음. 램프가 heat 값이 아니라 거리에만 의존하므로 **블록도 내부로
   인쇄 안 됨**. (제품 경로 pipeline.py도 이 함수를 써서 함께 수혜)
2. `clipseg_masks`(출시 예정 마스크): heat를 임계 전에 Gaussian(σ=fw/45)으로 스무딩 → 격자 소멸.
3. `texture.tonal_seam_step`(신규 자, report-only): 편집 발자국(result−original)의 **경계 링을
   따라 평균**(등방성 grain은 상쇄, coherent 단차만 생존) → 경계 안/밖 좁은 밴드의 톤 점프(회색조).
   공간 블러를 안 써서 step(수 px)과 ramp(수십 px)을 구분. 자 테스트 2종(하드 단차 감지, 램프 통과).

### 결과 (A/B: 같은 보정, alpha만 교체, medium)

| 사진 | 톤 단차 OLD→NEW | 감소 |
|---|---|---|
| psd02 | 20.8 → 15.6 | −25% |
| psd03 | 47.0 → 17.2 | **−64%** |
| psd05 | 22.0 → 10.1 | −54% |

### 시각 관문 (outputs/seam_ab/, 직접 관찰)

- `ab_psd03_medium.jpg` — OLD은 하관 전체가 밝은 타원 패치 + 뚜렷한 경계(가장 심함). NEW은 경계
  소멸, 결 연속, 자연스러움. **이 스프린트에서 가장 큰 육안 개선.**
- `ab_psd05_medium.jpg` — OLD 좌측 턱의 밝은 얼룩·경계가 NEW에서 melt.
- `ab_psd02_medium.jpg` — OLD 턱 모서리 경계가 NEW에서 완화(하관이 목보다 약간 밝은 잔여는 남음).

### 판정

**채택.** 톤 단차 자 신설로 가드 사각지대를 메웠고(규율 1·2), A/B에서 3장 전부 수치·육안 동시
감소. 입술 불변식 9×3 diff 0, 56 테스트 green. medium이 사용자 선택 stage이며 그 이음새가 목표였음.

### 남은 한계 (기록)

- pic 세트(성긴 긴 털): waxiness 4~6로 털이 남음. 85% 감쇠 한도(제모 질감 계약)의 구조적 한계 —
  판정(detection)이 아니라 지우개 감쇠 상한 문제. 별도 논의 대상, 이번 이음새 작업 범위 밖.
- 톤 단차 자는 report-only(GuardCheckId enum 고정). 배선은 Step 5 이후 별도.

---

## Step 5 — 홀드아웃 원샷 (2026-07-10 밤). 단 한 번 소진, 튜닝 금지.

동결 설정: stage=medium(사용자 선택), 마스크=채택 CLIPSeg raw-sigmoid(thr 0.06)+C1 strands,
엔진=Step 2.5/3/4 커밋 기본값. 게이트는 **보기 전에** `run_holdout_eraser.py`에 사전 등록.

### 결과 (hold1~3, medium)

| id | orig ΔE | waxLocal | chroma ΔE | seam | guard |
|---|---|---|---|---|---|
| hold1 | 10.66 | 1.23 | 6.52 | 8.7 | pass |
| hold2 | 19.58 | 1.98 | 13.41 | 11.7 | pass |
| hold3 | 11.23 | 1.13 | 6.27 | 4.6 | pass |

- **G1** waxLocal∈[0.7,1.6]: **2/3** (≥2 필요) PASS — hold2만 1.98(밀집 수염 잔존).
- **G2** chroma<원본: **3/3** PASS. **G3** seam≤18: **3/3** PASS. **G4** 가드 통과: **3/3** PASS.
- **G5** 시각(outputs/waxiness/holdout_oneshot/, 직접 관찰):
  - hold1 — medium 자연, 콧수염·턱 잔털 옅어짐, 이음새·매트 없음, 입술 유지.
  - hold3 — medium 자연, 턱 수염 확연 감소, 결 보존, 이음새 없음.
  - hold2(최다 털) — 콧수염 일부 감소하나 잔존(waxLocal 1.98). **인공 결함(이음새·매트·입술)은
    전무** — 기록된 "굵은/밀집 털 85% 감쇠 상한" 한계이지 회귀 아님.

### 판정: 채택 (일반화 확인)

수치 4/4 + 시각 3/3(결함 기준). 홀드아웃은 소진됨 — 이후 이 3장으로 어떤 임계도 조정하지 않는다.
남은 유일한 미달(hold2 완전 제거 실패)은 dev pic 세트와 동일한 밀집-털 감쇠 상한 문제로, 별도 과제.

## 스프린트 총괄 (Step 0~5, 커밋)

fb5c420 자 구축 → e38111c 가는털 context → 6cd8393 크로마 이동 → 206ed1c 3중 희석 제거 →
be1822d 결 재주입 → (Step4) 안쪽 램프 이음새 제거 → (Step5) 홀드아웃 원샷 채택.
결과: 검은 띠·주황 얼룩·매트 패치·이음새 4대 결함 해소, 색 얼룩 ΔE 10.3→3.9(dev strong),
이음새 psd03 −64%, 홀드아웃 일반화 확인. 미해결: 밀집/굵은 털 완전 제거(감쇠 상한).

---

## 2026-07-11 후속: 유령 자 + 오라클 대체 스파이크 (커밋 cc46a01)

배경: 사용자가 "감쇠" 결과를 기각("아예 안 지워진 것처럼 보임") → 유령 자(GhostResidual,
black-hat 2스케일) 구축 → 기준선: medium의 최암부 털 생존율 r=1.00 (dev 8/9장). 원인은
점수 버그가 아니라 감쇠 방식의 상한 + 유효 지지 부족(Codex 검증). 포토샵 레퍼런스 2건
(사용자 제공)과 수렴: 감쇠 → **어두운 픽셀 키잉 후 대체**로 방향 전환, 반증 스파이크 실행.

### 스파이크 결과 (pic2·pic4·psd03·psd05, 승인된 p95 광역 키)

- 유령 게이트: B/C가 4장 중 3장 통과 (r 1.00→0.00~0.05). pic4(흐린 사진)만 0.375 부분 실패.
- 립코너 컨트롤 표류 0.0 (스코프 밖 바이트 동일), 턱 그늘 변화 ≤1.5%.
- **블라인드 판정(X/Y/Z, 매핑 봉인)**: 사용자 Y 전승 — Y=B(pic2·psd03), Y=C(pic4·psd05).
  A(현 medium)는 한 번도 선택 안 됨. → 대체>감쇠 방향은 블라인드로 검증됨.
- **그러나 품질 기각**: "수염을 뭉개기만 했지 색이 퍼져 남음. 지우개로 문지른 느낌.
  전부 사라진 것도 아님. 서비스 불가."

### 잔존 얼룩 프로브 (기각 원인 정량)

재건 목표 S(강건 2차 + NC 잔차) 대비 B의 저주파 잔존율:

| 사진 | 얼룩 px (지대 대비) | 필요 밝힘 L Q50/Q90 | B 잔존율 |
|---|---|---|---|
| pic2 | 5,547 (51%) | 18.2 / 52.4 | **1.00** |
| psd03 | 37,103 (65%) | 22.0 / 60.5 | **1.10** |
| psd05 | 22,381 (40%) | 6.3 / 12.6 | **0.87** |
| pic4 | 2,488 (26%) | 3.9 / 7.0 | **0.96** |

- 기각 3요인 코드 확정: ① B는 저주파 L 무편집(compose(lab_low[...,0])), C 리프트 캡
  7~12L vs 필요 최대 60L → "색 퍼짐" 방치. ② 키가 지대의 5~16%만 커버 → 못 지운 털.
  ③ 키 페더 안에서만 고주파 교체 → 어두운 저주파 위 밝은 조각 = 모틀링("문지른 느낌").
- 유령 자는 black-hat 기반이라 저주파 얼룩에 맹목 → 저주파 잔존 자(lowResidual) 신설 필요.

### 다음 방향 (Codex gpt-5.6-sol xhigh 교차검증 진행)

털별 키잉 → **지대 전체 2밴드 재건**: 저주파 L·ab를 외곽 클린 링에서 보간(증거 가중,
턱 그늘 보존), 고주파는 초과 증거 연속 가중으로 도너 결 교체. 포토샵 레퍼런스와 동일 구조.

### Codex 5차 교차검증 판정 (gpt-5.6-sol xhigh, 2026-07-11 수신)

**총판정: 조건부 GO** — "zone-wide **target reconstruction** GO / zone-wide **unconditional
replacement** NO-GO. S confidence·beard evidence·anatomy preservation·saturated high core·
two-sided low ruler를 갖춘 guarded reconstruction으로만 진행."

- 진단 3건(저주파 방치·키 과소커버·모틀링) 코드로 확인. psd03 잔존율 1.10의 원인
  (Lab 왕복 비선형 누화)까지 규명.
- **신규 버그**: 위쪽 확장 키가 합성 직전 strict zone 7×7 dilation(`allowed`)에 절단 —
  0.03fw 확장 실질 무효, keyPx 과대 보고, 확장 영역 털은 편집·채점 모두 누락.
- 멘토라비알 폴드: 외곽 링만으로 내부 홈 복원은 식별 불가능 → landmark 구조 밴드로
  macro 음영(σ≈0.08–0.12fw) 보존, hair-scale만 보정. 이거 없으면 "밝은 에어브러시 패치" 기각 예고.
- 캡 재설계: 고정 7/12L 폐기, 완전 uncapped 금지, 사진별 Q97/Q99 winsorize + 저신뢰 w 축소
  + S 이상 시 명시적 abstain. L 보정은 delta에만 (D=max(S−O,0)).
- quilt 자체가 "비늘·도장" 무늬의 공범: 90° 회전 제거, 동일 y-band donor, SSD 결정론 선택.
- lowResidual 자 게이밍 6종 지적 → two-sided(잔존+overLift)+sConfidence+structureBand
  자 스위트로 확장, 최종 결과 재저역통과 측정, N/A는 fail.
- pic4: OOD 선언 전 원인 계측 먼저; 최후엔 시뮬레이션 거부 게이트.
- 순서: 증거 커밋 → 자 동결 → 키 버그 수정 → 고주파/quilt → 가드된 재건 → 4장 진단 →
  dev 9장 → 블라인드 ③("서비스 허용" 기준) → 통과 시에만 S1a 배선 후 이식.

프로브 요약: `probe_low_residual_20260711.json`. 실행 계획: `.claude/plans/floofy-crafting-meerkat.md` 승인본.

### 아카이브 AI 생성 사진 선별 (2026-07-11, 사용자 제공 68장)

`아카이브/` — AI 생성 아시아 남성 수염 사진. 사용자 조건: 실사 같은 것만, 이질감 있으면 금지.
시리즈 대표 5장 직접 관찰(01, 2-3, 3-10, 4-2, asian_stubble_40s):

- 넘버 시리즈(01~20, 2-x, 3-x, 4-x PNG 51장): 실사 셀피 스타일(자연광·지하철·밤거리·카페),
  수염 점묘·피부 결 자연 → **사용 가능** (사용 시 장별 재확인).
- asian_* JPG 17장: 스톡 사진풍(완벽 조명·왁시한 피부) → **보류** (왁스감 판정 오염 위험).

용도 제한: **미라벨 시각 강건성 검증 전용**. GT 없음 → 오라클 스파이크/게이트 튜닝 금지.
dev 9장 통과 후 일반화 눈 검증(체크포인트 ③ 보조 자료)에만 사용.

---

## 2026-07-11 스파이크 v2: 가드된 지대 재건 (spike/zone_recon.py)

Codex 조건부 GO 설계 구현 + 반복 디버그 8회 (각 반복은 시각 관문 또는 수치 프로브로 원인
특정 후 수정 — 임계 완화는 근거 기록 시에만):

1. **자 스위트 신설** (dcebe25): stainResidual·overLift(신규 초과만)·structureBand·abQ90,
   자 테스트 10종 green. overLift는 "편집이 새로 만든 초과"만 계상(원본이 S보다 밝은 곳 무죄).
2. **확장 키 절단 버그 수정** (69a47e9): cand_zone = 위쪽 확장 공유, soft/saturated 고주파
   가중(robust-z, p99 포화, hysteresis).
3. **포락선 목표**: T = min(S, E-blend). E = 보호영역 제외 국소 최대(0.05fw)의 저주파 —
   "털 사이 밝은 피부"가 목표라는 사용자 검증 통찰의 구현. 밀집 코어는 λ(털 커버리지)로
   S 쪽 블렌드. S abstain은 S가 실제 목표를 결정하는 픽셀에만 적용.
4. **희석 3대 원인 제거**: ① scope 이중 가중(w² → 지지영역 이진 scope), ② 곱셈 가드 스택
   (min-캡으로 재구성), ③ 얇은 밴드 재블러 되퍼짐(Van-Cittert 역합성곱 3회, 2×캡).
   측정: 의도 리프트 도달률 19% → ~85%.
5. **도너 위생**: L-매칭이 어두운 '클린'(=턱 그늘, GT 밖 잔털 서식지) 패치를 선호해 털을
   재도장 → black-hat 초과 패치 도너 금지. psd03 ghost 0.53→0.41, B의 1.92 이상치도 해소.
6. **코·콧구멍 정책**: 콧구멍 인접(0.025fw)은 고주파 페더 베토(콧털 스코프 밖, 사용자 확정),
   코 밑 밴드는 저주파 무편집(포락선 창이 코 그림자보다 넓어 평탄화 위험 — 시각 관문 3회로
   확정). 채점 제외는 강베토 코어만, 제외 수 기록 (pic2:0, pic4:1, psd03:18, psd05:10).

### 동결 결과 (C=v2; ghost Q90r/생존, stain 잔존율, struct)

| 사진 | A(medium) | C(v2) | 비고 |
|---|---|---|---|
| pic2 | ghost 1.00, stain 1.00 | **ghost 0.00, stain 0.67**, struct 1.00 | 얼룩 잔존은 코 밑 베토 비용 |
| pic4 | ghost 1.00, stain 1.00 | ghost 0.63✗, stain 0.65 | 흐린 사진 — 품질 게이트(거부) 후보 |
| psd03 | ghost 1.00, stain 1.02 | **ghost 0.00, stain 0.42**, struct 1.00 | 콧구멍 코어 제외 18 기록 |
| psd05 | ghost 1.00, stain 0.82 | **ghost 0.00, stain 0.29**, struct 0.99 | 전 게이트 통과 |

overLift ≤1.5L(신규 초과), 턱그늘 ≤±1.6%, 컨트롤 표류 p95 ≤1바이트(v1은 0 — Lab 왕복
스코프 확대 비용, 공개 보고).

### 시각 관문 관찰 (직접 열람, psd03_upper 4회 추적)

- v2 초기: 코 밑 "에어브러시 패치"(Codex 예고 NO-GO) + 콧구멍 베이지 도넛 → 원인 3건
  (포락선 창 폭, 콧구멍 soft 키 발화, 이진 베토 경계) 순차 제거.
- 동결본: 콧수염 완전 소거, 콧구멍 실루엣 보존, 잔여 = 코 밑 옅은 밝은 띠 + 인중 약간의
  평탄함. 판정은 블라인드 ③.

다음: 체크포인트 ③ 사용자 블라인드 판정 ("서비스 허용" 기준) → 통과 시 dev 9장 확대 +
컨트롤 추가, S1a 배선 후 프로덕션 이식.

### 체크포인트 ③ 블라인드 판정 (2026-07-11)

사용자 선택: psd03=Z, psd05=Y, pic2=X — **매핑 공개 결과 3장 전부 C(v2 가드된 재건)**.
2라운드 연속 블라인드 전승. 그러나 **서비스 허용 불가** 판정:

1. 경계 부자연 (특히 입술 쪽) — 정량 확인: 잔존 얼룩의 39~50%가 입술 링(dpr<0.03fw),
   잔존율 0.48~0.85. 원인 = 입술 인접 저주파 베토(정책)가 어두운 테로 남음.
2. 콧구멍에 뭔가 붙여놓은 느낌 — 페더 베토 구간의 "절반 도너 + 절반 원본" 혼합.
3. pic2 턱 안 지워짐 — 원인: 턱 아래 지지 링이 턱 그림자 속 → S가 어둡게 피팅 → D<2
   → 리프트 미처방 (stain 마스크에도 안 잡혀 지표 사각).
4. psd05 턱 끝 마감 이상 — chinLow 잔존 0.00 = 경계까지 완전 리프트 → 턱 그림자와 급경계.

노트: 코 밴드 잔존(0.61~0.96, 비중 37~58%)은 이번 지적에 없음 — 코 밑 잔여 어둠은
자연스럽게 읽힌다는 방증(베토 정책 유지).

다음: v2.1 설계(입술 링 scale-separated 리프트+넓은 램프, 콧구멍 전체 베토+바깥 페더,
턱 하부 페이드, S 지지 방향 가중) — Codex 교차검증 진행.

### v2.1 (Codex #6 교정 반영, 커밋 bfd5ec1) + 체크포인트 ④ 준비

- 입술 분리 리프트(D_hp 통과 + 광역 성분 0.04fw 램프인, ab 별도 가중) → 입술 링 잔존
  0.85→0.45(pic2), 0.49→0.28(psd05). 콧구멍 코어 보존 페더 + 픽셀 단위 채점(에너지 기록).
  턱은 입술과 통일된 broad gate(열별 하부 윤곽 페이드). S 지지 하부 섹터 제외(열 범위 한정).
- 회귀(같은 4장, non-blind): ghost C 0.0~0.22, 얼룩 0.24~0.71, 입술 위 어두운 테 소멸(시각),
  콧구멍 실루엣 보존. pic4는 abstain(=거부) — 의도된 프로덕션 동작.
- **체크포인트 ④ (새 dev 5장, A/C 블라인드, 절대 허용 기준)**: pic1·pic3·psd01·psd02·psd04,
  립코너 컨트롤 랜드마크(61/291) 자동 고정. 수치(C): ghost 전부 0.000, 얼룩 0.11~0.39
  (psd04 최밀집 0.112!), 컨트롤 ≤1바이트. 관찰 리스크: psd01·psd02 overLift 3.6~4.3L,
  psd02·psd04 structureBand 0.90 (폴드 부분 평탄화 가능성) — 판정은 사용자.
- pseudo-hole 격자 폴백(밀집 사진에서 격자 미적중 시 2배 밀도) 추가.

### 체크포인트 ④ 판정 (2026-07-11): 5장 전원 불합격 → 재설계 기준 발동

새 5장 A/C 블라인드(절대 허용 기준). 매핑: C = pic1·psd01·psd04의 Y, pic3의 X, psd02의 X.

| 사진 | 판정 | 결함 (사용자) |
|---|---|---|
| pic1 | C 크게 개선·자연·전반 커버, **불가** | 입 밑·양옆 갑작스런 밝은 밴드, 절반만 지워짐 ("아예 깨끗" 옵션 필요) |
| pic3 | C **불가** | 턱 밴드가 목 침범, 인중·입술밑 미커버, 인위적 모양 |
| psd01 | 둘 다 **불가** | A=원본과 동일, C=너무 얼룩덜룩(모틀) |
| psd02 | **A가 더 자연**, 둘 다 불가 | A는 거의 못 지움; C는 부자연 |
| psd04 | 둘 다 **불가** | C: 비교적 잘 덮었으나 볼·입술밑 미커버 + 너무 밝음(턱); A: 못 지움+턱 어두움 |

수치 경고(overLift 3.6~4.3L, struct 0.90)가 그대로 지각 결함으로 실현됨.
**Codex 사전등록 손절 기준 충족 → 현 구조(링-피팅 S + 포락선 + 가드 스택) 종료.
재설계: 방향성 조명장 + 랜드마크 해부 밴드 + PatchMatch 고주파, boundary-constrained.**

5라운드 인간 판정에서 도출된 재설계 요구사항:
1. 완전 소거 능력(강도 조절로 "아예 깨끗"까지) 2. 밝은 밴드/헤일로 절대 금지(조명 일관 리프트)
3. 모틀 금지(균질 질감 재건) 4. 볼·입술밑·인중 포함 지대 전체 커버(재현율)
5. 목 침범 금지(경계 규율) 6. 코 밑 자연 음영 보존(검증된 정책 유지)

### 경로 전환 (2026-07-11): 경로 B 승인 — LaMa 하이브리드

- Codex #7 판정("짙은 수염 아래 피부는 관측 불가 — 거부 없는 laser-complete는 결정론만으로
  불가, 생성형 인페인터 필요") + 사용자의 갤럭시 S25 AI 지우개 실증(삼성 = LaMa 인페인팅,
  형태 유지·질감 뭉갬) → **사용자 경로 B 승인. 계약 수정: 생성형 신경망 허용 —
  단 추론 결정론(같은 입력 bytes → 같은 출력 bytes, per-environment) 필수. diffusion 금지 유지.**
- 디스크: 승인 하에 external/CelebAMask-HQ + facial_hair_annotations 삭제(2.7GB 확보,
  둘 다 재다운로드 가능한 공개 데이터셋, 스테이지4 학생모델은 이미 기각).
- big-lama traced TorchScript 205.7MB 다운로드, SHA256 `344c77bb…` `spike/lama_manifest.json`에
  고정(로드 시마다 재검증). 라이선스: 코드/가중치 Apache-2.0, 학습 데이터 Places2 —
  **상용화 전 법무 검토 blocking item**.

### Codex #8 교차검증 (2026-07-11): 하이브리드 설계 — 조건부 GO, 블로커 4

1. `0.06 + 0.04fw 일괄 팽창` 과마스크 위험 → 팽창 폐기/축소, erase_core와 context 분리,
   jaw 클립은 팽창 전+후 이중 적용. fringeRing 기반 재팽창은 v1 금지(Goodhart).
2. **quilt donor 선정이 원본 저주파(수염 얼룩 포함)에 매칭 — LaMa 채움 위에서는 어두운
   donor를 골라 모틀 재생산.** → target_low_l 파라미터 추가(LaMa low 기준 선정) + rehipass.
3. lip byte-identity는 필요조건일 뿐 — 인접부 입술 연장/치아/이중 fold 환각 가능.
   넓은 moat 금지(soul-patch 리콜 재손실), **좁은 lip polygon 홀 + lipLeak·newBrightStructure
   자 + 환각 시 abstain**.
4. 러너 결정론 미달 → freeze(optimize_numerics=False), manifest 없으면 로드 거부,
   interop 실패 hard-fail, use_deterministic_algorithms, OMP/MKL 고정, ABA·사이즈·마스크
   토폴로지·PNG bytes 테스트. **전부 반영, tests/test_lama_runner.py 14/14 green.**
- 손절 순서 재정렬: **oracle-mask kill test 최우선**(완벽 마스크에서도 추한지) —
  실패 시 CLIPSeg 배관·자 작업 전 즉시 NO-GO.
- U_b(조명 일관 상한)는 pixel clamp가 아니라 one-sided reject/abstain 자로 유지.
- 체크포인트 ⑤ 프로토콜: ORIG 상시 노출 + A/C 블라인드, **절대 허용/불가가 1차**, 선호는 2차.
  게이트는 새 사진 보기 전 동결.

### Oracle-mask kill test (2026-07-11): 생존 — 단 승자는 LaMa-as-is

pic1·pic3·psd01·psd04, REGION GT 기반 oracle 마스크(위쪽 확장 + 좁은 lip 홀 + 콧구멍 코어
제외 + jaw 클립), LaMa ≤512 추론(1.4~5.0s/장). 패널: ORIG | LAMA(그대로) | HYB(LaMa 저대역
+ 교정 donor 고대역).

- **LAMA 팔 생존**: 입 환각 0건(Codex 최대 베팅 미발화 — 좁은 lip 홀 유효), 턱 과밝음 0건,
  LaMa 자체 모틀 0건. pic1·psd01은 즉시 서비스 후보 수준(여드름 자국까지 자연 소거).
- **HYB 팔(donor 재주입)이 오히려 실패**: psd01·psd04 격자무늬 모틀(Hann 격자 잔존,
  psd04는 donor 침식 과다로 nDonors=1 — 단일 스탬프 반복). 죽인 건 LaMa가 아니라 우리 quilt.
- grain 비율(채움 고대역 RMS / clean 피부): pic1 0.54, pic3 0.55, psd01 0.66(**결손** —
  갤럭시식 왁스), psd04 **1.53(과잉** — 짙은 문맥을 이어 그린 잔털 질감 재생성 의심).
  → grainMatch 자는 **양방향** 필수.
- pic3 인중 양옆 잔존 = GT 리콜 빈틈(LaMa 실패 아님) — 프로덕션 존(CLIPSeg ∪ soft-high)에서
  재검증. psd01 인중 상단 잔존 = 콧구멍 코어 정책 제외(의도된 불가침).
- **psd04 턱선 하드 클립이 "위는 깨끗/바로 아래 수염 링" 경계 생성 → under_jaw_margin 실험:
  jaw+0.06fw(∩ roi — 목 불가침 유지)에서 링 소멸, 턱 실루엣·이중턱 음영 보존. 0.06 채택,
  0.10과 사실상 동일(roi가 캡).**

**v1 방향 확정(Codex #9 교차검증 예정)**: 마스크 내 단일 소스 = guarded LaMa as-is
(자체 저+고대역). donor 재주입은 기본 OFF — grainMatch 트립 시의 조건부 후속 과제
(결정론 스펙트럼 grain 또는 quilt 개선). 근거: 시각 관문에서 LaMa 자체 질감 > quilt,
Codex #8 Q3의 "erase core donor w=1" 권고는 현행 quilt 품질에서 성립하지 않음.

### Codex #9 (2026-07-11): guarded-LaMa-as-is v1 승인 + 프로덕션 블로커 2

- **판정: LaMa-as-is 메커니즘 GO** (Codex 스스로 #8의 donor-high 권고를 증거로 폐기),
  HYB(quilt 재주입) NO-GO 확정. 후속 re-grain 후보는 quilt가 아니라 **결정론적
  phase-scrambled spectral grain**(clean PSD 매칭, 입력 해시로 위상 고정).
- grain 자 재정의: raw RMS는 품질 순서를 역전시킴(시각 OK pic1 0.54 < 시각 waxy psd01
  0.66). → relative_grain_map 기반 G50(log-median)·G90, 참조는 마스크에서 0.03fw 이상
  떨어진 동일 높이대·L-매칭 clean. 초기 밴드 G50 [0.75,1.30]/hard [0.67,1.50],
  G90 ≤1.45/hard 1.60 — **컨트롤로만 동결**.
- newDark(Area/Energy/Coherence OR-판정), newBright, lipLeak 추가 요구 (환각 3종 분리).
- jaw+0.06 채택 동의하되 "∩roi = R5 보장" 반박(roi는 기하 프록시, 목 세그먼테이션 아님).
- edit_mask/model_mask 분리(합성은 edit만, 모델 홀은 +0.004fw), blanket 팽창 금지,
  strict hair 성분만 0.003fw 할로.
- 러너: OMP/MKL setdefault의 외래값 묵인 금지(강제 거부), deterministic flag는
  프로세스 전역이라 동시 호출 race → forward 직렬화. 동시호출·env 불일치 테스트 추가.
- 레이턴시: dev9는 512 고정 + 랩 6~7s 허용, 단계별 타이머 분리, 448은 A/B 동등 시에만.
- 동결 순서: 게이트 즉시 동결 → 합성 컨트롤 6종으로 자 숫자 동결 → dev9 단 한 번.
  **실패한 dev9를 보고 임계값을 바꾸지 않는다.**

### 프로덕션 파이프라인 v1 + dev9 원샷 (2026-07-11)

구현: spike/hybrid_recon.py (GT-free prepare_unlabeled: C1∪CLIPSeg 합집합 필드,
zoneEditThr 0.03 — 서빙 0.5는 GT 리콜 6.6%로 사용 불가(스윕: 0.06→70%, 0.03→83%,
0.01→91%); edit/model 마스크 분리; editable = roi ∪ (얼굴오벌∩코밑) — pic3 flank
잔존의 roi-밖 가설은 오탐(배경/헤어라인 오검출)으로 반증, 실제는 in-roi 증거 구멍 +
재생성 혼재). eval/hybrid_rulers.py + 합성 positive control 8종 green (94 passed).

dev9 원샷 결과: 9/9 실행(clean 사다리 후), 판정은 hard-fail 5 / abstain 4 — 그러나
**1:1 시각 판독과 게이트 판독이 역전**: pic1·pic2·psd04 결과는 눈으로 서비스급인데
게이트는 전멸 수준. 원인 규명:

1. **clean 캘리브레이션 붕괴**: 0.03 존 팽창 제외 후 pic1 clean 2.6k px →
   black-hat 임계값 2.0/5.5 (정상 15/19)로 폭락 → 정상 질감 전체가 "털" 판독.
   → 결정론적 제외 임계값 사다리(0.03→0.06→0.12→0.20→0.50, cleanPx 바닥
   max(1500, 10% roi), 사다리 레벨 기록) 도입. psd02 prepare 탈락도 이것으로 해소.
2. **ghost 게이트 오염**: cand_zone이 jaw 클립/보호를 모름 → 정책상 편집 불가
   구역(턱밑) 후보가 survival을 지배. → 마스크 내 후보만 게이트, fringe(편집 가능
   구역 내 미커버)/policy(정책 제외) 분리 기록.
3. 수정 후에도 잔존 문제: **무라벨 hairy 사진에서 clean 참조의 '품질'이 미보장**
   (사다리는 존재만 보장) — pic2 G90=11, pic1 ghost survival 0.91은 참조 붕괴 서명.
   실물 잔존(진짜)은 콧구멍 코어 정책 구역의 반점(pic2 인중)과 pic3 flank.

**결론: 메커니즘·마스크는 전진, 측정 참조(clean) 재설계가 다음 블로커.**
dev9 결과를 보고 임계값을 조정하는 것은 금지(Codex 규율)이므로, 참조 재설계는
Codex #10 교차검증 대상. 레이턴시: warm 2.4~7.3s/장 (512, M1).

### Sprint A: 커버리지-퍼스트 v3 (2026-07-11, 사용자 승인 + Codex #10 공동 설계)

- 방향 전환: "목이든 가장자리든 최대 커버, 디테일은 위에서". R5(턱 아래 금지) 은퇴 —
  geometry 누락은 이제 coverage failure. 계획은 Codex #10과 공동 설계(신규 자 공식,
  목 코리도, ReferenceBundle 레시피, 체크포인트 프로토콜 전부 반영).
- **워크플로 병렬 구축**: 4모듈(eval/reference_bundle.py 17t, eval/silhouette_ruler.py
  12t, eval/fill_color_ruler.py 9t, spike/mask_v3.py 15t) + API 리뷰(함정 8건: bool 마스크
  계약, 풀프레임 좌표, lab_cov ab 슬라이스, 2-패스 가먼트, 시드 독립성 등). 147 passed.
- LaMa 윈도 전략: 마스크 bbox+0.12fw 타이트 윈도(pic2 scale 1.0 회복).
- **오클루더 과발화 수정**: 짙은 수염·그늘 목 피부가 볼 앵커 대비 비피부 판정
  (occluderFrac 0.545, 마스크 96% 삭제) → 증거 픽셀 제외 후 가드(0.112로 정상화).
  Codex #10 원칙("skin likelihood는 수염 마스크 hard gate 금지") 재확인.
- **실루엣 붕괴 발견·완화 경로 확정**: 얼굴+목 단일 거대 홀 → LaMa가 턱 윤곽을 뭉갬
  (jawShift 0.022fw, corr 0.69 — 실루엣 자가 정확히 hard-fail). 거대 홀 순차 분할은
  턱 회청색 밴드(fillColor abstain이 포착). **최종 구조: 얼굴측은 v2 검증 레시피
  (jaw+0.06 분할선 위) 그대로 + 목은 z-증거 밀착 밴드만 별도 패스(청소된 얼굴을
  문맥으로)** — psd04에서 목수염 소거 성공 + 회색 밴드 소멸, grain 0.756 통과.
  남은 관찰: 턱 미묘한 회색기(fillColor abstain)·부드러운 실루엣(자 hard-fail) —
  자들이 시각 관찰과 일치하게 작동.
- **dev9 v3 원샷 — 수정 라운드 필요한 결함 2건 (모두 빌더가 사전 경고한 리스크)**:
  (A) 앵커 QC 과엄격: 실사진 4/9 (pic1·psd02·psd03·psd05) reference abstain.
  (B) 작은 얼굴(pic 계열, crop fw<350 위험 영역)에서 anchor-z 과발화 → 마스크 0.5로
  폭증, newDark 0.28~0.71, ghost survival ~1.0 — 측정이 아니라 참조 전이의 문제.
  규율 준수: dev9 결과로 임계값 조정하지 않음. 수정은 reference_bundle 계층에서,
  Codex #11 교차검증 대상.

### v3 수정 라운드 (2026-07-11 심야)

- **가먼트 열별 플로어**: 행-지속성 검출이 경사 네크라인(psd04 후디)에서 None →
  코리도가 옷감 위로, CLIPSeg+z 이중 시드가 어두운 네크라인에서 동시 발화해 옷감이
  편집 마스크에 포함(오클루더는 증거 픽셀 면제라 통과). garment_floor_cols(열별
  첫 교차 + 중앙값 필터)로 해결: psd04 gY 531 검출, newDark 0.048→0.008.
- **reference_bundle 수정 (전담 에이전트, 22 tests)**:
  (A) 앵커 abstain 4/9의 원인 = 고정 0.6 코히런스 게이트가 큰 얼굴(fw~650)의 깨끗한
  볼 중앙값(0.55~0.59)보다 낮았음 + 무조건 상위 15% Maha 컷 + 이마 합산 불가 사다리.
  → 스케일 연동 게이트(0.6→0.75 램프), 이상치만 트림(chi3 p96, 15% 캡), 검증된 이마
  보충 + level-4 low_conf 강등. 9/9 번들(전부 level 1).
  (B) 소형 얼굴 z 폭발 = fw≤376에서 3px 블랙햇이 앵커에서 전부 0 → MAD=0, 시그마가
  eps 바닥 → 크롭 텍스처 전부 z 수백만. + 검은 옷에서 명암 분모 바닥.
  → p95 기반 강건 시그마 + 앵커 최저 L −70 아래 무효화. pic 발화율 0.40~0.67 →
  0.01~0.10, 합성 가닥 재현율 98~100% 유지.
- **dev9 v3b (152 tests green)**: 9/9 실행, psd 계열 dark·ghost ≈ 0, 마스크 0.06~0.25
  정상 범위, 가먼트 플로어 전 사진 작동. 잔여 이슈:
  ① **실루엣 자 판별력 부재 (핵심)**: v2급(사용자 극찬)과 에어브러시(명백 변형)를
  같은 숫자로 hard-fail(corr 0.687 vs 0.691), 수염 묻힌 턱에선 identity도 abstain
  (supportFrac<0.30). "stable point"가 털 가장자리를 포함해 **수염 소거 자체를 윤곽
  변화로 측정**. 8/9 hard-fail의 실체 → 재설계 필요(Codex #11).
  ② pic1/pic4 grain 과잉(1.68~1.75, 흐린 원본 위 선명한 채움), pic2 이상치(검은
  재킷 지배 크롭에서 참조 왜곡: G50 0.163/dark 0.21 — 시각은 양호).

### Codex #11 (2026-07-12): 체크포인트 ⑤ 조건부 GO — 서비스는 아직 NO-GO

- **판정**: "고정된 파이프라인을 7장 홀드아웃에서 인간 절대평가하러 가는 GO"이지
  서비스 출시 GO가 아님. 조건 3: ① 실루엣 자 advisory 강등(구조 결함 4종 지적 —
  stable point가 hair-free/SNR 미검사, _peak의 무조건 argmax, jawToLandmark 전 사진
  0.025 = 탐색창 포화로 정보 없음, corr hard-fail이 support 검사보다 먼저), ② 마스크
  전용 사전점검 선행(기준 5종 동결), ③ 사전점검 실패 사진은 수정 없이 제품 abstain.
- 영구 실루엣 자 v2 스펙: hair-free(nz≤0 이웃) stable만, hair-free support<30%→전체
  advisory-abstain(hard 금지), obscured는 연속 contour path 추적, identity 미통과
  사진에선 어떤 hard-fail도 금지. → Sprint B 백로그.
- **게이트 정합 수정 2건 반영**: grainLowConf가 hard-fail로 새던 버그(pic2), fillColor
  성분별 low_conf를 사진 pass가 덮던 버그(pic4). bundle low_conf도 report에 기록.
- D1(face/neck 분리) 영구 승인(공유 jaw curve 통일 조건), D2 원칙 승인(단
  evidence∩raw-occluder는 conflict→abstain으로 — 백로그), D3 승인(단 production
  폴백은 "불확실→0.35fh 개방"이 아니라 abstain — 백로그), S1 승인, S2 시그마 승인
  (−70L은 veto가 아니라 validity로: normalized_excess가 (excess, valid) 반환 — 백로그),
  S3 QC 승인(level-4 전파 수정 완료).
- 정확한 기록 요구 반영: "자 판정 임계값은 컨트롤로 동결, 탐지/참조 운용 임계값
  (zoneEditThr·coherence ramp·−70L)은 dev9(개발 세트)로 보정"이 맞는 문장.
- Codex의 dev9 패널 육안 감사: pic2 전역 밝은 반점, psd02 콧수염 대량 잔존/재생성
  (ghost=0 위음성!), psd01 과평활(병변까지 소거), psd05 따뜻한 blob — **목 커버리지·
  가먼트 안전은 전진했으나 전체 소거·자연스러움·자 신뢰성은 미입증**이 정확한 상태.

### 체크포인트 ⑤ 실행 (2026-07-12, cp5a)

- 마스크 전용 사전점검(동결 기준): nb1·nb7·nb10 통과 / **nb2·nb12 실패 → 제품
  abstain**(가드가 보이는 수염 차단; nb12는 크롭 얼굴 랜드마크 불신뢰) / nb4·nb6
  reference abstain. **라이브 C팔 3/7 — abstain율 4/7이 자체 핵심 데이터.**
- 패널(ORIG 상시 + X/Y 봉인) + 판정표(judgment_form.md) 생성, 매핑은
  spike/blind_mapping_cp5a.json에 봉인. 오퍼레이터는 결과 패널 미열람.

### 체크포인트 ⑤ 판정 결과 + 원인 포렌식 (2026-07-12)

- **인간 블라인드 판정 (봉인 개봉: nb1 X=A/Y=C, nb7 X=C/Y=A, nb10 X=A/Y=C)**:
  nb1 "X 거의 안지워짐 / Y 턱 뭉개져 얼굴형 바뀜 / 인중 거의 안지워짐",
  nb7 "Y 거의 안지워짐 / X 턱-목 경계 못 지움 + 지운 턱 색 잔존 + 인중 잔존, 굳이 X",
  nb10 "X 턱 과백·턱끝/가장자리 미소거·인중 그대로 / Y 수염을 더 만들고 입모양 바뀜".
  총평 "전체적으로 실망, 갤럭시 S25가 훨씬 좋았다". **Sprint A 종료 조건 미달(커버리지
  reject 3/3)** — 단, 게이트도 7/7 abstain·hard-fail(통과 0)로 인간과 일치, 실루엣 자
  (advisory)는 판정 3장 전부 hard-fail로 인간의 "얼굴형" 지적과 일치 → 측정층 검증됨.
- **포렌식 (워크플로 4에이전트: 사진 3 시각 + 코드 감사) 결함 귀속**:
  ① 인중·입양옆 = guard-block — nostril_core(hybrid_recon.py:185-188)가 팽창-립 슬리버
  (protect_mask upper_cap 0.018fw, lower_face_roi.py:107-117)를 씨앗으로 0.025fw 재팽창,
  인중 최대 98.6% 차단(nb10). growth는 인중 100% 발화 — 증거가 아니라 가드 기하 문제.
  ② 목·턱-목 경계 = dual-seed AND 기아 — black-hat은 그늘 목에서 침묵, CLIPSeg 약함,
  1px-AND 결과 dualSeedPx nb1=0/nb7=1; face_mask&=above_split이라 경계 밴드 구조적 미편집.
  nb7은 경계 그림자 호가 occluder 밴드로도 차단.
  ③ nb10 턱끝 밴드 = mask-miss — seed 47%(4.98만px, 87% 하단) 발화에도 최종 edit이
  30-80px 위에서 잘림(분할선 침식 추정; 가드 겹침 235px뿐).
  ④ 채움 붕괴 = lama-artifact — 왁스 회갈색·무모공(nb1 lapvar 32→8.3·dL−13.3, nb7 밴딩·
  grain 1.81, nb10 lapvar 76→9·dAb50 21.9 → "수염 추가"로 지각). nb10 입모양 변화는 립
  바이트가드 유지 확인(마스크 밖 변경 680px) — 립 경계에 붙은 어두운 채움 테의 착시.
  악화: inferenceScale nb1 0.744/nb10 0.381(512 캡 붕괴) — 단 nb7은 1.0에서도 실패 →
  다운스케일은 증폭 요인. oracle(완벽 마스크·작은 창·성긴 수염)은 승인급이었음: big-lama는
  **구멍 크고 문맥에 짙은 수염이 남을 때** 붕괴한다.
- nb1·nb7·nb10 **SPENT** (판정 후 재실행 금지). Codex #12 교차검증 발제(진단 동의 여부 +
  가드 축소/커버리지-주도 마스크 전환/해상도 전략/채움 품질/실루엣 자 hard 복귀 + Sprint B
  순서·새 홀드아웃·갤럭시 비교팔 여부).

### Codex #12 (2026-07-12): 진단 교차검증 + Sprint B 설계 합의

- **귀속 판정**: ①가드 잠식·②목 기아 = 코드상 확정 동의. ③턱끝 절단은 "split이 잘랐다"
  까지 확정, 랜드마크-수염-오염 원인론은 유보(ablation 필요). ④채움 붕괴 = 복합 원인 —
  "dense-beard 도메인 미스매치 + 불완전한 문맥 정화가 근본, 저해상도·마스크 축소·창 분리가
  증폭". **추가 발견 4**: (i) canvas 상단 nose_y+0.01fw 절단도 인중 ceiling, (ii)
  normalized_excess −70L 정책이 깊은 목 그림자를 정책적으로 소거, (iii) 마스크 INTER_AREA
  축소 후 >64 이진화가 0.381 스케일에서 얇은 마스크 토폴로지 파괴(hybrid_recon.py:355-362),
  (iv) model halo 0.004fw는 문맥 정화에 과소. **측정층 맹점**: ghost는 z-검출 후보만 평가
  (nb7 nCands=0) — solid beard 커버리지는 자에 비관측; 다음 체크포인트는 "거절 일치율"과
  "자 원인별 recall" 분리 보고.
- **Sprint B 합의 순서**(각 단계 kill 기준 로그 별도): 0) 마스크 픽셀 provenance 계측 →
  1) 가드·canvas 수리(립=버밀리언+0.003~0.005fw, 콧구멍=NOSTRILS 타이트 디스크
  0.010~0.014fw, NOSE_BOTTOM 씨앗 제거, protect_mask 재사용 금지, 인중 명시 폴리곤;
  philtrum coverage ≥95% kill) → 2) **영역 활성화 구조 전환**: "증거는 어느 해부학 영역을
  켤지만 결정, 켜진 영역은 해부학이 전체 커버"(무조건 목 편집은 반대 — negative FP 비용;
  dual-seed OR 완화는 임시방편으로 기각), occluder hard 감산은 확실한 의복·액세서리만,
  jaw curve는 hard boundary 금지(±tube ∪ contour, overlap feather) → 3) 해상도 factorial
  512/1024/native(타일링 반대 — FFC 전역 문맥 훼손; 공식 refinement는 결정론 재고정 필요)
  → 4) model halo {0.02,0.04fw} × 2-pass(확대 정화→원마스크 재채움) → 5) Lab 정합(합격권
  진입 후에만)·spectral re-grain(ring 위상 소스) → 6) **kill switch**: 완전커버+고해상+정화
  후에도 dev positive 인간 usable<60%면 MI-GAN(MIT)/AOT-GAN(Apache-2.0) 연구 팔 투입
  (MAT·EdgeConnect는 CC BY-NC 상용 불가, GFPGAN은 정체성 변형 위험+라이선스 혼합).
- **실루엣 자**: serving veto(비-pass=미출고) 즉시 복귀 가능, 단 hard-fail 확정은 6조건
  AND(identity pass, support≥0.30, shift>0.012fw ∧ corr<0.75, hair-free probe≥20,
  두 신호 동일 구간)로 제한.
- **다음 체크포인트**: subject-disjoint 신규 40장(dense 8/medium 8/jaw-neck 8/negative 10/
  hard 6), 3팔 A(동결 서빙)·B(Sprint B)·**S(갤럭시 S25 지우개 — quality-reference arm,
  동일 원본·기기·작업자·고정 예산 기록)**, 판정자 3인, 사전등록 게이트(usable≥18/24,
  anatomy change 0, S25 갭≤15%p, negative 열화 0/10, unsafe serve 0/6). usable<12/24 또는
  anatomy 실패≥2면 후처리 튜닝이 아니라 backend/도메인 전환. LaMa 매니페스트에 라이선스·
  출처 URI 필드 보강. cp5a 3장 재실행 금지 재확인.

### 정정 (2026-07-12, 사용자 지적): "승인급"과 "짙은 수염" 서사 폐기

- **정정 1 — 승인 사례는 0건**: "성긴~중간 수염 + 완벽 마스크 = 승인급"은 과장.
  oracle kill test의 "승인"은 내부 평가(운영자 육안 + 게이트 + Codex 감사)일 뿐이며,
  Codex 감사조차 결함을 지적했음(psd02 콧수염 잔존·재생성, psd01 과평활). 인간 블라인드
  판정을 통과한 결과는 어떤 밀도에서도 아직 없다. oracle의 정확한 의미: "완벽 마스크에서
  메커니즘이 죽지 않는다"는 하한 증거까지만.
- **정정 2 — cp5a에 짙은 수염은 없었다**: 원본 재확인 결과 nb1=옅은~중간 스터블(점 패턴,
  피부 전면 가시), nb7=매우 옅은 스터블(그림자 수준), nb10=중간 스터블(콧수염 점+턱 아크).
  풀/짙은 비어드 0장. 즉 **채움 붕괴는 옅은~중간 스터블에서 발생**했고, "dense-beard
  도메인 미스매치가 근본"(Codex #12 종합이 승계한 내 프레이밍)은 데이터 근거 없음 → 폐기.
  남는 진짜 변수: 마스크 형상(가드 구멍·들쭉 경계), 구멍 크기(maskFrac 0.31~0.41 vs
  oracle 0.12~0.29), 창 크기/스케일(824~1344px/0.38 vs 208~696px/0.74~1.0), 순차 2창,
  사진 도메인(jpg/avif/webp 압축). 전부 통제 실험으로 분리 가능(Sprint B 3~4단계 factorial
  설계가 그대로 유효). 엔진 도메인 갭은 "짙은 수염"이 아니라 "Places2→얼굴 피부 채움
  전반"으로 재정의.
- **수염 밀도 조작적 정의(이후 이 용어 사용 시 기준, 다음 체크포인트 층화 전 동결)**:
  ① stubble = 개별 털이 점으로 분리, 사이 피부 가시 / ② medium = 국소 연속 덩어리,
  피부 부분 가시 / ③ dense/full = 연속 털 덩어리, 수염 영역 내 피부 가시율 낮음(예 <20%).
  측정 프록시: 수염 영역 내 피부색 픽셀 비율 + 점 검출기(black-hat)의 개별 털 분해 가능
  여부. 지금까지 전 실험(dev9·newbeards)은 ①~② 범위였다.
- 진행 중인 딥 리서치(5앵글)는 질문 자체가 밀도와 무관하게 유효(엔진 대안·fine-tune·
  상용 스택·데이터)하므로 유지하되, 결과 해석에 본 정정을 반영한다.

### 딥 리서치 (2026-07-12, 5앵글 + 적대적 검증 12건, 에이전트 16/17)

- **A1 전용 수염 제거**: 기성 오픈소스 수염 제거 모델은 존재하지 않음. StyleGAN 잠재편집
  계보(HairMapper 등)는 전부 비상업 + aligned crop 전체 재생성이라 정체성 훼손 구조적
  (arXiv 2403.08092 정량 증거). 마스크 국소 인페인트(우리 접근)가 문헌상 정체성 보존 우위.
  Olszewski CVPR 2020(합성 수염 50종으로 페어 데이터) 레시피만 차용 가치. FaceApp은 검증
  단계에서 기각(클라우드 전용 + TOS가 연구 목적 사용까지 금지).
- **A2 LaMa 얼굴 fine-tune (핵심 가설) — 실행 가능, 선례 2**: 공식 리포가 학습 레시피 +
  fine-tune용 big-lama-with-discr 체크포인트 배포. anime-lama(big-lama→512px 도메인
  fine-tune→TorchScript 배포 = 우리 경로 실증), GLaMa(LaMa 얼굴 재학습, NTIRE 2022 1위).
  공식 CelebA-HQ LaMa(256px, 비상업)로 며칠 내 가설 사전검증 가능(연구용). 함정: 손실
  레시피 변경 금지(#342 품질저하), 마스크 분포가 1급 하이퍼파라미터, 커뮤니티 fine-tune
  성공담은 과장(#341은 실패담) — 난이도 있음. **refinement는 추론 중 Adam 최적화 →
  결정론 위반, 영구 배제**.
- **A3 상용 정찰 — S25 재해석**: S25 지우개는 클라우드 diffusion(계정 필수·12MP 리사이즈·
  워터마크) — 동일 조건 경쟁자가 아니라 다른 리그. LaMa 자체가 삼성 리서치 산물(계열
  선택은 옳음). Google Magic Eraser(10MB U-Net 온디바이스)가 비-diffusion 상용 실증이나
  '배경 물체'용이고, 큰 구멍은 업계 전체가 생성형 클라우드로 이동. 전략: 일반 도메인
  추격 불가 → 수염 도메인 특화 학습으로 좁혀 이김. 오프라인·무워터마크·결정론이 차별점.
- **A4 엔진 시장**: 상업-클린 얼굴 인페인터 부재. MI-GAN: 코드 MIT이나 가중치는 라이선스
  무표기(개인 HF 계정 배포) + 리포에 NVIDIA-NC 코드 혼입(inference/ONNX 경로만 클린) +
  얼굴 가중치 256px. AOT-GAN: 코드 Apache, CelebA-HQ 512 가중치는 비상업 오염(실험용만).
  MAT/FcF/CoModGAN research-only. 2024+ 비-diffusion 신모델 사실상 소멸 → 엔진 교체보다
  **현 LaMa 재학습이 최소 위험 경로**.
- **A5 데이터 전략 — 역방향 페어 합성**: AI Hub 한국인 안면 데이터(FAQ 기준 영리 모델
  허용·출처 명시 의무·국외반출 제한·일부만 공개 — 법무 확인 필요) 무수염 얼굴에
  Qwen-Image-Edit(Apache-2.0 확인)로 수염을 '추가'해 픽셀 정렬 페어 생성 → 역방향 제거를
  지도학습. diffusion은 오프라인 데이터 공장 전용이라 추론 결정론과 무충돌. 단 한국인
  얼굴 수염 추가 품질은 미실증 → 스파이크 필요.
- **법무 리스크 목록 갱신**: big-lama 가중치 자체도 저자 명시 라이선스 없음(제3자 HF
  미러의 자체 표기 + 라이선스 문서 없는 GDrive; Yandex 원링크 전멸 — 링크 부패 이력).
  기존 '상용화 전 법무 검토' blocking item에 구체화하여 추가.
- **밀도 정정 반영 해석**: 리서치는 'dense 붕괴' 전제로 발주됐으나 결론은 밀도 무관 —
  문제를 'Places2→얼굴 피부 채움 도메인 갭'으로 재정의해도 처방(도메인 fine-tune) 동일.
  스터블에서도 실패했다는 정정이 오히려 fine-tune 필요성을 강화.
- **계획 반영**: Sprint B 코어(가드·영역활성화·해상도·문맥정화)는 엔진 무관이므로 유지.
  병행 스파이크 2건 추가 — (S1) CelebA-HQ LaMa·AOT-GAN 512·MI-GAN 256을 개발 실패
  사진(dev9 + 미사용 newbeards nb3/5/8/11; cp5a 3장 재실행 금지)에 걸어 '얼굴 도메인
  가중치 → 왁스 감소' 가설 검증(연구용), (S2) Qwen-Image-Edit 수염 추가 품질 스파이크.
  두 스파이크 GO 시 페어 공장 + big-lama fine-tune(Sprint C 규모)으로.

### Sprint B 단계 0-1 실행 + S1 스파이크 (2026-07-12, 커밋 166aa3f)

- **가드 재설계 결과 (마스크 전용, dev9)**: 인중 coverage 평균 42.9%→87.2% (psd 5장
  96.0~99.9%), 상단 gap 9장 전부 0px, 진짜 립/콧구멍 침범 0px, pytest 160+3skip.
  guardExcludedEnergyFrac 9장 전부 감소(잔여는 거의 occluder). **정직 kill 판정**:
  ① 인중 <95% 3장(pic1 93.9 / pic2 45.8 / pic4 55.0 — 희박 콧수염에 zone 증거 자체가
  안 뜸), ② 입옆 밴드 미개선(occluder_guard가 증거 없는 수염그늘 피부를 대형 비피부
  성분으로 오인 — pic1 밴드의 68% 차단), ③ y+ 랜드마크 섭동 취약(6/9장 >15%p 하락,
  canvas 상단이 NOSE_BOTTOM y 앵커). 적대 리뷰: 치명 0, 수치 게이밍 없음 확인(스팟
  재실행 일치), minor 4건(콧구멍 원판이 어퍼처가 아닌 콧방울 위치 — 어퍼처 실보호는
  canvas 경계가 담당하는 순환 정의; 섭동 시 침범 미측정; top_gap 컬럼 스킵; 미추적
  JSON 정리). newbeards 잔여 4장(nb3/5/8/11)은 전부 게이트/abstain 탈락 — **가용
  newbeard 재고 소진, 다음 라운드 신규 사진 필수.**
- **S1 얼굴 도메인 엔진 스파이크 (오라클 마스크 4케이스, 연구 전용)**: verdict=mixed.
  celeba-lama-512는 왁스 실측 감소(큰 마스크 lapvar ratio 1.4~1.6배, psd01 ΔL +16.7→
  +5.8; 소형 마스크는 4팔 중 육안 최상) **그러나 큰 턱 마스크 2/4에서 '두 번째 입'
  구조 환각** — 왁스보다 치명적. AOT-GAN 이득 없음(과명도+스머지), 256 조건은 업스케일
  블러. MI-GAN skip(onnxruntime이 numpy 핀과 충돌 위험). venv 무사(순수 파이썬만 추가,
  결정론 16 tests green), 다운로드 375MB, 디스크 4.6GB 잔여. 산출:
  outputs/ghost/s1_face_domain/. **해석**: 도메인 갭 실재 입증 + 범용 얼굴 사전분포는
  위험 → fine-tune은 수염 모양 마스크 + 피부 GT 페어(환각 압력 제거)로 설계해야 함 —
  딥리서치의 "마스크 분포=1급 하이퍼파라미터"와 정합.

### 범위 결정 (2026-07-12, 사용자): 엣지케이스 전면 제외 — 촬영 게이트로 균일화

- 사용자 지시 원문 요지: "마스크 쓴 사람, 강한 그림자, 목수염 등 엣지케이스 전부 제외.
  촬영 게이트로 품질을 균일화할 것이므로 잘 찍힌 사진만 대상."
- 함의: ① 목 코리도·dual-seed·가먼트 플로어·오클루더의 목 영역 로직 = 범위 밖(제거
  vs 휴면은 Codex #13 자문), ② 오클루더 가드는 촬영 게이트 전제에서 대폭 축소 가능
  (입옆 밴드 차단 문제의 구조적 해소 경로), ③ 체크포인트 층화에서 occluded/hard 층
  제거 — 잘 찍힌 사진 내 밀도 층화 + negative(무수염 오편집 안전)만, ④ 촬영 게이트
  스펙이 제품 구성요소로 승격(정면성·조명 균일·하관 무가림·최소 얼굴 크기 — 기존
  파이프라인 게이트 재사용: face_not_frontal, reference abstain, landmark 실패),
  ⑤ fine-tune 마스크 분포도 "잘 찍힌 정면 사진의 얼굴 수염 모양"으로 좁힘.
- 주의: 목수염 커버리지-퍼스트 지시(07-11)는 본 결정으로 대체됨(superseded).

### Codex #13 (2026-07-12): 범위 축소 반영 설계 합의

- **Q1 목 로직 = 물리 제거** (휴면 플래그 반대): neck_corridor·garment_boundary/floor_cols·
  목 dual-seed·neck_ev/neck_mask·faceNeckSplit·underJawExtend 크롭 확장·occluder의
  목/의복 판정 전부. 새 face canvas = FACE_OVAL(버퍼된 코밑 아래) ∪ 턱선 튜브 얼굴쪽;
  composite 쓰기 마스크는 턱선에서 종료(model_mask만 문맥 정화용 소폭 확장 허용).
  되돌림은 커밋 태그+연구 스크립트 보관+턱선 회귀 테스트로.
- **교차검증 정정**: pic2 인중 실패는 noEvidence(662px) 단독이 아니라 **occluderGuard
  (1,417px)가 더 큼** — kill (a)/(b)는 결합돼 있음 (maskfix_b1/report.json 확인).
- **Q2 단계 2 스펙**: ① 콧수염 region activation — zone_soft의 max 합성을 풀어
  CLIPSeg/texture 신호 분리 보존, 좌·중·우 3하위영역, 발화 규칙 A(coincident ≥
  max(8px,0.5%|R|) + x 3분할 중 2칸) or B(strict edit ≥3%|R| + hair성 성분 3개
  span≥0.04fw + semantic ≥1%|R|); 활성화되면 R∩canvas−가드 전체 채움; Lab 어둠 단독
  발화 금지·negative 활성화 0건 hard kill. ② **얼굴 canvas 내 occluder hard subtraction
  제거** — 확실한 가림물은 픽셀 carve가 아니라 사진 단위 abstain/재촬영(semantic
  conf≥0.90, canvas 겹침≥0.5%). ③ 상단 버퍼 top=NOSE_BOTTOM−0.025fw + **실제 콧구멍
  어퍼처 검출 가드**(어두운 compact 쌍 검출+0.003~0.005fw 팽창, 검출 실패 시 가드 날조
  금지·재촬영). 현행 "침범 0px" 자는 순환(같은 원판 재정의) — 수동 어퍼처 GT로 교체.
  perturb kill은 하락률이 아니라 섭동 상태 절대 coverage ≥95%.
- **Q3 촬영 게이트 초안**: fw≥320px·ratio≥0.25, roll≤8°·yaw≤0.15·pitch±10°, face
  lapvar≥15, 밝기 70~210·클리핑<1%, 좌우 볼 L차≤12, 하관 무가림, 목수염 없음, reference
  번들 성공(양볼·low_conf=False). 항목별 재촬영 안내 문구 포함. **게이트-파이프라인 정렬**:
  동일 바이트에 서비스 _fit 적용 후 게이트, 게이트 산출물(랜드마크·reference)을
  파이프라인이 재사용(재검출 금지), capture_pass→pipeline_abstain <1% 목표.
- **Q4 dev 재정의**: 유지 pic3·pic4·psd03·psd05(+조건부 pic1, fw 272.8<320), 제외
  pic2(검은 재킷)·psd04(후디)·psd01/02(상방 pitch) → **엄격 4장뿐, 다음 튜닝에 불충분**.
  권장 dev 36(stubble8/medium8/dense8/neg12, subject-disjoint). 95% kill은 폐기 대신
  재정의: 활성 영역 coverage ≥95%, mouth-side는 수염 있는 사진만 분모, 사진·영역별 보고.
- **Q5 S1 해석 정정**: "환각 압력 제거"가 아니라 "**데이터·마스크 분포 기인 환각 압력을
  낮춤** — pretrained face prior와 아키텍처의 전역 생성 압력은 잔존". 학습 마스크 3묶음
  (서빙형 수염 마스크/경계 perturbation/no-op negative) + 수염형 vs 직사각 vs random
  마스크 ablation으로 환각의 데이터/아키텍처 기인 분리.
- **Q6 체크포인트 재설계**: 40장(stubble10/medium10/dense10/neg10), 1인 1장
  subject-disjoint, 판정자 3인, S25는 kill 기준에서 제외(참고 상한선). 사전등록:
  usable≥25/30, 밀도별 ≥8/10, anatomy 0/40, neg 열화 0/10, coverage≥95% 사진 ≥29/30.
- **주(오케스트레이터)**: Q4·Q6의 인물 수 요구(dev 36 + 체크포인트 40, subject-disjoint)는
  신규 촬영 불가 제약(2026-07-12 사용자)과 충돌 — 아카이브 전수조사 결과로 현실화 필요.

### 사진 재고 전수조사 (2026-07-12, 시각 에이전트 5) — 아카이브 68장 전량 AI 생성

- **아카이브/ 판정**: 01~20(합성 인물 1명의 연령·장소 변주 20장, 전부 stubble, 게이트
  통과 17), 2-1~6(합성 페르소나 1명의 연령·체형 변주, stubble, 통과 6), 3-1~20(베이스
  얼굴 1개의 4클러스터 변주, stubble, 통과 19 — pic 인물과 비동일 확인), 4-1~5(합성 1명,
  3/4 측면 위주로 통과 1), asian_* 17(diffusion 확정: 1024², EXIF 없음, 2026-07-10 01:32
  KST 9초 간격 배치, 의사문자·기기 오류·해부학 오류; **dense 4 / medium 7 / stubble 6**,
  통과 16). 생성 근거: 동일 얼굴의 20~50대 공존, 인쇄된 듯한 균질 점묘 수염, 배경 용융.
- **실사 재고**: pic1~4(실존 1명, dev9 소진), psd01~05(실존 5명 각각 상이, dev9 소진;
  302beardPhotos smartobject 레이어=원본, pixel 레이어=**초록 수염 GT 마스크** — 증거
  계층 캘리브레이션 자산), hold1~3(실사, 소진; hold2·3는 아카이브 인물과 비동일 확인),
  green1~4는 사진이 아니라 주석 마스크. **미소진 실사 신품 = 0장.**
- **무수염(negative) 공백**: 전 재고에 무수염 게이트 통과 사진 0장 — negative 활성화
  0건 kill 기준을 검증할 수 없음. 해소책: 사용자가 보유한 생성기로 무수염 한국인 남성
  정면 셀피 10~12장 생성 요청(촬영 불필요). dense 실사도 0장 — dense 층은 당분간 생성
  4장뿐.
- **사진 경제 (신규 촬영 불가 확정)**: ① 마스크 기하·커버리지·강건성 측정 = 생성
  게이트 통과 ~43장 + dev9 생존 4장(pic3·4·psd03·05) — 기하 측정은 수염 질감 비의존이라
  생성 사용 안전. ② 증거 계층 캘리브레이션 = psd GT 마스크(실사 5명) 활용. ③ 채움
  품질(왁스) 평가 = 실사만 신뢰(dev9 생존 4 + oracle 케이스) — 생성 수염은 질감이
  비현실적(인쇄형 점묘)이라 fine-tune GT·왁스 평가에 부적합. ④ 인간 판정 체크포인트 =
  합성 층(밀도 층화 가능) + 실사층은 소진분 재사용 여부 사용자 결정 필요.

### 신규 실사 15장 조사 + 사전 등록 분할 (2026-07-12)

- **무수염 후보 10장(IMG_4559~4568, 실사 확인)**: 순수 negative 게이트 통과 5장 —
  IMG_4560·4561·4564·4565·4567 (4566≒4567 동일 인물, 4564·4567은 강한 보정으로 피부
  텍스처 소실 — "보정 심한 피부" 분포 대표로만). **IMG_4559는 확대 결과 면도 후 모근
  스터블 → negative에서 제외, 실사 양성(stubble) dev로 전환.** 완화 등급(게이트 탈락이나
  negative 가치): 4562(그림자·머리 가림), 4563(3/4 측면+MK스포츠 워터마크), 4566(크기
  미달), 4568(236×314 저해상+후드).
- **dense 후보 5장(denceKoreanActor, 5인 각각 다른 배우)**: 정직 강등 — **실제 dense는
  IMG_4572 단 1장**(풀비어드, 핵심부 피부 가시율 <20% 확인). 4569·4570 = medium(게이트
  통과), 4571 = medium(비정면 탈락, 한경닷컴 워터마크), 4573 = stubble(활짝 웃음 비중립
  표정, 아시아투데이 워터마크).
- **권리 주의**: 대부분 연예인 프로필·행사 보도사진, 3장에 언론사 워터마크 — **내부
  평가 전용, 학습 데이터·외부 공개·산출물 커밋 금지.**
- **사전 등록 분할 (파이프라인 실행 전 확정, 파일명 정렬 교대 규칙)**:
  ─ negative dev(오발화 검증용): IMG_4560, 4564, 4567 (+완화 프로브 4562, 4568)
  ─ negative 홀드아웃: IMG_4561, 4565
  ─ medium: dev IMG_4569 / 홀드아웃 IMG_4570
  ─ **dense 홀드아웃: IMG_4572 (유일 실사 dense — dev 사용 절대 금지)**
  ─ 실사 양성 dev 추가: IMG_4559(stubble), IMG_4573(stubble, 표정 프로브)
  ─ 범위 밖 보관: IMG_4563, 4571(비정면), 4566(크기)

### dense 2차 배치 조사 (2026-07-12, denceKorean/ 5장 추가 — 폴더명 변경됨)

- 판정: **IMG_4574 = dense 통과**(핵심부 가시율 ~15%, 비니 풀비어드 차량셀피, 게이트
  통과, 워터마크 없음) / 4575 = stubble(게이트 통과, 스튜디오) / 4576 = medium(다크
  스튜디오 방향성 그림자로 게이트 탈락) / 4578 = medium(게이트 통과, **OSEN 워터마크**) /
  4580 = stubble(영상 캡처, 얼굴 ~180px 크기 미달 탈락). 신규 5인 전원 상호·기존 5인과
  비동일. 주의: 4572·4574 모두 "차량 내 풀비어드 셀피" 장면 유형 중복(다양성 참고).
- **실사 dense 총 2장.** 분할 확장(사전 등록 규칙 연장): dense — 4572 홀드아웃(기봉인) /
  **4574 dev**(첫 실사 dense dev 프로브 — 채움 왁스 개발에 사용). stubble — 4575 홀드아웃
  (실사 stubble 홀드아웃 첫 확보), 4580 범위 밖 보관. medium — 4578 dev(워터마크,
  내부 전용), 4576 범위 밖 보관.
- 현 실사 풀 요약: dev 양성 = pic3·pic4·psd03·psd05(+pic1 조건부)·4559·4573·4569·4578·
  **4574(dense)** / 홀드아웃 양성 = **4572(dense)**·4570(medium)·4575(stubble) /
  negative dev 3(4560·4564·4567)+완화 2 / negative 홀드아웃 2(4561·4565).

### 마스크 v4 착지 (2026-07-12, 커밋 138d04b)

- 구조: 목/가먼트/코리도/faceNeckSplit/2창 LaMa/얼굴 내 occluder 차감 물리 제거(리뷰어
  grep 완전성 확인), canvas 상단 NOSE_BOTTOM−0.025fw 버퍼, **콧구멍 어퍼처 실검출 가드**
  (미검출→abstain "lower_face_uncertain"), underJawExtend 0.38→0.12(구조 에이전트가 보고
  직전 API 오류로 사망해 커밋 메시지로 귀속 보완).
- 콧수염 활성화(spike/mustache_region.py): semantic/texture 신호 분리, L/C/R 3하위영역,
  규칙 A/B 동결. **psd GT recall(측정만)**: psd02·03·04 전체 활성 97~100% / **psd01
  전면 침묵(CLIPSeg max 0.047<0.06) recall 85.9%** / psd05 어퍼처 미검출 abstain(규칙
  우회 진단 99.9%). pytest 170+3skip.
- 적대 리뷰: 치명 0·okToCommit, 정직성 재측정 일치. minor: 집계에 활성화-침묵 kill 지표
  부재, gtRecall canvas 스코프 분모, pic3 perturb 88.2%<95%(스팟), 바이트 게이트 키 부재
  시 무음 스킵.
- 측정 에이전트가 64장 실행을 6장에서 중단 반환 → **maskv4_b3로 재실행**(하네스 지적
  2건 수정 + 신규 실사 dev: dense 4574·negative 오발화 3장 포함, 실사/생성/negative
  3분리 집계).
- 열린 리스크: ① psd01형 semantic 침묵(저대비 CLIPSeg 0.06 미달 시 부분 삭제 위험) —
  negative 세트 확보됐으니 다음 Codex 라운드에서 임계 재검토, ② 어퍼처 검출 recall
  (psd05 1/5 abstain), ③ lobe 0.08fw·mole 가드 0.03fw는 스펙 밖 초기 동결값 — Codex
  교차검증 대상.

### maskv4_b3 전체 측정 + 독립 검증 (2026-07-12, 하네스 커밋)

- 72장(real 10/neg 3/gen 59), 검증 에이전트 독립 재측정 **불일치 0건**, 집계 정직성
  PASS(침묵·perturb abstain·abstain율 모두 분모 조작 없음), pytest 170+3skip.
- **Kill 판정**: ① 활성 커버리지 PASS(real 4/4 = 100%, raw 97.1~99.1) ⑤ 활성화 침묵
  0건 PASS / ② 침범 FAIL(IMG_4573 웃음 프로브 어퍼처 어둠 223px — 짙은 콧수염 털
  가능성 있으나 수치대로) ③ perturb FAIL(y방향 구조적 88~91% — 밴드가 nose_y 앵커라
  같이 밀림; perturb 중 어퍼처 재검출 abstain 빈발) ④ negative FAIL(**활성화는 완벽
  침묵했으나 strict hysteresis 마스크가 민낯에 14,549px 연속 blob 오발화** — 스펙클
  아님, 성분 39개 최대 6,638px) ⑥ abstain율 real 60%(reference 4 + aperture 2).
- **병목 확정: reference abstain** — 신규 실사 5장 중 4장(첫 dense 4574 포함)이
  "no valid anchors"로 측정 불가; gen asian_stubble 계열 11장 동일. 다음 라운드 최우선.
- gtRecallBandPct 신설로 확인: 콧수염 밴드 내 GT는 사실상 전량 커버(99.9~100), canvas
  스코프와의 격차는 밴드 밖 턱수염 몫. mouthSide는 여전히 약함(50~56%).
- 검증자 지적 허점 1(미발현): 활성화됐지만 가드 차감 후 분모 0이면 coverage=None으로
  분모에서 조용히 빠질 수 있음 — assert 추가 예정.

### 사용자 결정 4건 (2026-07-12)

1. **필터(보정) 셀피 = 지원해야 함** — 촬영 게이트에서 거부하지 않는다. 참조 계층
   재설계(보정 피부에서의 앵커 QC 완화/대체 기준)가 로드맵 우선순위로 승격. 이번에
   abstain된 신규 실사 4장(4559/4569/4574/4578)은 성가신 예외가 아니라 **인도메인
   개발 대상**으로 재분류.
2. **판정자 = 사용자 1인 고정** — 다음 체크포인트는 1인 판정 전제: 절대 기준
   체크리스트를 촘촘히 하여 주관 편차 최소화, 다수결 불가.
3. **소진 실사 재사용 금지 유지** — nb1·7·10 등 판정 사용분 봉인 지속. 새 홀드아웃
   (실사 5장 + 생성 층화)으로 진행.
4. **S25 기준팔 불가** — 이전 비교는 대여 기기였고 사용자는 아이폰 사용. S25 팔 폐기.
   (대안 검토 가능: iPhone Clean Up은 온디바이스 비-diffusion으로 확인됨(딥리서치) —
   지원 기기면 오히려 더 공정한 기준팔. 사용자 의향 확인 필요, 강제 아님.)

### Codex #14 (2026-07-12): v4.1 수리 라운드 설계 + 체크포인트 ⑥ 프로토콜

- **Q1 민낯 오발화 = (a) 중심**: strict hysteresis를 해부학 부위 활성화(볼 L/R·중앙
  턱·턱선 L/R·입가·콧수염) 아래로 종속 — 활성 부위 안에서만 허용, 전 부위 비활성이면
  **사진 전체 exact no-op**. (b)는 보조 방어(부위별 semantic+texture 이중 지지·strong-
  support 밀도 하한·geodesic 성장 거리 제한·저역치 bridge 분리 — 최소 성분 크기 단독은
  기각: 4564 최대 blob 6,638px이라 살아남고 진짜 스터블 점만 죽음). (c) 별도 무수염
  분류기는 negative 3장으로 과적합 위험 — 도입 금지. cands_ext도 활성 부위로 종속.
- **Q2 정정 — 참조 실패 주범은 저해상**: Codex 재실행 실측 — 4578 fw167(raw 411px),
  4560 fw214(703px), 4574 fw255(QC후 639<768), 4559 볼 594px+이마 inlier 1.3% 폐기,
  4569 fw531이지만 클리핑 705/1010px+이마 inlier 63.9%<70%. 생성 11장도 전부 Nmin 미달
  (fw 256~310). 처방: ① build_reference_bundle이 None 대신 **typed 실패 사유**(rawCapacity/
  clipRejected/coherenceRejected/mahaRejected/foreheadInlier/nmin) 반환, ②
  rawCapacity<Nmin은 insufficient_face_resolution으로 분리(QC 완화 대상 아님 — 촬영
  게이트 fw≥320 항목과 정합), ③ 동일 인물 해상도/압축/필터 통제 factorial로 원인 분리,
  ④ QC 일괄 완화 금지 — 필터 지원은 "필터 여부"가 아니라 좌우 앵커 일치도·통계 안정성
  기준으로.
- **Q3**: perturb 분모는 이미 원본 고정(코드 확인) — 88~91%는 가짜가 아니라 **실제 기하
  민감도**(밴드가 랜드마크에 결박). 분모 조작 금지, 제품 측 uncertainty envelope는
  negative 침범 별도 검증 후. abstain 방향은 별도 집계(perturbAbstainDirections 등).
- **Q4**: IMG_4573 223px는 FAIL이 아니라 **inconclusive** — gray<60 휴리스틱이 짙은
  콧수염 털을 어퍼처로 오분류. 지표 3층 분리(순환 core overlap / 독립 dark-component
  후보 / **홀드아웃은 수동 어퍼처 annotation**). 어퍼처 검출기에 좌우 대칭·간격·다중
  임계 유지·perturb 합의 추가.
- **Q5**: LOBE_EXT 0.08fw는 현 자료로 튜닝 금지(실측 콧수염 외측 p95 annotation 필요),
  MOLE 가드는 이름(DIAG)과 달리 bbox max-side 구현 — 버그성, 임시 방어로만.
- **Q6 순서**: 마스크 v4.1 hardening 1라운드 먼저, 단 무기한 금지. **Sprint B 진입 조건
  5**: ① negative 4장 exact no-op ② 참조 abstain typed 사유 분리 ③ perturb 실패/어퍼처
  abstain 분리 집계 ④ 4573 재판정 ⑤ coverage=None loophole 봉쇄(분모 0 = invalid/kill).
  4574(fw255)는 end-to-end 불가 — **oracle 고정 마스크 순수 채움 연구로만** 사용.
- **Q7 체크포인트 ⑥**: 실사 5장 primary(사전 annotation 후 봉인, negative는 editPx=0 +
  byte identity, 5/5 처리 필수 — gate 통과 사진의 abstain도 실패) + **신규 생성 15장**
  secondary(기존 생성물은 dev 전용 — 신규 identity·프롬프트·시드 봉인: positive 12 =
  밀도3×표정2×보정2, negative 3). 1인 판정: 최종 challenger 1 + frozen baseline 1만,
  무기명 A/B 좌우 랜덤, 항목별 A/B/tie 1회 판정, rubric은 dev로 동결, 원자료(3/3 등)로만
  보고. 실행 전 코드/모델 해시+이미지 manifest 커밋, 결과 열람 후 수정 시 5장 전체 소진.

### 체크포인트 ⑥ 사진 자체 조달 (2026-07-12, 아카이브/webset_cp6 — 봉인)

- 웹 라이선스 명시 소스에서 28장 수집, **21장 게이트 통과**: dense 4(전원 19~20세기 초
  아이누 흑백/세피아 — 현대 컬러 dense는 라이선스 명시 소스에서 0장, 도메인 편향 주의;
  현대 dense 앵커는 실사 홀드아웃 4572가 담당) / medium 8(봉준호·홍상수·서극 등 CC) /
  stubble 4(설경구·김기덕·조진웅·릴리 프랭키 — 최약체 층) / 무수염 5(한국 배우 CC BY 3.0
  + Unsplash). 인물 중복 0, 밀도 정정 3건(정직 강등·정면 초과 탈락).
- manifest.json(출처·라이선스·저작자) + README(봉인·내부 평가 전용) 작성, 누락 출처 12건
  병합 완료(잔여 2건 = 라이선스 미상 cand_* 격리 유지). **파이프라인 투입 금지 봉인** —
  체크포인트 ⑥ 홀드아웃 후보.
- 체크포인트 ⑥ 재료 충족 상태: 실사 primary 5(4572/4570/4575/4561/4565) + webset 21
  → Codex #14의 secondary 15장 요구를 생성 없이 실사로 충당 가능. 보강 희망: 정면
  stubble +2, 현대 컬러 dense +2 (선택).

### 정정 (2026-07-12, 사용자 지적): 아이누 세피아 4장 체크포인트 제외

- 아이누 사진은 "라이선스 명시 ∩ 동아시아 ∩ dense ∩ 정면"의 교집합이 현대 사진에
  사실상 없어서 나온 기계적 결과 — 그러나 1900년대 세피아/흑백은 컬러 Lab 통계·피부
  앵커·현대 셀피 도메인 전제와 불일치, 촬영 게이트로도 탈락감. **체크포인트 ⑥ 재료에서
  제외, webset_cp6에 '극단 프로브 참고용' 라벨로 보관만.**
- dense 층 정직한 현실: 현대 실사 dense = 홀드아웃 4572 단 1장 → 체크포인트에서 primary
  1장으로 쓰고 "dense 일반화는 표본 1로 미입증"을 사전등록 한계로 명기. dense 실질 검증은
  dev 쪽(4574 수동 마스크 채움 factorial)이 담당.
- webset_cp6 사용 층: medium 8 / stubble 4 / 무수염 5 (+실사 primary 5).

### 범위 결정 (2026-07-12, 사용자 방향 + 오케스트레이터 적용): dense는 v1 목표에서 제외

- 근거: ① 한국인 레이저 제모 수요의 실체는 짙은 스터블~미디엄(청수염)이지 풀비어드가
  아님 ② 체크포인트 전패도 스터블에서 발생(dense 서사는 이미 금지된 오귀속) ③ 현대
  실사 dense 표본이 1장뿐이라 통계 불가.
- 적용: 체크포인트 ⑥ 합격 기준에서 dense 층 삭제 — **stubble·medium이 승부처**. dense
  (4572 홀드아웃·4574 dev)는 비차단 스트레스 프로브로만 측정·보고. 채움 factorial의
  4574는 유지(데이터 가치). 추후 제품은 dense 감지 시 "지원 준비 중" abstain 옵션.
- 효과: "현대 dense 실사 부족" 문제 자연 해소, 아이누 세피아 소동 종결, fine-tune 데이터
  설계도 스터블~미디엄 분포 중심으로 단순화.

### 범위 결정 (2026-07-12, 사용자): 평가·데이터를 동양인에 국한하지 않음 — 글로벌 앱

- 사용자 지시: "동양인 남성에 국한하지 말고 필요하면 서양인 사용. 앱은 전 세계 대상."
- 적용: ① 사진 수집 풀을 전 인종으로 확장(약체였던 stubble 층 즉시 보강 가능) ② 평가
  세트에 피부톤 다양성 포함 — 어두운 피부에서 대비 기반 검출기(black-hat)·피부 앵커
  거동을 측정 항목으로 추가(기술적으로 유일하게 톤 의존적인 지점) ③ fine-tune 데이터
  설계도 다인종 분포로(한국인 중심 가정 해제).

### 세션 핸드오프 스냅샷 (2026-07-12, 진행 중 작업의 복구 지점)

- **실행 중 (이 세션 종료 시 소실 — 완료 후 절단 권장)**: ① v4.1 hardening 워크플로
  (b2 성분 이중신호 게이팅 + 참조 typed 사유/열화 factorial + mask_eval 보강 + 리뷰;
  hybrid_recon/mustache_region/reference_bundle/mask_eval 편집 중 — 중단 시 git status로
  잔여 확인, 최악 시 git checkout으로 138d04b/cadfe81 복원) ② 채움 factorial(오라클 4 +
  4574 수동마스크 × 512/1024 × halo 3 × 1/2-pass → outputs/ghost/fill_factorial_f1/)
  ③ webset 글로벌 보강 수집(stubble+5 등 → 아카이브/webset_cp6).
- **다음 절차 (새 세션 재개 시)**: 3개 결과 통합 → Sprint B 진입 조건 5 판정(Codex #14
  §Q6) → 채움 최선 조합 채택 → 필요 시 Codex #15(b2 편차 비준 + fine-tune go/no-go) →
  체크포인트 ⑥ (프로토콜: Codex #14 §Q7 로그 참조; 재료 = 실사 5 봉인 + webset_cp6
  17장(아이누 제외), 합격 기준 stubble·medium만, 1인 무기명 A/B).
- 마지막 커밋: cadfe81 (mask_eval v2 + maskv4_b3). 로그·메모리는 최신.

### 채움 factorial 완료 (2026-07-12, fill_factorial_f1 — 60조합, 오라클 마스크 순수 채움)

- **헤드라인 1 — 왁스의 주범은 512 캡, 처방 확정**: 큰 크롭(fw 600+)에서
  d1024+halo0.02fw+단일패스가 결 보존을 psd01 0.38→**1.19**, psd04 0.20→**0.99**로
  회복(1.0 = 원본 결과 동일). 색 조화도 최선(psd01 ΔL 5.7→2.2, Δab 0.3~0.9).
  작은 크롭(<512)은 어차피 scale 1.0이라 영향 없음(512와 바이트 동일) → **lamaMaxDim
  512→1024 + modelMaskDilate 0.004→0.02 채택이 안전**(큰 창만 시간 2배: ~5.7초/창).
- **헤드라인 2 — dense는 전 조합 붕괴**: img4574(수동 오라클 마스크, maskFrac 0.57)는
  60조합 어디서도 결 보존 0.055~0.17 + 과명도 ΔL +10~14 — 해상도·halo·2패스 전부 무효
  (크롭이 작아 해상도 무관, halo·2패스는 오히려 잔여 문맥을 지워 악화). **dense v1 제외
  결정을 데이터로 확정 + dense를 하려면 fine-tune 외 길이 없음의 직접 증거.**
- 보조 발견: ① 2패스는 결을 올리지만 작은 흐릿한 크롭에서 과질감(pic1 1.04→2.43 —
  원본보다 2.4배 날카로움 = 역방향 부자연) + psd04에선 결 저하(0.99→0.54) → 기본
  단일패스, 2패스는 채택 보류. ② halo 0.04fw는 줄무늬 악화(psd01 stripe 0.62→0.82).
  ③ pic1(흐릿한 원본)은 halo 최소+단일패스가 결 비율 1.04로 최적 — 원본 선명도에 따라
  적정 조합이 다름을 시사(추후 적응형 여지).
- 반영 계획: v4.1 착지 후 CONFIG 변경(1024/0.02) 커밋 → dev 재측정으로 회귀 확인.

### webset 글로벌 보강 완료 (2026-07-12)

- +15장 (전 인종 확장): stubble +6(어두운 톤 3 포함), medium +4(어두운 톤 3), 무수염
  +3(오바마 PD 초상 등, 어두운 톤 1), 현대 컬러 dense 프로브 +2(비차단). manifest 갱신.
- **웹셋 최종: 게이트 통과 36장** — none 8 / stubble 10 / medium 12 / dense 6(비차단).
  피부톤 다양성 확보(신규 배치 밝음4/중간4/어두움7) — black-hat 대비·톤별 오발화 측정
  가능. 체크포인트 ⑥ 재료 = 실사 primary 5 + 웹셋 36, 충분.

### v4.1 착지 + 채움 설정 채택 (2026-07-12, 커밋 6e578dd·8bb2950)

- **v4.1 (5에이전트, 리뷰→수정 루프 1회)**: ① b2 성분 이중신호 게이트 — 민낯 4564
  오발화 14,549→**0px**, 양성 pic3·psd03 리콜 손실 0.00pp. 정직 기록: 지시된 "형태학
  이전" 배치는 실측 psd03 −19.5pp로 반증 → post-close 성분 게이트로 이동(두 실측 모두
  코드 주석에). ② 전역 no-op(300px, 잠정) — 리뷰가 치명 1건 적발(양성 pic1 통째 억제)
  → 수정: **동결 규칙 A/B 발화가 잠정 질량 플로어를 오버라이드**(pic1 복원 18,273px/
  리콜 99.6, 민낯 0px 유지). ③ 어퍼처 5중 강건화+typed 사유(17장 캘리브레이션, 무손실).
  ④ 참조 typed abstain + 외과수정 2건(채도 sigma floor 1.5·클리핑 2단; **factorial로
  H1 확인 — 주 붕괴 인자는 bilateral보다 JPEG q30 크로마 서브샘플링**) — 4569 회복,
  4574는 코히런스 거절로 정정(용량 아님, 다음 라운드), 4559 미회복(실제 색 캐스트 —
  정직 미조치). pytest 189+3skip. 리뷰: okToCommit, b2 편차 정당(Codex #14는 개방형
  질의였다는 공정한 정정 포함), 수치 재현 소수점 일치.
- **maskv41_c1 (72장)**: negative 전원 편집 0px, real coverage kill 5/5, **Sprint B
  진입 조건 5개 전부 PASS** (④ 4573은 진짜 어퍼처 무접촉 확인 — 92px 잔존은 코 왼쪽
  스터블 그늘 성분의 우연 정렬). 열린 과제: perturb y+ 방향(어퍼처 ROI가 코를 벗어남,
  abstain 16/19)·순수 y± coverage 88~93%.
- **채움 설정 채택 (8bb2950)**: lamaMaxDim 512→1024, modelMaskDilate 0.004→0.02
  (factorial 판정). E2E 스모크(psd03): scale 1.0 단일창, LaMa 8.3s, 바이트 게이트 전부
  green, hard-fail 0, fillColor dAb50 9.2→4.3(합격선 3.0 근접). 189 tests green.
- **세션 절단점 도달**: 실행 중이던 3전선 모두 착지·커밋 완료. 남은 실행체: 체크포인트
  ⑥ 사전 준비 워크플로(annotation+프로토콜 — 완료 시 커밋 예정).

### 체크포인트 ⑥ 사전등록 완료 (2026-07-12)

- **annotation 잠금**: 실사 양성 3장(4572/4570/4575)의 수염 목표·입술·어퍼처 annotation
  제작(2회 검수, 침범 0 수치 검증), negative 2장은 editPx=0+byte identity 기준 명시.
  아카이브/webset_cp6/annotations/ — 판정 전 수정 금지.
- **CHECKPOINT6_PROTOCOL.md**: 재료 43장(실사 5 + 웹셋 sha256 결정론 선정 23 + 필터/압축
  변형 15), 블라인드 A/B(sha256+salt, 봉인 chmod 400), 1인 판정 rubric(dev 사진 앵커),
  **차단 기준 K1~K5**(negative 10/10 무편집·anatomy 0/30·stubble ≥12/15·medium ≥12/15·
  primary 5/5 처리), dense는 비차단 보고, 사후 변경 금지·소진 조항.
- **make_filter_variants.py**: 변형 3종 동결 파라미터(beauty/jpeg40/down06) + 봉인 재료
  가드(--checkpoint-build 없이는 거부), dev 사본으로 결정론·육안 검증.
- 남은 §6 절차: challenger(B=v4.1+1024, 커밋 8bb2950) 해시 기입 → manifest 커밋 →
  패널 생성 → **사용자 판정** → 개봉.

### 체크포인트 ⑥ §6-1~3 실행 완료 (2026-07-12, 커밋 7a7b072·478d56b·a4e3d61·8e5ab9b·26fba7c)

- **§6-1 동결**: A=cadfe81(mask v4/maskv4_b3 시점, lamaMaxDim 512·dilate 0.004),
  B=8bb2950(v4.1+채움 factorial, 1024·0.02), 가중치 sha 344c77bb…, venv 해시
  3003eaad…(python 3.12.4). 실행기 `spike/checkpoint6.py` — §6 전 절차 + served/noop/
  abstain 어휘 + K1~K5 집계 로직을 결과 실행 **전에** 이 파일에 동결(비-pass=미출고
  서빙 계약 준수). dev 스모크(pic3·4564)로 기계 검증 후 홀드아웃 첫 접촉은 §6-3b.
- **§6-2/3a 봉인**: 소스 28장 + 변형 15장(결정론 §2.2/§2.3 선정 재계산 일치 확인)
  sha256 봉인 — `cp6_run/image_manifest.json`. rubric 앵커 4종(dev 전용) 판정 전 확정.
- **§6-3b/c**: 43장×2팔 실행, 33 블라인드 패널(`cp6_run/panels/`), 매핑 봉인
  (`blind_mapping.sealed.json` chmod 400), 팔 디렉터리·드라이버 로그 chmod 000.
- **공개 사실(좌우 대칭 — 블라인드 무손상)**: **33/33 패널이 양측 ABSTAIN**.
  두 팔 모두 전 차단 양성·dense 프로브에서 미출고. dev pic3에서 사전 관측된 E2E
  품질 게이트(grain/fillColor/ghost) 전면 abstain 거동과 일치. negative 10장의 기계
  측정(K1/R3)은 봉인 상태 — 개봉 시 확정.
- 프로토콜 일탈 기록(정직): 빌드 진행 점검 중 A팔 드라이버 로그 일부(스터블 5장
  상태 문자열)가 세션 채팅에 노출됨. 전 패널이 양측 ABSTAIN(좌우 대칭)이어서 사이드
  식별 정보량 0 — 판정 오염 없음으로 기록. 이후 팔 식별 로그 즉시 봉인.

### 체크포인트 ⑥ 결과: 불합격, B 기각 (2026-07-12, 개봉 커밋 da9edf8)

- **판정 단계 처리(일탈 기록·정직)**: 33/33 패널이 양측 미출고(ABSTAIN)로 §5
  체크리스트의 판정 가능 셀이 0개 — 1인 판정자(사용자)가 비교할 결과 이미지가 어느
  사진에도 존재하지 않았다. §8의 동결 규칙(B팔 abstain = K3/K4 자동 탈락)이 판정
  내용과 무관하게 전 판정 대상의 귀결을 이미 정의하므로, **판정 단계를 "공집합에서
  자명 완료"로 기록하고 사용자 입력 없이 개봉**했다(judgments.json 33행 전량 강제
  tie·비인간판정 명기). 사용자 판정이 담을 수 있었던 정보량은 0 — 어떤 인간 판정도
  아래 결과를 바꿀 수 없었다.
- **게이트 원자료 (합격선: K1 10/10 · K2 0 · K3 12/15 · K4 12/15 · K5 5/5)**:
  **K1 1/10 FAIL**(4565만 clean no-op; 4561 및 웹셋 negative 5·변형 3은 abstain —
  출력 없음 = byte identity 불성립, 편집 오발화로 인한 실패는 0장) ·
  **K2 PASS(자명 — B 출력 0장이라 anatomy 훼손 관측 불가)** ·
  **K3 0/15 FAIL · K4 0/15 FAIL**(B 전량 미출고 → 자동 탈락) ·
  **K5 1/5 FAIL**(4570/4572/4575/4561 abstain, 4565만 처리). **종합 불합격 → §9에
  따라 B(v4.1+1024, 8bb2950) 기각. frozen baseline 교체 없음.**
- **실패 구조 분해 (B팔 43장)**: 사전(채움 이전) abstain 28장 — reference bundle 17
  (forehead_verify_rejected 11 / insufficient_face_resolution 3 / coherence_rejected 3)
  + aperture 검출 거절(lower_face_uncertain) 11. 채움 후 품질 게이트 abstain/hardfail
  14장(fillColor·ghost·brightBand 조합). clean no-op 1장. **즉 주 실패 지점은 채움
  품질이 아니라 입구 게이트(reference·aperture)의 홀드아웃 도메인 일반화 실패** —
  dev 17장 캘리브레이션(Codex #14 Q4)이 웹셋/실사 홀드아웃의 해상도·조명·각도
  분포를 커버하지 못함. A팔도 동일 구조(reference 17 + aperture 9 + 게이트 17,
  no-op 0).
- **R 시리즈**: R1 dense 프로브 3장 전량 양팔 미출고(원자료 verdict.json) — "dense
  미지원, v1 범위 밖" 서사 유지, dense 일반화 미입증 명기. R2 순수 개선 0/30(비교
  성립 사진 0). **R3(A팔 negative)**: abstain 7 + hardfail 3 — hardfail 3장은 A
  마스크가 negative에서 실제 발화해 채움까지 진행(기지 maskv4_b3 오발화 계열 재현).
  B는 negative 발화 1장(none_wikimedia_02, 게이트가 차단) — 오발화 자체는 A 대비
  감소했으나 K1 기준(무편집 no-op)에는 양팔 모두 미달.
- **소진 조항 상태**: 개봉 완료 — 이후 코드·모델·기준·rubric 수정 시 본 세트(실사
  5 + 웹셋 28)는 dev 강등(§7). 실패 원인 분석은 dev 재료로만(§9), 홀드아웃 재실행
  금지. 다음 체크포인트(⑦)는 신규 재료로만.
- **다음 진단 과제(dev로만)**: ① reference bundle의 forehead_verify/coherence 거절이
  왜 웹 도메인에서 대량 발생하는지 dev 웹사진 재현 ② aperture 강건화 게이트의 도메인
  민감도 측정 ③ 품질 게이트(fillColor 합격선 3.0 등)의 현실적 달성 가능성 재검토 —
  세 겹 게이트가 모두 열려야 출고되는 현 구조에서 홀드아웃 통과율 1/43이 데이터로
  확정됐으므로, LaMa 얼굴 fine-tune 이전에 **입구 게이트 재설계가 선행 차단 과제**.

### §7 소진 발동 선언 (2026-07-12, 체크포인트 ⑥ 개봉 후 첫 코드 수정 착수)

- 사용자 지시("3차 시험까지 진행")에 따라 입구 게이트 수리를 시작한다. 이 수정으로
  §7이 발동되어 **cp6 사용 세트 — 실사 primary 5장(4561/4565/4570/4572/4575) +
  선정 웹셋 23장 + 변형 15장 — 는 홀드아웃 자격을 영구 상실, dev로 강등**된다.
  이후 이 43장은 게이트 재설계의 캘리브레이션/측정 재료로 자유 사용.
- **보존(무접촉 유지)**: cp6에 선정되지 않은 webset_cp6 풀 — none 3(04·unsplash_01·
  g03), stubble 2(04·05), medium 2(02·03·g01 중 gateOk 잔여), spare/cand 격리분,
  아이누 4(참고용) — 은 파이프라인 미접촉 상태로 체크포인트 ⑦ 풀 후보 자격 유지.
  ⑦ 재료는 신규 수집(정면 셀피 도메인 매칭)으로 주력 조달한다.
- 체크포인트 ⑦ 팔 계획(사전등록에서 확정): A(기준선)=8bb2950(cp6의 B, 기각됐지만
  마지막 동결 후보), B(도전자)=입구 게이트 수리본. 판정자 1인 체제 유지.

### 입구 게이트 수리 라운드 (2026-07-12, 커밋 78eeb0c — cp6 postmortem 이행)

- **수리 3+1건 (전부 실측 근거를 상수 주석에 동결)**: ① reference Nmin 상한 1024
  (제곱 폭주 제거; dev9는 플로어 768 지배라 무영향) ② grace band [0.9N, N) —
  기각 절벽을 기록되는 low_conf 통과로(grace_band 필드 신설) ③ coherence 기각에
  에너지 조건(grain RMS>3.0) — beauty/다운스케일 매끈화 피부를 방향성 결함으로
  오독하던 것 교정(경계 실측: 매끈화 1.9~2.1 vs 실제 방향성 텍스처 4.5~17.8)
  ④ aperture 톤 적응 폴백 — 베이스 파이프라인 실패 시에만 가동(회귀 구조적 0):
  ref=max(median, p80−12)·thr=max(ref−40, 0.45ref)·cap 0.085fw·ratio≤10,
  toneFallback 기록. 근거: g02(어두운 톤 정면)는 ROI 중앙값 L=11로 median−40이
  음수 — median이 피부라는 전제 자체가 붕괴한 사례.
- **실측**: 강등 43장 입구 통과 **15→29** (reference abstain 17→6, aperture가
  지배 병목으로 노출→폴백 후 10). 잔여 14 = 3/4 포즈·마이크 가림(도메인 밖) 5 +
  실사 해상도 3(fw≈150~200, 촬영게이트 fw≥320 소관 — 의도된 유지) + 슬릿/병합
  기하 6. dev 회귀: pic1 편집 18,279px(기존 18,273, +6px), pic3 경로 동일,
  4564/4565 no-op 유지. pytest 195(신규 6 포함).
- **정직 노출 — 마스크 층 rule-B 오발화**: 입구가 열리자 웹셋 negative 8/10에서
  mustache rule-B(텍스처 성분)가 민낯에 발화(4561은 rule-A도 발화, coincident
  2019px — CLIPSeg가 턱 그늘을 semantic으로 읽음). **출고는 0**(품질 게이트
  전량 차단 — 안전은 현재 게이트 미출고가 담당, 마스크 no-op 아님). dev 민낯
  4564/4567 기준의 negative-kill이 웹 도메인 민낯에 일반화되지 않음이 확정 —
  체크포인트 ⑦ K1 설계는 "출고 0 차단 + no-op율 보고"로 잡아야 하고, 마스크
  수준 negative-kill 일반화는 별도 수리 과제로 등재.
- **다음 병목 예고**: 입구를 열어도 E2E 품질 게이트(grain/fillLift/brightBand/
  fillColor/ghost)가 dev 양성 pic1·pic3 포함 대부분을 여전히 abstain —
  ⑦ 설계에서 "판정자에게는 원시 채움을 보여주고(게이트 표기 병행), 서빙 계약
  평가는 별도"로 분리해야 채움 품질의 첫 인간 측정이 성립한다.

### 체크포인트 ⑦ 사전등록 (2026-07-12, 커밋 562e737·61b6d59·c79140b)

- **재료 (webset_cp7, 신규 24장 전량 Wikimedia Commons CC BY/BY-SA)**: stubble 7 /
  medium 9 / none 8 + 변형 15 = **39장** (분모 stubble 13·medium 15·negative 11).
  큐레이션 게이트 = 제품 촬영 게이트 프록시(단일 얼굴·정면 asym≤0.10fw·fw≥340·
  현대 컬러·콧구멍 가시·인물 중복 0, detect_face 측정만 허용). 수집 과정: 커먼즈
  검색+카테고리 423후보 → 기계 선별(정면성·fw) → 육안 큐레이션 3라운드 →
  촬영게이트 실측 재검. **cp6 무접촉 이월 후보 4장은 정면성(asym 0.22~0.36)에서
  전원 탈락** — cp6 웹셋이 제품 도메인보다 가혹했다는 postmortem 결론을 재확인.
  st101은 밀도 정직 재배정(stubble→medium), 옅은 콧수염 의심 1장은 negative에서
  제외(안전).
- **설계 차이 (CHECKPOINT7_PROTOCOL.md §0)**: ① 판정 대상 = **원시 채움**(품질
  게이트와 무관하게 제시; cp6의 전패널-ABSTAIN 퇴화 방지) + 게이트 verdict는 별도
  축으로 기록해 R2 "게이트 vs 인간" 교차표 확보 ② 차단 = B 절대 사용성(usable),
  비교 3항목은 회귀 감지 보고 축 ③ dense·실사 primary 없음(소진).
- **팔**: A=8bb2950(cp6 기각본), B=78eeb0c(입구 게이트 수리본) — 채움 로직 동일,
  차이는 입구 게이트뿐. **차단 기준**: K1 negative 서빙 사고 0/11 · K2 anatomy 0 ·
  K3 stubble usable ≥7/13 · K4 medium usable ≥8/15 · K5 채움 생산 ≥22/28.
  해석 약속: K3/K4 실패 = LaMa fine-tune go의 직접 근거(§8).

### 체크포인트 ⑦ 결과: 불합격 (2026-07-12, 판정자 판정 완료·개봉)

- **판정(사용자 1인, 채움 8패널)**: "턱 모양·입 모양 다 바뀌었고 좌우 우열은 없다"
  — usable 0장, 전 항목 tie, **B 채움 8/8 anatomy 훼손**. 나머지 20패널은 양측
  미출고 강제 기록.
- **게이트 원자료**: K1 PASS(negative 서빙 사고 0/11 — 유일 통과; A팔 negative는
  11/11 abstain) · K2 FAIL(훼손 8/8) · K3 FAIL(usable 0/13) · K4 FAIL(0/15) ·
  K5 FAIL(채움 생산 8/28, 합격선 22). **종합 불합격.**
- **R2 게이트-인간 정합 8/8 (핵심 수확)**: B 채움 전부를 품질 게이트도
  기각(hard-fail 5·abstain 3)했고 인간도 기각 — passUsable=0, 불일치 0.
  **품질 게이트는 과보수가 아니라 정확했다.** cp6의 전패널-ABSTAIN도 게이트가
  인간 판정을 옳게 대리한 것. "게이트 합격선 재캘리브레이션" 과제는 우선순위
  하향, **채움 자체의 품질(LaMa 얼굴 fine-tune 또는 채움 접근 교체)이 유일한
  전진 경로임이 인간 데이터로 확정**(§8 사전 약속 이행).
- **R1 회귀 0**: A승 사진 0 — 게이트 수리로 인한 채움 회귀 없음(B 채움 8 ⊃ A 채움 4).
- **입구 게이트 2차 실패**: 큐레이션된 정면 신규 재료에서도 생산율 8/28 —
  강등 세트 기준 수리(15→29/43)가 신선 표본에 일반화되지 않음. 강등-세트
  캘리브레이션 과적합. 아키텍처 수준 재설계(앵커/어퍼처 검출을 임계 튜닝이 아닌
  구조적으로) 필요 — 단, 채움이 미달인 동안 입구만 열어봐야 K2/K3/K4는 못 넘는다.
- **§7 상태**: ⑦ 개봉 완료 — 이후 수정 시 webset_cp7 24장 dev 강등. ⑧은 신규
  재료로만.
- **전략 귀결(§8 해석 약속)**: ① **LaMa 얼굴 fine-tune go** — 인간 판정 근거 확보
  (훼손 8/8·usable 0). 데이터 파이프라인 설계는 딥리서치 결론 승계(역방향 페어,
  법무 리뷰 blocking). ② 입구 게이트 아키텍처 재설계는 fine-tune과 병행 가능한
  독립 트랙. ③ 다음 체크포인트는 fine-tune 착지 후 신규 재료로.
