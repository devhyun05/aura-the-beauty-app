# RFC: 아우라딘 의도해석 — 룰-퍼스트 유지 + LLM 보조 해석의 단계적 도입

- 작성: 2026-07-15. 기반: `아우라딘_추천시스템_고도화_종합보고서.md`(승인본, 이하 "종합보고서") + 실코드 검증
- 증거 등급: [확인]=코드/실행으로 확인, [강한 추론]=코드 구조에 근거한 추론, [실험 필요 가설]=측정 전 미확정
- 검증 커밋: `7724e1c249f8` (종합보고서 부록 B와 동일 기준)

---

## 1. 결정 (Executive Decision)

**LLM-퍼스트 전환(Option 2)은 기각한다. "룰 정본 + LLM 보조 해석"(Option 3 변형)을 조건부 채택한다.**

1. **A7 룰 수정이 무조건 선행이다** — 가격 방향·부정·가격대는 어떤 아키텍처에서도 결정론 코드가 정본이어야 하므로(§3), 이 수정은 LLM 도입 여부와 무관하게 필요하고, 현 실패 사례 8종 중 3종을 그것만으로 닫는다.
2. LLM은 **하드 필터를 절대 생성하지 못한다.** 소프트 선호 제안·오탈자/구어 정규화·미해석 스팬 신고만 허용하며, 전 필드에 원문 인용 스팬 + enum allowlist + 결정론 검증을 강제한다.
3. 도입은 **섀도 모드부터**(Phase 1). 활성화(Phase 3+)는 실측 게이트(하드 제약 위반 0건, 날조 필드 <0.5%, 불일치 분석 완료) 통과 조건부 — 현재는 쿼리 분포 데이터가 없어(로깅 부재) 활성화 시점의 기대 효과가 [실험 필요 가설]이기 때문이다.
4. LLM 불가용 시 시스템은 **현행 룰 경로로 무손실 동작**한다(구조적 보장 — LLM은 가산 레이어).

한 줄 판정: *"Raw prompt → LLM → rules"가 아니라 "Raw prompt → rules(정본) + LLM(보조·검증됨) → rules"이다.*

---

## 2. 현행 아키텍처 진단 (실코드 기준)

[확인] 구계획 문서가 아닌 실제 구현:

- **의도 파싱은 100% 룰**: `intent_parser.parse_intent`([intent_parser.py:156](services/backend/app/services/auradin_agent/intent_parser.py#L156)) — substring 매칭 + 가격 regex. LLM은 의도 경로에 전혀 없음(`parse_intent_optional`류는 계획 문서에만 존재).
- **실측 실패 4종** (종합보고서 F10, 재현 스크립트 `reports/auradin/recsys_review_evidence_20260714/verify_codex_claims.py`): "2만원 이상"→`lte 20000`(방향 반전), "1만원대"→`lte 10000`, "립 말고 블러셔"→립 잠금, "매트는 싫고 글로시"→둘 다 positive.
- **LLM 인프라는 이미 존재하되 카피 레이어에 격리**: `BedrockReasonCopyClient`/`BedrockQuestionCopyClient` + **충실성 게이트**(발명 라벨 검출 시 전량 리젝, [enrichment.py:84-110,392-437](services/backend/app/services/auradin_agent/enrichment.py#L84-L110)) + settings 게이트 + graceful fallback. → 본 RFC의 검증 패턴은 신규 발명이 아니라 이 관례의 재사용이다.
- **지연 허용 구조**: 세션 생성 API는 이미 enrich(상한 12s)를 await한 뒤 응답하고, 모바일은 350ms 폴링으로 phase 전환을 기다린다([session_manager.py:705-731](services/backend/app/services/auradin_agent/session_manager.py#L705-L731)). → LLM 파싱 +0.5~1.5s는 **아키텍처가 이미 흡수 가능한 예산** [확인].
- **되묻기 질문 엔진이 네이티브 존재** — "모호하면 사용자에게 묻는다" 폴백이 신규 UI 없이 구현 가능 [확인].
- **검증 allowlist가 극소형**: enum 37개(카테고리 6, 색 11, 마감 5, 제형 7, 톤 3, 강도 3, occasion 2) + 브랜드 17 [확인 — 실행 집계]. 스키마 검증·환각 차단 난도가 낮다.
- **제약 조건**: §9 정직성 불변식(LLM은 후보·필터·속성 발명 금지), 골든 스위트(재현성), 세션 멱등화(A9 예정).
- **중요한 상한**: 카탈로그 속성 커버리지(색 17.5%, intensity 5.2%, occasion용 suitableFor 희소). "꾸안꾸"를 완벽히 해석해도 매칭할 속성 데이터가 빈약하다 — **해석 품질 개선의 기대 효과는 데이터 커버리지에 의해 절단된다** [확인 — 종합보고서 §2].

### 실패 사례 8종의 정직한 귀속 (적대적 핵심)

| 실패 클래스 | A7 룰 수정으로 해결? | LLM 우위? | 비고 |
|---|---|---|---|
| 가격 방향(이상/초과) | **완전 해결** | 불필요 | 고위험 — 룰 정본이어야 함 |
| "1만원대"/범위 | **완전 해결** | 불필요 | 〃 |
| 단순 부정("말고/빼고/싫어") | **대부분 해결** (절 단위 규칙) | 경계 사례만 | 〃 |
| 복합 부정("글리터 강한 건 말고 은은한") | 부분 해결 | **우위** | 스팬 판단 필요 |
| 오탈자·띄어쓰기 | 미해결 | **우위** | 정규화로 룰 재활용 가능 |
| 구어·슬랭("꾸안꾸") | 미해결 | **우위** | 단, 효과는 커버리지 절단 |
| 상대 가격("학생도 살 만한") | 미해결 | **우위** | soft priceTier 힌트로만 |
| 무드·occasion | 미해결(어휘 2종뿐) | **우위** | 〃 커버리지 절단 |

→ 전환 동기 8종 중 3종은 룰로 즉시·안전하게 닫히고, LLM 고유 가치는 4~5종이며 그중 2종은 데이터 상한에 걸린다. **이것이 "LLM-퍼스트 기각, 보조 채택"의 정량 근거다.**

---

## 3. 옵션 비교

| | 언어 커버리지 | 결정론 안전 | 지연 | 비용 | 구현 복잡도 | 테스트 용이성 | 폴백 품질 | 운영 리스크 |
|---|---|---|---|---|---|---|---|---|
| **O1 현행 룰-퍼스트(+A7)** | 낮음~중 | **최상** | 0 | 0 | 최저 | 최상 | 해당 없음 | 최저 |
| **O2 LLM-퍼스트** | 최상 | 낮음(검증해도 크리티컬 패스 의존) | +0.5~1.5s 전 질의 | 전 질의 과금 | 높음 | 낮음(캐시/목 필수) | LLM 다운=전면 룰 폴백 이중화 필요 | **높음** |
| **O3 룰 정본 + LLM 보조(잔여 스팬)** | 높음 | **높음**(하드=룰 전속) | 어려운 질의만 +α | 어려운 질의만 | 중 | 높음(LLM off 기본) | **구조적**(가산 레이어) | 낮음~중 |
| **O4 병렬 + 충돌 해소** | 최상 | 높음 | 전 질의 +α | 전 질의 | 높음 | 중 | 좋음 | 중(상시 이중 유지비) |
| **O5 LLM 제안 + 룰 검증** | 최상 | 중~높음 | 전 질의 +α | 전 질의 | 높음 | 중 | 룰 재파서 필요=O1 중복 | 중 |

판정: **정상 상태 = O3**, **섀도 기간(Phase 1-2)만 O4의 병렬 비교를 계측 용도로 사용**. O5는 "고위험 조건의 룰 재파싱"이 결국 A7 파서 전체를 요구하므로 O3 대비 이점 없이 전 질의 비용만 추가 [강한 추론]. O2는 다음 이유로 기각: ① 단순 질의(골든 9종 전부가 이 부류)에 불필요한 지연·비용·비결정성 부과, ② 하드 제약의 권위가 크리티컬 패스의 확률적 컴포넌트에 얹힘(검증 실패 시 폴백 복잡도), ③ 동일 질의 비일관성이 골든 재현성 계약과 충돌(캐시로 완화 가능하나 캐시 미스 시 잔존).

---

## 4. 권고 목표 아키텍처

```text
Raw prompt
  │
  ├─① 결정론 룰 파서 (A7 확장: 방향·범위·절단위 부정·스팬 소비 추적)   ← 하드 필터의 유일한 생산자
  │     └→ lockedFilters(hard) + softPreferences(rule) + consumedSpans + residualSpans
  │
  ├─② 트리거 판정 (결정론): residual에 내용어 잔존? or 부정 인접 모호? or broad+장문?
  │     ├─ NO → LLM 호출 없음 (현행과 동일 경로·지연 0)
  │     └─ YES → ③
  │
  ├─③ LLM 구조화 해석 (Bedrock, temp 0, 타임아웃 2.5s, 가드레일)
  │     └→ StructuredIntentProposal (스키마 §5) — soft 제안 + 정규화 + ambiguities + unresolved
  │
  ├─④ 결정론 검증기
  │     ├─ enum allowlist(37+17) / 스팬 존재성(원문 substring) / 필드 화이트리스트
  │     ├─ 고위험 필드(가격·부정·카테고리·채널·브랜드제외): 룰 재파스와 일치 시에만 채택,
  │     │   불일치 시 [폐기 or 소프트 강등 or 되묻기 질문 후보]
  │     └─ 통과분만 softPreferences(source="llm") 병합 — mode/locked 키 미부여(리포트와 동일한 구조적 hard 차단)
  │
  ├─⑤ 시맨틱 질의 직렬화 (결정론): 검증된 positive 조건 + 부정 스팬 제거된 residual
  │
  └─⑥ 이후 전 단계 현행 유지: 하드필터 → 랭킹(R2) → floor → MMR → 3역할 → 근거 → 질문엔진
        └ ambiguity가 가격/카테고리면 질문엔진에 결정론 clarification 템플릿 주입
```

핵심 불변식: **LLM 출력이 후보 집합을 줄이는 경로는 존재하지 않는다**(hard 필터 생산 불가 — `report_profile.py`가 mode/locked 키 자체를 방출하지 않아 `apply_hard_filters`에 절대 닿지 않는 것과 동일한 구조적 차단 [확인]).

---

## 5. 구조화 의도 스키마 (StructuredIntentProposal)

```jsonc
{
  "schemaVersion": 1,
  "normalizedPrompt": "촉촉한 립 2만원 이하",      // 오탈자·띄어쓰기 정규화 (룰 2차 패스 입력용)
  "fields": [
    {
      "attribute": "finish",                      // allowlist: category|priceTier|channel|colorFamily|
                                                  //  finish|texture|intensity|undertone|occasion|brand
      "values": ["glossy"],                       // 해당 attribute의 enum allowlist 값만
      "avoidValues": [],                          // 부정·제외 대상
      "proposedMode": "soft",                     // "soft" | "hard_suggest" (hard 확정 권한 없음)
      "confidence": 0.86,                         // 0~1
      "evidenceSpan": "촉촉한",                    // ★원문 verbatim substring 필수 — 서버가 in-검사
      "note": "촉촉→glossy 매핑"
    }
  ],
  "priceHint": {                                  // 상대·모호 가격 전용 — 숫자 연산자 생성 금지
    "tier": "under_15k", "confidence": 0.6, "evidenceSpan": "학생도 살 만한"
  },
  "ambiguities": [                                 // 되묻기 후보
    { "kind": "price_direction", "span": "3만원쯤", "options": ["이하", "근처"] }
  ],
  "unresolvedSpans": ["꾸안꾸느낌으로다가"],        // 해석 포기 선언 (침묵 금지)
  "injectionSuspected": false                      // 지시문 혼입 감지 자가 신고 (보조 신호)
}
```

**서버 검증 규칙 (결정론, 순서 고정)**:

1. JSON 파싱 실패/schemaVersion 불일치 → 전량 폐기(부분 적용 금지 — `question_copy_is_faithful`의 전량 리젝 관례와 동일).
2. `attribute`·`values`·`avoidValues`가 allowlist 밖 → 해당 필드 폐기 + `fabricated_field` 카운트.
3. `evidenceSpan`이 원문(정규화 전) substring이 아니면 → 필드 폐기 + `unsupported_span` 카운트. **스팬 없는 필드는 존재할 수 없다.**
4. 고위험 필드(§6): `proposedMode="hard_suggest"`여도 룰 재파서와 (attribute, op, 값) 일치할 때만 hard로 실체화 — 불일치 시 가격·카테고리는 **폐기**(오적용 비용 최대), finish/texture/색은 **soft 강등**, 브랜드 제외는 **되묻기 후보**.
5. `confidence < 0.5` → soft여도 폐기. `0.5~0.7` → weight 0.5 하향 병합. `≥0.7` → weight 0.75.
6. 같은 attribute에 룰 파서 산출물 존재 → LLM 필드 억제(§7 위계).
7. 통과 필드는 `{source:"llm", weight, confidence, values, avoidValues}`로 softPreferences 병합 — **mode/locked 키 미부여**.
8. `priceHint`는 priceTier soft 선호로만. 숫자 `priceKrw` 연산자는 룰 전속.
9. 결측·전부 폐기 → 현행 룰 결과 그대로 (LLM 기여 0으로 퇴화).

---

## 6. 고위험 조건 처리 (질문 3에 대한 직답)

| 조건 | 처리 | 근거 |
|---|---|---|
| 가격 부등호(이상/이하/초과/미만/부터/까지/넘는) | **룰 단독 파싱**(A7). LLM 제안은 룰 일치 검증 실패 시 폐기 | 오적용=결과셋 오염, 룰이 잘함 |
| "1만원대"/"3만원쯤" | 룰: 범위 [N만, N+1만)·근처 ±20%. 모호("쯤") 시 **되묻기**(가격 질문 템플릿 재사용) | 〃 |
| 상대 가격("완전 비싼", "학생도") | LLM → priceTier **soft** 힌트만 | 숫자화는 날조 위험 |
| 부정·제외(말고/빼고/제외/싫어) | 룰: 절 단위 스팬 규칙(A7). 복합 문장은 LLM이 avoidValues 제안 → 룰 스팬 검증 | F10 재현 사례 |
| 카테고리 치환("립 말고 블러셔") | 룰: 부정 스팬 내 카테고리어는 잠금 제외. LLM 불일치 시 폐기 | 카테고리 오잠금=전체 오염 |
| 브랜드 제외("이 브랜드 빼고") | 브랜드 17종 allowlist라 룰 가능. 랭커에 brand attribute 지원 추가 필요(현재 미지원 [확인 — ranking `_item_value`에 brand 부재]). 모호 시 되묻기 | 소어휘 → 룰 적합 |
| 성분 회피 | **현재 카탈로그에 성분 데이터 없음** → 정직 안내("성분 정보는 아직 다루지 않아요") — 해석 이전에 데이터 부재 [확인] |
| 상호 모순 조건("촉촉한 매트") | 룰: 동일 attribute 상충 감지 → 되묻기(finish 질문 템플릿). LLM ambiguity 신고는 보조 신호 |
| "둘 다 괜찮아/상관없어" | 현행 noop 관례 유지 — LLM이 이를 선호로 승격 금지 |
| "너무 쨍하지 않은/살짝 촉촉한" | LLM → intensity/finish soft + avoidValues(bold). 커버리지 절단 명시 |

**저신뢰 시 공통 규칙**: 하드 후보는 절대 하드로 넣지 않는다. 선택지는 ①soft 강등 ②폐기 ③되묻기 셋뿐이고, 결과에 영향이 큰 순서(카테고리>가격>채널>브랜드)일수록 되묻기를 택한다.

---

## 7. 결정론 유지 목록 · 소스 위계 (질문 4·7)

**LLM 개입 금지 (전원 현행 유지)**: 카탈로그 조회, 구매가능 게이트, 하드 필터 적용(`apply_hard_filters`), 권한/소유자 검사, 가격 비교, 라이브 오퍼 검증, 점수 계산(R2 포함), 선호 소스 위계, floor 게이트, MMR, 역할 배치, 카드 구성, 구조화 근거 생성, 결과 검증. 이유: 전부 ⓐ재현성·테스트의 근간이거나 ⓑ§9 불변식의 집행 지점이거나 ⓒLLM이 잘할 이유가 없는 산술이다. LLM 허용 지점은 **의도 제안(검증됨)·카피 재작성(기존)·수집 시점 속성 추출(별도 파이프라인)** 셋뿐.

**소스 위계 (충돌 시 상위 승, 같은 attribute 하위 억제)**:

```text
① 명시 하드 (prompt/refine의 룰-검증 locked + 질문 hard 답변)
② 질문 soft 답변 · refine soft
③ 룰 파서 soft (prompt 유래)
④ LLM 검증 통과 soft (같은 발화 유래지만 확률적 → 룰 아래)      ← 신설
⑤ 분석 리포트 (undertone, cap_report 0.02)
⑥ 장기 프로필 (profileScore 분리 — preference 축 밖, anchor 전용)
```

제시된 위계(Explicit > deterministic > LLM soft > report > profile)와 본질 동일하며, 유일한 정밀화는 ③/④ 분리 — **같은 원문에 대한 룰 해석과 LLM 해석이 충돌하면 룰이 이긴다**(정밀도 우위 + 디버깅 가능성). LLM④가 리포트⑤보다 위인 근거: 현재 발화가 과거 진단보다 세션 의도를 더 직접 반영한다 [강한 추론 — R2 위계 원칙의 연장].

### 임베딩의 역할 (질문 5)

- 임베딩이 하는 것: 어휘 불일치를 넘는 후보-질의 유사(“물먹은 입술” ↔ 글로시 계열 설명).
- 임베딩이 못 하는 것: 연산자("2만원 이상"), 부정(“매트는 싫고”가 매트를 **끌어당김** — 현행 `build_query_text`가 부정 미구분 직렬화라 실제로 일어나는 결함 [확인 — 종합보고서 F16 인접]), 하드 제약.
- **정본 직렬화 권고 (하나만)**: *검증된 positive 조건의 결정론 직렬화 + 부정 스팬을 제거한 residual 원문*. 원문 전체 사용(부정 오염)도, LLM 정규화문 단독(비결정·검증 불가)도 기각. LLM 기여는 검증 통과 필드가 직렬화에 포함되는 형태로만 간접 반영.

---

## 8. 실패·폴백 상태기계 (질문 6)

```text
[트리거 아님] ──────────────────────────────→ 룰 경로 (LLM 0회 — 기본 상태)
[호출] ─ 타임아웃(2.5s)/프로바이더 다운 ────→ 룰 결과로 진행 + logs.llmParse="timeout|unavailable"
       ─ JSON 파싱 실패/스키마 불일치 ──────→ 전량 폐기, 룰 결과 (재시도 0회 — 서빙 경로)
       ─ enum 밖/스팬 불일치 필드 ─────────→ 해당 필드만 폐기 + 계측 카운트
       ─ 고위험 필드 룰 불일치 ────────────→ §6 표 (폐기/강등/되묻기)
       ─ 모순 필드 ───────────────────────→ 되묻기 후보 or 폐기
       ─ 저신뢰 전체 ─────────────────────→ 룰 결과
       ─ injectionSuspected or 지시문 패턴 ──→ LLM 출력 전량 폐기 + 룰 결과 (프롬프트는 데이터로만 취급,
                                              시스템 규칙 변경 시도는 스키마에 표현 자체가 불가능)
       ─ 동일 질의 상이 출력 ───────────────→ temp 0 + (정규화 프롬프트 키) 응답 캐시로 완화;
                                              잔존 편차는 골든에서 LLM off로 격리
[반복 네트워크 요청] ──────────────────────→ 세션당 LLM 파싱 1회(생성 시)·캐시 — A9 멱등화와 정합
```

폴백 정본 = **현행 룰 파서** (별도 "최소 안전 파서" 불필요 — 룰 파서가 이미 그 역할). 되묻기는 폴백이 아니라 §6의 1급 처리 경로. 광역 검색 폴백은 룰 파서의 broad 판정이 이미 수행.

---

## 9. 저장소 변경 계획 (질문 10 — 그린필드 금지)

| 위치 | 변경 | 비고 |
|---|---|---|
| `intent_parser.py` | A7(방향·범위·절단위 부정) + **consumedSpans/residualSpans 반환** | 기존 함수 유지 — 폴백이자 검증기 겸임 |
| 신규 `intent_llm.py` | `BedrockIntentClient` + `build_intent_client(settings)` — enrichment 클라이언트 관례 복제(게이트: `auradin_llm_intent_enabled=False` 기본, 타임아웃 2.5s, 가드레일 kwargs) | 테스트는 기본 off → 골든 무영향 |
| 신규 `intent_verifier.py` | §5 검증 규칙 1~9 (순수 함수 — 단위 테스트 대상) | |
| `session_manager.create_session*` | 파싱을 `*_persisted`(async)에서 오케스트레이션: 룰 → 트리거 → (섀도: 병렬 로깅만 / 활성: 검증 병합) | 세션 멱등화(A9)와 동일 파일 — 파싱 1회 보장 |
| `retrieval_service.build_query_text` | §7 정본 직렬화(부정 스팬 제거) | LLM 무관하게 개선 |
| `ranking._item_value` 등 | brand attribute 지원(제외용) | 소규모 |
| `question_engine` | 결정론 clarification 템플릿 2종(가격 모호·모순 조건) — ambiguity를 gain 루프 밖 우선 질문으로 | 구조(id/filterDelta) 관례 준수 |
| 세션 state | `llmIntent`(원출력+검증 결과), `intentSource`, `parseDisagreements` — logs에 포함, postgres 직렬화 자동 | API 계약 무변경 |
| `auradin_events` | 기존 A5 스키마의 payload로 수용(`event_type='parse_shadow'`는 CHECK enum에 추가) | |
| 테스트 | ① A7 회귀 행렬(종합보고서 §15), ② verifier 단위(스팬/enum/고위험 불일치), ③ 섀도 불일치 fixture, ④ 골든은 LLM off 그대로 + LLM on 별도 스위트(캐시 응답 고정) | |

`lockedFilters` 생산자는 변경 후에도 룰 파서 하나뿐 — `softPreferences`에 source="llm" 항목이 추가될 뿐이며 R2의 소스 위계·cap이 이미 이를 수용하도록 설계됨(종합보고서 §13). refine 경로는 동일 파이프(파서 교체 없음), 질문 답변 경로 무변경.

---

## 10. 오프라인 실험 계획 (질문 8·9)

- **질의셋**: 500문 (정상 120 / 부등호·범위 60 / 부정·제외 60 / 오탈자·띄어쓰기 50 / 슬랭·구어 50 / 불완전 30 / 모호 가격 40 / 모순 20 / 다중 카테고리 20 / 무드·occasion 30 / 적대·인젝션 10 / 초장문 10). 라벨: 필드별 정답 intent(2인 독립 라벨 + 불일치 중재), 커밋·모델·프롬프트 버전 고정, **응답 캐시 저장으로 리플레이 가능**.
- **비교군**: A=A7 적용 룰(기준선 — 버그 수정 전 룰을 기준 삼으면 LLM이 부당하게 유리해지므로 반드시 A7 이후), B=LLM-퍼스트, C=본 RFC 하이브리드. 카탈로그·랭커·골든 고정(해석만 변인).
- **지표**: 필드 정밀도/재현율, 연산자 정확도, 부정 정확도, **하드 제약 위반율(허용 0)**, **미지원 필드 날조율(<0.5%)**, 스팬 불일치율, 되묻기율(과다 방지 상한 — 초기 15%), 폴백률, p50/p95 지연, 질의당 비용, 동일 질의 일관성(캐시 미스 기준), 최종 추천 회귀(골든 diff).
- **수용 게이트(Phase 3 진입)**: C가 A 대비 필드 재현율 +10%p 이상(슬랭·모호 클래스), 하드 위반 0, 날조 <0.5%, p95 추가 지연 <1.5s, 골든 diff 전건 사람 승인.
- **중지/롤백**: 운영 중 하드 위반 1건 발생 즉시 플래그 off(설정 1개 — 코드 롤백 불요), 날조율 >1% 또는 되묻기율 >25% 시 Phase 하향.

---

## 11. 단계별 도입 · 최종 판정

```text
Phase 0  A7 룰 수정 + 평가셋 500 구축 + A 기준선 측정          ← LLM 없이도 무조건 실행
Phase 1  LLM 파서 섀도 모드 (호출·검증·로깅만, 서빙 무영향)      ← auradin_events에 parse_shadow 적재
Phase 2  룰/LLM 불일치 분석·라벨링 → 게이트 측정 (2~4주 데이터)
Phase 3  게이트 통과 시 소프트 필드 한정 활성화 (플래그, 트래픽 일부)
Phase 4  전체 기본 활성화 또는 기각 — Phase 2 증거로 결정
```

**최종 답변**: "raw prompt → rules"에서 "raw prompt → LLM → deterministic rules"로 바꿔야 하는가? — **아니오, 그 형태로는 바꾸지 않는다.** 하드 제약의 정본은 룰에 남기고, LLM은 룰이 소비하지 못한 표현의 **검증된 소프트 해석·정규화·모호성 신고**로 한정해 단계 도입한다(조건부 채택). 근거 요약: ① 전환 동기의 3/8은 룰 수정으로 닫힌다 [확인], ② LLM 우위 영역의 절반은 카탈로그 커버리지에 절단된다 [확인], ③ 지연·폴백은 기존 아키텍처가 흡수 가능하다 [확인], ④ 실트래픽 분포 부재로 활성화 기대 효과는 미정이다 [실험 필요 가설 — Phase 1-2가 해소].
